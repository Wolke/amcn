# AMCN 最終評比與整合架構

| 欄位 | 內容 |
|---|---|
| 文件 | 三提案評比（SDD §24）與最終整合架構決議 |
| 日期 | 2026-09-05 |
| 輸入 | `docs/proposals/` 三份完整提案＋三份獨立深讀評審報告 |
| 評選方法 | 每案由提案 Agent 自評、由未參與撰寫的獨立評審逐頁深讀複評（含交付物完整性、矛盾挖掘），最後由整合者裁決。依 SDD §24 要求逐面向比較，不以文件長度取勝（三案皆約 1,500 行，長度已中性化） |
| 結論 | **以提案 B（MVP-first）為工程骨幹，移植提案 C 的經濟層與驗證市場、提案 A 的架構原則與稽核模組**；三案各有評審實證的缺陷，整合前必修清單見 §4 |

---

## 1. 評比結果

### 1.1 §24 逐面向分數（自評 / 獨立評審 / 整合者裁定）

| 面向 | 權重 | A 自評/評審/裁定 | B 自評/評審/裁定 | C 自評/評審/裁定 |
|---|---:|:---:|:---:|:---:|
| 解決臨時額度耗盡的真實需求 | 20% | 7 / 6 / **6** | 8 / 8 / **8** | 7 / 6 / **6** |
| Key／帳密與本機環境安全 | 15% | 9 / 8 / **8** | 8 / 8 / **8** | 8 / 7 / **7** |
| Agent 自主閉環程度 | 15% | 8 / 8 / **8** | 8 / 8 / **8** | 7 / 7 / **7** |
| Mutual Credit 經濟可行性 | 15% | 7 / 6 / **6** | 6 / 6 / **6** | 8 / 7 / **7** |
| MVP 可實作性 | 15% | 6 / 4 / **4** | 9 / 8 / **8** | 6 / 5 / **5** |
| 去中心化與可替換性 | 10% | 9 / 8 / **8** | 5 / 5 / **5** | 6 / 5 / **5** |
| 驗收、反作弊與可稽核性 | 10% | 8 / 6 / **6** | 6 / 5 / **5** | 9 / 8 / **8** |
| **加權總分** | | 7.55 / 6.50 / **6.50** | 7.35 / 7.10 / **7.10** | 7.15 / 6.40 / **6.40** |

整合者裁定全數採納獨立評審分數。理由：三位評審均完成逐頁深讀、每個扣分都附提案章節證據，且一致地發現了提案自評未揭露的實質矛盾（A 案 7 項、B 案 10 項、C 案 8 項）；相比之下自評分數系統性偏高 0.4–1.05。特別說明兩處大幅下修：

- **A 案 MVP 可實作性 6→4**：45–55k LOC 的新穎分散式系統 ÷ 72 人週，且 W9–10 兩週要塞五個子系統、Firecracker 承諾未排程、無形式化方法人力卻要交 TLA+。這不是「緊」，是大概率交不出 §20 十條驗收。
- **C 案 Mutual Credit 8→7、驗收 9→8**：機制框架是三案最強，但「攻擊者期望值 < 0」的三張關鍵成本表被評審逐一算破（幣值錨定差 10 倍、旗艦分錄不守恆、金絲雀成本低估約 40 倍）——結論方向可信，數字必須重算。

### 1.2 逐面向勝者與理由

| 面向 | 勝者 | 一句話理由 |
|---|---|---|
| 真實需求 | **B** | 唯一給出需求側具體入口（MCP server 3 tool／3 人日）與 429/quota 觸發的完整實作路徑；A 的協議開銷 P95 11–40s 在「現在很急」場景踩線，C 的緊急路徑疊了太多反作弊摩擦 |
| Key 安全 | **A/B 並列** | A 的程序級隔離（Sandbox 與 Keystore 不同程序、單向窄介面）是結構性設計；B 的封包側錄＋日誌掃描驗收是可證偽的驗收法。整合架構兩者都收 |
| 自主閉環 | **A/B 並列** | 兩案的還債策略、目標餘額區間、全 transition 逾時定義都完整；C 的預批門檻與人工爭議略多 |
| 經濟可行性 | **C** | INV-C1、T_flow 信任流、四層壞帳瀑布、六種經濟攻擊逐一參數化——是唯一把 Sybil 防禦搬到額度層並可稽核的框架（數字待修） |
| MVP 可實作性 | **B** | 逐週交付物、砍單順序、「永不砍」清單、4.5 FTE 相稱範圍；評審仍估超載 40–60%，但遠優於另兩案 |
| 去中心化 | **A** | 九層分析＋「官方消失後」欄＋拔線演習；「哪幾層不可晚做」的判準是三案唯一說清楚的 |
| 驗收反作弊 | **C** | seeded-random＋commit-reveal＋雙向金絲雀＋偏離 slash＋tx_class 誠實計量，且「稽核稽核者」為獨有設計 |

**依 SDD §27 判準的最終裁決：選 B 為骨幹。** §27 說得明白：核心不是 Token、區塊鏈或 Marketplace UI，而是 Agent 自主完成的互惠信用閉環。三位評審一致認定 B 是「最可能在 12 週內真的跑出閉環」的提案；A 案評審的原話是它應作為「原則來源與經濟／稽核模組供應者，而非直接採納的 MVP 實作路線」；C 案評審的原話是其經濟機制「應成為整合架構的骨幹」但證明尚未成立。三個結論拼起來就是本文件的整合方案。

---

## 2. 整合架構決議

### 2.1 最高決策原則（採 A 案 §25 Q11，全文照收）

> **不可事後補裝的層（第一天就要做對）：身分、帳本語義、收據格式、驗收權分配。** 因為遷移成本＝全網信任重置。
> **可以先中央化的層（綁定退場三件套）：撮合、傳輸、索引、託管。** 每個中央化元件必須配齊：協議內發現與輪替機制、至少一個開源替代實作、全部狀態可由公開簽署事件重建。

### 2.2 分層決策表

| 層 | 採用 | 設計要點 | MVP 誠實現況 |
|---|---|---|---|
| **Identity** | A 案 | did:key（Ed25519）＋Root/Hot 分離＋UCAN 0.10 委派鏈（子任務縮小委派）＋三層撤銷（短效期／gossip 撤銷／高額線上查驗） | 第一天即去中心化，零基礎設施依賴 |
| **Transport / 撮合** | B 案 | 出站 WSS 至 Coordination Hub，E2E 加密（Hub 只見 metadata）；`ITransport` 介面抽換，**W10 強制交付第二實作＋拔線演練（A 案演習納入）** | 中央化。**第二實作已交付**（`lib/transport-{tcp,http}.js`，`demo-transport.js` 斷言兩傳輸產生同一本帳）；4 小時拔線演練仍未做，見 §4 #10 |
| **Ledger 語義** | B 案骨架＋A/C 補強 | 複式帳、每組 posting Σ=0、雙簽 SettlementReceipt、per-account hash chain、每小時公開 Merkle checkpoint；Hub 是「第一個排序器」不是信任根。**負餘額支出採 C 案不對稱一致性**：使餘額更負的支出需經序列化確認（MVP 由 Hub 擔任、Phase 2 起 3–5 席可替換 Witness 聯邦 2/3 共簽），正餘額支出走雙簽＋樂觀對帳 | 排序中央化；語義（收據格式、守恆規則、checkpoint 驗證）第一天即最終版 |
| **單方 posting 合法性**（修 A①/B⑤） | 新規則 | Hub/協議可執行的非雙簽 posting（demurrage、違約罰、announce 費、沖銷）**必須引用 Owner 預簽的標準政策 Grant**（加入網路時簽署的費率表 UCAN）；審計重放時無對應 Grant 的單方 posting 一律判無效。Hub 從「能單方改帳」變成「只能執行被預簽的規則」 | — |
| **Credit Line** | C 案（修數字） | INV-C1：`credit_limit ≤ k_earn × E_eff + collateral + L_boot`，**L_boot 明文列為不變式的有界例外**（**參數 starter_cc = 50 CC；t=0 的有效額度 = 25 CC，經 30 天年齡斜坡升至 50**、Treasury 補貼科目、tx_class=subsidy）；E_eff＝多樣性折減（單一對手 ≤20%）× T_flow 信任流加權；A 案的 WilsonLower95＋1−HHI 併入 E_eff 實作 | GATE-0 定案 starter 參數 = 50。**先前寫的「上限 25 CC」不是錯誤**，它描述的是 t=0 的有效額度（50 × 0.5 年齡係數）——我在 #1 的第一次裁決把兩者混為一談並改錯了這一格，E5 的跨語言對照才把它釐清。詳見 #1 與 #60 |
| **初始信用**（修 B③） | 新規則 | 貢獻任務**只給聲譽，不給信用額度**；CL 只能來自押金、Vouch 連帶（擔保人 50% 連帶、同時上限 5 個）、多樣性折減後的已驗收淨貢獻。堵死「Treasury 付錢的工作反向養出可違約額度」 | — |
| **Verification** | B 案 deterministic-first＋C 案市場 | 驗收 DSL（assert 集在 TaskSpec 簽章時鎖 hash、Provider 可預跑）＋Attestation.reasons 指向 assert index；Judge quorum **列入「永不砍」清單**（修 B⑦：它是三條例外路徑的唯一出口）；抽選種子＝**未來一輪** checkpoint hash（修 A④ grinding）＋commit-reveal；Verifier 報酬顯式列入每筆 postings（修 A⑦/C⑤）；金絲雀抽查以**重算後的成本**編列並限定高風險對手方分層抽樣（修 C⑦ 的 40 倍低估） |
| **防拒付** | A 案＋C 案 | 成交時 Requester 預簽 `pre_authorization`（驗收通過即結算）；Requester 拖延時 Provider 持「合約＋交付＋attestation＋預簽」證據包強制入帳——在本整合中入帳者是負餘額序列化器（Hub/Witness），與 C 案 ADR-006 的路徑衝突（C⑥）就此消解：**強制入帳一律走序列化路徑，無論餘額正負** |
| **Reputation** | A 案 | 事件層（對手方/Verifier 簽章、錨定收據 CID）與評分層（模型任選、可替換）分離；同對手重複交易次線性折損 |
| **誠實計量** | A/C 案 | 收據強制 `tx_class: market / test / subsidy / related-party`；writeoff 為一級科目；模擬與生產共用指標定義（FR-083、§20-9/10） |
| **Treasury 啟動**（修 B②/C 相關） | 新規則 | Treasury 創世餘額 0；啟動期支出（金絲雀、逆週期收購、L_boot 補貼）計入**有治理上限的「創世補貼額度」**（Treasury 在該額度內可為負，全部 tx_class=subsidy）；熔斷規則改為「淨部位觸及補貼上限」而非「餘額 <0」，否則網路第一天即熔斷 |
| **Stablecoin/L2** | 三案一致 | Phase 3 選配 Escrow 擴充點；MVP 不上鏈、不發幣。狀態機差異：Stablecoin 模式在 AWARDED 加 `ESCROW_LOCKED` 子狀態 |
| **Governance** | A 案立場 | 協議層不存在全網凍結；「讓 fork 便宜」；最低版本政策與簽章更新（NFR-010）走 B 案的發布管線 |

### 2.3 狀態機

採 B 案狀態機為基準（全 transition 有呼叫者/簽章/逾時），疊加三個修正：

1. `VERIFYING` 逾時的備援 Verifier 與 default 分支保留 A 案規則（deterministic 任務 default-accept if evidence 自帶、主觀任務 default-reject）。
2. `ACCEPTED → SETTLED` 增加強制結算路徑（見上表防拒付列）。
3. Reservation（C 案負餘額預留）統一時序：**發標前**向序列化器取得 Reservation（金額＝max_price），`AWARDED` 時折算為實際成交價，TTL 綁合約 deadline＋緩衝而非固定 15 分鐘（修 C④）。

---

## 3. Phase 0 模擬（已實作）

`sim/amcn_sim/` 已依 SDD §19 實作並通過煙霧測試（120 agents × 21 天）：

- 守恆 Σ=0、全帳可由簽署事件重建：PASS（§20 驗收 4 的模擬級證明）
- 借用→負餘額→額度恢復→還債閉環成立，還債週期中位數 ≈ 3 天
- 已實作情境：`baseline` / `expiry_cliff`（月底同日到期崩價）/ `high_default`（15% 跑路）/ `wash_heavy`（20% 洗量對）

**待辦**：以整合後的 C 案公式（INV-C1＋E_eff）替換現行簡化 credit_limit；跑全量（500–10,000 agents × 84 天）驗證 GATE-0 八判準。全量模擬前先與 Owner 確認執行。

---

## 4. 整合前必修缺陷登記簿

三位評審共發現 25 項實質矛盾。按「不修就不能開工」分級：

### P0（阻斷開工）

| # | 來源 | 缺陷 | 整合修法 |
|---|---|---|---|
| 1 | B①/C③ | 新戶信用額度數字三處打架；INV-C1 被 L_boot 形式違反 | **已裁決並於 2026-09-17 修正**（見 #60）：**starter 參數 = 50 CC，t=0 的有效 L_boot = 25 CC，30 天內斜坡升至 50**。原裁決寫「L_boot = 50」是錯的——它把參數與 t=0 的有效額度混為一談，而那正是本條目一開始要消滅的形態。原裁決的依據仍然成立：依據本欄原本的修法——「單一數值表由 GATE-0 模擬定案」——而 GATE-0 的證據與 §2.2 先前寫的 25 CC 相反，且方向反直覺：`sim/out/sweep.csv` 掃描 starter ∈ {10,20,50} × 費率 × deadbeat ∈ {5,15,30}%，**提高 starter 反而降低壞帳率**（30% deadbeat 下 starter 10→50 使壞帳 5.27%→2.23%），因為流動性成長（結算量 5.4k→32.9k CC，6 倍）快過壞帳絕對值，違約損失被市場規模稀釋；starter 10 同時餓死市場又放大壞帳占比（`phase0-results.md` §結論 2/5）。三處現已一致：§2.2 改為 50、`node/lib/eeff.js` 的 `STARTER_CC` 本來就是 50、模擬器同值。**t=0 的實際額度是 46.25 CC**（50 × quality prior 0.925），這個差異是刻意的，不是誤差。**仍未關閉的兩件事另行登記**：(a) L_boot 的 Sybil 維度沒有證據，見 #50；(b) ~~`tx_class=subsidy` 科目在 node 端完全未實作~~ **已於 2026-09-17 交付**：收據強制標示四種 tx_class 之一，Hub 內部分錄標 `protocol`、金絲雀付款標 `subsidy`，市場指標依此分流（§20-9／§20-10）|
| 2 | B②/C② | Treasury 創世即負、熔斷自觸發；CC/USD 錨定差 10 倍 | §2.2「Treasury 啟動」列；全案統一 1 CC = US$0.05，三張攻擊成本表重算（W2 交付） |
| 3 | A① | 非自願扣帳在自簽鏈中無法落地、守恆不封閉 | §2.2「單方 posting 合法性」列（預簽政策 Grant） |
| 4 | B⑦ | Judge quorum 在可砍清單卻是例外路徑唯一出口 | 移入「永不砍」；最小三人池 W9 交付 |
| 5 | C⑤/A⑦ | 驗證費在旗艦分錄中憑空消失／三套數字並存 | **已交付**（W9）：收據新增 `acceptance_method`／`verifier_pool`／`verifier_pool_hash`／`panel_seed_cp`，judge-quorum 每筆結算必須付給**由 Hub 自行推導**的 panel（成交價 4%、均分，從 provider 毛額扣除），dsl-local 不得有 verifier 分錄；`validateSchedule` 強制此規則，雙簽與強制結算路徑共用。`demo.js` 有對應斷言。**費率 4% 取 proposal-C 3–6% 中點，尚無模擬證據**——模擬器沒有 Verifier agent，見 #25 |

### P1（開工後兩週內修）

| # | 來源 | 缺陷 | 整合修法 |
|---|---|---|---|
| 6 | A④ | Verifier 抽選種子可 grinding | **未來 checkpoint 已交付**（W9）：合約不再指名 panel，只釘住 pool 雜湊與一個**尚未產生**的 checkpoint 序號；panel 由該 checkpoint 的 root 推導，Hub 在強制結算時**重新推導**而非採信合約名單。`node/lib/panel.js` 為雙方共用規則，`demo.js` 斷言「合約時已知序號 < 種子序號」且實際 attester 全在推導結果內。commit-reveal 亦已交付（見 #26）|
| 7 | A⑤/C⑦ | Verifier 押金 vs 經手上限差 20 倍；金絲雀成本低估 40 倍 | 經手上限 ≤ 押金×3；金絲雀改分層抽樣並重編預算 |
| 8 | B⑥ | 抽查率 0.13% 撐不起威懾宣稱 | 同上＋高風險對手方加權抽樣 |
| 9 | C⑥ | 不對稱一致性與強制入帳互斥 | 強制入帳一律走序列化路徑（§2.2） |
| 10 | B④ | 「Indexer 停止」驗收是循環論證 | **部分已修**：`demo-rebuild.js` 的驗收不是「Hub 能否重啟」，而是**另一個進程、另一個埠**能否僅憑簽署產物重建出相同餘額、鏈與信用額度，並偵測偽造品——實測 7 收據／28 事件／8 帳戶零不符，竄改檔被拒。**第二個 `ITransport` 實作已交付**（W10）：`lib/transport.js` 定義介面（`listen`／`dial`／`probe`）、`transport-tcp.js` 是原行為、`transport-http.js` 是長連 chunked NDJSON＋POST。刻意不選另一種 socket 方言——那共用 TCP 的故障模型，換了等於沒換；HTTP 線上無連線狀態、送達以請求為單位、POST 需自行保序。關鍵斷言不是「兩種都連得上」而是**同一場閉環在兩種傳輸下產生同一本帳**：`demo-transport.js` 比對 fingerprint（收據／事件／餘額／額度／驗收方式），實測 `2ea62f9f…` 完全相同，且四支 demo（21／12／6／7 項）在兩種傳輸下全過。抽取過程本身暴露一件事：語義原本散在傳輸層裡——frame 切分、版本閘門（#33）、handler 隔離（#13／#34）全都寫在 `wire.js` 的 socket 讀取器內，若讓第二實作各自複製一份，兩份帳分岔只是時間問題；因此移到 `lib/channel.js` 由兩者共用，傳輸層只准提供 write／close。混用傳輸時雙方都會明確拒絕（tcp 端回 HTTP 400、http 端回一行 framed `transport_error`）——這是 #36「靜默卡住比崩潰難查」的教訓。**仍缺 4 小時真實拔線演練**，Owner 裁定等第三台機器到位再跑（2 台雖足以驗證「官方基礎設施消失後能否由簽署產物接手」這個主張，但切換後全網跑在單一主機上，2-of-3 quorum 在部署層仍是裝飾——見 #31）。演練排程前有三項前置：**(a) #40 重連**，否則量到的是手動重啟時間而非接手能力；**(b) 匯出必須先離開 Hub 那台**（`HUB_DUMP_PATH` 寫本機檔，而受災節點就是它——第二台以 `ledger-dump.js <out> <hub-ip> <port>` 定時拉取即可，無需改碼）；**(c) 接手者的 Hub 身分**——pinned agent 依設計拒絕非 pinned DID，所以要嘛預先把 `HUB_SEED` 複製到待命機（「第一個排序器」的鑰匙成為多台共有的秘密，本身是應登記的安全性質），要嘛接受 pinned agent 不跟隨並手動重新 pin。演練必須記錄走的是哪一條。另需明確：拔線後受災節點**不得**再接回同一網段，否則測到的是雙排序器分叉而非接手 |
| 11 | A②③ | 雙花上限低估 5 倍、未見證折損對 equivocation 無效 | 整合架構負餘額支出走序列化，此攻擊面整類消失；正餘額路徑保留分叉偵測作稽核 |
| 12 | B⑩ | Key 隔離的 IPC 方向寫反 | 採 A 案程序模型：Prompt 處理程序無 Key，經單向窄介面提交推理請求給持 Key 的 Adapter 程序 |

（其餘 P2 級文字/排程不一致 13 項，開發中隨頁修正；完整清單見三份評審報告原文。）

### Phase 1 原型實作發現（非評審來源）

上列 1–12 項來自三份獨立評審。以下由 `node/` 的 Phase 1 原型與兩台機器試點實作過程發現——評審讀不到的層級，只有跑起來才會暴露：

| # | 級別 | 缺陷 | 狀態與修法 |
|---|---|---|---|
| 13 | **P0** | Hub 對畸形 frame 會整個進程崩潰：`createPublicKey()` 遇到壞 DER 是丟例外而非回傳 false，例外從 socket `data` handler 逃出即 uncaught。Hub 綁 `0.0.0.0` 時，區網任一裝置送一行垃圾就能打掉整個網路（掛在上面的 Verifier 全數斷線） | **已修**（`lib/wire.js` 驗簽不丟例外＋frame 層隔離＋socket error 處理＋行長上限；`demo.js` 回歸閘門） |
| 14 | **P1** | 中央化 Transport／撮合層缺「協議內發現與輪替機制」——§2.1 把它列為中央化的綁定條件，§2.2 裁定 B 案時未交付。Hub 位址只存在各機設定檔，換位址等於全網手改 | **已修**：發現＝區網 UDP 簽署信標（`lib/discovery.js`，`hubHost: "discover"`，跨機實測可行見 #18）；輪替＝`HUB_SEED` 讓 Hub 身分跨重啟不變（`identityFromSeed`，本來為金絲雀授權而加），`hubPin` 因此第一次有跨生命週期的意義——搬移 Hub 只要保留 seed，所有釘住的 agent 會跟著新位址。Hub 啟動 log 一律印出 `hub did`（不再取決於 beacon 是否開啟）|
| 15 | **P1** | `contract_id` 可重複：任務 id 為 `t-<設定名>-<seq>`，`seq` 每次啟動歸零且設定名不唯一，導致兩個不同 DID 持有相同 contract_id 的收據。W1 已凍結的 schema 中 contract_id 是結算冪等鍵，識別碼不唯一將使 W9 爭議、W11 紅隊重放、W12 §20 證據包出現雙重計算 | **已修**（id 併入 DID 標籤；Hub 以 `settledIds` 拒絕重複結算，雙簽與強制路徑共用同一守門；`demo.js` 重放斷言） |
| 16 | **P2** | Hub 的 `receipt`／`task`／`forced_settlement` handler 對缺必要欄位的 frame 會丟例外。目前由 #13 的 frame 層 try/catch 接住並記錄為 `[wire] dropped frame`，但缺逐欄位驗證，錯誤訊息對送出方也不具指引性 | **已修（2026-09-18）**：派發前逐型別檢查「handler 會直接解參照、缺了就會拋例外」的欄位，回覆指名缺哪幾個（`forced_settlement: missing required field(s) "receipt", "provider_sig", "evidence"`）。**刻意只列這些欄位、不做完整 schema**：目標是把「靜默丟棄」換成「指名缺什麼」，而簽章與語意檢查仍然留在各自的 handler 裡——在派發層重做它們會讓同一條規則有兩個實作（#60 的形態）。**原本的失敗形態值得記**：進程不會死（G1／G3 驗過），但**送出方什麼都學不到**，所以「設定檔少打一個欄位」跟「網路有問題」長得一模一樣。**第一版把 `task` 的 `to` 列成必填，而 task 是廣播**——於是所有任務被拒、市場整個停掉，紅隊連真實收據都拿不到。列必填欄位本身就是一次規格宣告，寫錯的代價是把合法流量當成畸形流量。回歸閘門 G14（空的 `forced_settlement` 與只帶 `did` 的 `collateral_post` 都必須被指名拒絕）。**紅隊因此成為 block 41、known-open 0** |
| 17 | **P2** | Hub 帳本與 Agent 身分皆不持久化：帳本在記憶體、`export` 有出口無 import 入口，`agent.js` 每次啟動 `genIdentity()` 產生新 DID。任一邊重啟即歸零，且被棄置的負餘額身分會在帳上留下永不償還的洞（試點實測留下 +16.52 CC 花不掉的正餘額與 −10 CC 不會還的債）| **身分已修（W10 前半）**：`agent.js`／`verifier.js`／`panel.js` 接受 `seed`（`identityFromSeed`），Hub 用 `HUB_SEED`；搭配 #35 的「同 DID 重註冊沿用既有 stats」，重啟後餘額與信用額度歷史都延續。**測試時發現身分穩定並不夠**：agent 自己的餘額檢視仍從 0 開始，而策略引擎（FR-055 區間、還債模式）與 `planPurchase` 都讀它——帶著真實負債重啟的 agent 會以為自己在 0、不進入還債模式、並高估可支用額度。Hub 現在於註冊回應中交還 `balance_cc`／`stake_cc`／`settlements`，agent 採用之。實測：同 seed 重啟後 DID 與 −8.00 CC 餘額皆延續，Console 與 Hub 視角一致。**帳本 export→import 已交付**（W10 後半）：Hub 匯出完整帳務事件流（此前只有收據，而託管／沒收／金絲雀付款無法由收據推導——這正是 W10 揭露的缺口），`HUB_IMPORT` 讓第二排序器重建並**驗證而非信任**（逐筆驗簽、pubkey 自證、鏈重算、信用額度由 stats 重放，唯 checkpoint 需以 `hub_pub` 驗章）；`ledger-dump.js` 匯出、`demo-rebuild.js` 7 項斷言。竄改的匯出檔會使 Hub 拒絕啟動。**另加 `HUB_DUMP_PATH` 週期自動匯出**——export→import 交付後，試點 Hub 隨即在沒人執行 `ledger-dump.js` 的情況下下線，那本帳照樣丟了：需要人記得執行的災難匯出不是災難復原。自動匯出寫 temp 檔再 rename，崩在寫入中途不會留下截斷檔取代可用檔 |
| 18 | **P1** | 原登記：跨機 UDP 發現無法使用。**已證否**——`discovery-probe.js` 在原本失敗的那台實測：真實 hub_beacon 跨機抵達、簽章 OK、時差 1020ms（容許 60s），且 `resolve` 模式成功發現 Hub | **已撤銷**：登記本身有瑕疵——當初是從「機器 2 未出現在註冊清單」推論發現失敗，從未取得該機的 log，commit message 還寫入未經驗證的成因（AP isolation 等）。仍然成立的部分：`demo.js` 的發現檢查只驗同機、`af5836a` 宣稱「Verified on the real LAN」為錯誤陳述，該檢查已更名。診斷工具保留，若再發生可用 `listen`／`resolve` 定案 |
| 25 | **P1** | Verifier 費率 4%（#5 交付值）沒有模擬支持：`sim/amcn_sim` 完全沒有 Verifier agent | **已修**：模擬器加入 Verifier 族群（押注、收費、偷懶行為、金絲雀懲罰），`amcn_sim.sweep_verifier` 掃描 fee {0,2,4,6}% × canary {0,1,3,5}% × lazy {0,33}% 共 32 組。**4% 獲得支持**（與 #20 相反）：fee 0% 時驗證市場不存在（零收入）；4%＋canary 3% 下誠實者 56 天最低淨收 42.60 CC、偷懶者被抓率 62.5%、G3/G4/G5 全 PASS、成交率 97%、經手/押金比 1.17（≤3，滿足 #7）。CSV 在 `sim/out/sweep_verifier.csv` |
| 27 | **P1** | 金絲雀懲罰誠實者的一般能力誤差：零偷懶者情境下 fee 4%＋canary 3% 仍沒收 20.21 CC（占 verifier 收入 4.6%）。proposal-C 的「canary_failed 即沒收押注 10%」不區分誤差與作弊 | **已修**：改為門檻式——需先看過 `slash_min_samples`（預設 5）個金絲雀，且失敗率 ≥ `slash_threshold`（預設 25%）才沒收。實測完美分離：誠實 7 位失敗率 **2/77 = 2.6%、沒收 0.00 CC**；偷懶 2 位失敗率 **10/16 = 62.5%、沒收 21.79 CC**。偵測率不受影響（仍 62.5%），門檻在 0.10–0.60 之間結果一致（安全帶寬）|
| 36 | **P0** | commit-reveal 的揭示流程會**永久放棄**合約：寬限計時器（1500ms）觸發時若承諾不足 2 份即記錄後返回、不重試；而唯一的另一個觸發條件是「panel 全員都承諾」，因此最後只湊到 2 份承諾的合約永遠不會揭示，結算靜默卡住。只在真實跨機延遲下出現——localhost demo 的承諾一定在 1500ms 內到齊。實測：Windows panel 上線後 16 次 `cannot reveal`、21/39 筆任務未結算 | **已修**：計時器只標記「寬限期已過」而不再放棄；「寬限期已過 ＋ 達到 quorum」成為獨立觸發條件；寬限期改為可設定（`policy.revealGraceMs`，預設 3000ms）|
| 37 | **P1** | 空 pool 讓 judge-quorum 靜默降級為零驗證：`validateSchedule` 的 quorum 檢查包在 `if (panelDids.length)` 裡，pool 為空時整段跳過，收據便以 judge-quorum 名義在**沒有任何 attestation** 的情況下通過，帳本記錄成 quorum 結算。惡意 requester 只要釘住空 pool 即可繞過驗證。發現時機：查「無 verifier 在線時 12 筆任務為何全部卡住」 | **已修**：Hub 要求 judge-quorum 的 pool ≥ `PANEL_SIZE` 否則拒絕結算；Agent 端也拒絕在 pool 不足時得標（否則會產生永遠無法結算的合約，而 log 完全看不出原因——那 12 筆就是這樣）|
| 38 | **P1** | 「從收入託管押注」（#28 的資金模型）使**新身分幾乎沒有東西可失**：實測偷懶者累積 0.288 CC 押注、被沒收 0.288 CC，而 `SLASH_FRAC × STAKE_TARGET = 0.5 CC` 根本取不到——沒收上限是已累積的押注，所以全新身分可以先抽費一段時間才開始有代價。這是 earn-your-stake 的內生性質，也是一個 Sybil 角度：棄置身分重開比累積押注便宜 | **已修（2026-09-22，原型：沒收與退還都有閘門）**——本欄開頭一度停在「未修」，而右欄自己的後半已經在描述交付內容（`demo-forfeit.js` 14/14、協定 v7 的 `stake_release`、紅隊 S19／S20），這正是 #23 那個教訓的又一次：**登記簿的狀態欄也會過期，而它過期的方向是把做完的事記成沒做**。仍然開著的是量出來的那一半：逃逸點約 2 天（見下）。原始選項（歷史）：(a) 要求最低押注才能進 panel（回到 pay-to-play，原型辦不到）；(b) 以未實現收入為抵押（未託管的費用先凍結）；(c) 押注未達標前降低 panel 選中機率。需以模擬決定。**2026-09-21 已量化，而本欄原本的表述要修正兩處**。**(1) 穩態下偷懶嚴重不划算**：300 agents／20 verifier／84 天，偷懶者淨收 1.45 CC 對誠實者 46.71，**優勢 −45.26**，偵測率 0.76。本欄引用的「偷懶者累積 0.288 CC 押注」來自原型 30 秒的 demo——那時它根本還沒賺到錢，那個數字量的是**帳戶年齡**，不是威懾力。**(2) 但本欄點名的「棄置身分重開比累積押注便宜」是真的，而且門檻很低**。沒收有前置條件：`slash_min_samples=5`、`slash_min_failures=3`——**第 5 次金絲雀之前完全免費**。模擬「換身分」（金絲雀計數歸零、已賺的費用保留，那正是棄置的意思）：不換 → 淨收 1.45／沒收 187.9；7 天 → 11.32／148.4；3 天 → 40.44／32.0；**2 天 → 47.01／5.7，偷懶優勢轉正（+0.30）**；1 天 → 48.08／1.4。**逃逸點約 2 天**：沒收崩掉 97%，而偷懶者拿到的與誠實者相同（47.01 對 47.39）**卻完全不用做事**。而 #50 同日確認身分創建是免費的，所以這條路沒有成本。**推導出的要求**：要讓每 2 天換一次不划算，押注必須在**達到 `slash_min_samples` 之前棄置時被沒收**——84 天換 42 次、收益約 47 CC，所以每身分只要沒收 > 1.1 CC 即可。**門檻低得驚人，關鍵不在金額而在「未達標就棄置要有代價」這個規則本身**。本欄選項 (c)（押注未達標前降低 panel 選中機率）未測——它會改動 `panel_for` 的確定性雜湊抽選，屬協定變更而非模擬旋鈕。**2026-09-21 修法已在模擬器驗證：規則是「早退不退押注」，不是「加重懲罰」**。未達 `slash_min_samples` 就棄置身分時，已託管的押注轉入保險池；待到樣本數達標才離開的**照退**，所以它不罰誠實退場者。實測（20 verifier、偷懶比例 0.2、三種子）：**不換身分時規則完全不生效**（淨收 1.45、優勢 −45.26、沒收 187.9，開關一致）——正確，因為它只針對早退；**3 天換**：淨收 40.44 → **2.86**，優勢 −6.27 → **−43.85**；**2 天換（原本的逃逸點）**：淨收 47.01 → **1.81**，優勢 **+0.30 → −44.90**，沒收 5.7 → **186.5**；**1 天換**：淨收 48.08 → **0.72**，優勢 **−45.98**。**換得越快現在對攻擊者越糟**（1 天比 2 天更差），因為每次棄置都賠一份押注——逃逸路線從「越快越安全」反轉成「越快越貴」。**原型已實作（2026-09-22），而實作過程發現本欄漏了一半**：`hub.js` 的離線掃描多一條分支——離線超過 `HUB_STAKE_FORFEIT_AFTER_MS`（預設同違約門檻 14 天）、且 `canary_stats.seen < HUB_SLASH_MIN_SAMPLES` 的 verifier，託管的押注整筆轉入保險池（事件類別 `stake_forfeit`）。**漏掉的那一半：原型的押注從前只進不出**（託管 → 罰沒），所以「不退押注」在原型裡是**純記帳**——那筆 CC 早就不在 verifier 的餘額裡，沒收與不沒收對離開的人一模一樣。嚇阻要成立，就必須存在**拿得回來**的情形，因此同日補上 `stake_release`（協定 **v6 → v7**）：被測夠（`seen ≥ SLASH_MIN_SAMPLES`）且失敗率未達罰沒門檻者可取回託管的押注，**取回即退出 pool**（抽走押注還繼續驗證的人身上沒有東西可罰，那是 pay-to-play 的反面）。要回來就得從新的驗證收入重新託管，也就是回到新人的處境。歸屬記在 `canary_stats.forfeited_cc`，因為重建把「各自持有多少押注」定義為託管減去流出、並以 Σ持有 ＝ `protocol:stake` 交叉檢查；`lib/rebuild.js` 另加一條「`slash` 與 `stake_forfeit` 的事件總額必須各自等於其歸屬欄位」——只比總和的話，把一筆沒收改記成罰款不會被發現，而那會把一個早退者寫成作弊被罰的人。閘門 `node demo-forfeit.js`（約 70 秒，14 項斷言）**14/14**，它測的是一個 2×2 而不是一條規則：**離線 × 未被測夠 → 全額沒收 0.0217 CC 轉入保險池**；**在線 × 被測夠 → 取回 0.2851 CC 回到自己的餘額、退出 pool**；**離線 × 被測夠 → 押注留在託管、未被沒收**（0.2437 → 0.2437，退出不是規避）；**在線 × 未被測夠 → 取回請求 3/3 被拒、押注只增不減**（沒被測到不是罪，但也不能提前把押注抽走）。另含守恆、押注帳務一致、獨立重建、以及「把沒收改記成罰款會被重建拒絕」的竄改對照。**閘門自己寫壞過三次，而每一次都是會讓它看起來通過或看起來壞掉的那種壞**：(a) 寫死「殺 V4」，但 panel 是每筆合約從池子推導的，那一次剛好每次都抽到 V5，於是 V4 押注是 0——關鍵三條斷言全部在空資料上「通過形式」；改為從帳上挑受害者並先斷言前置條件。(b) 用「一買一賣」的流量，買方 22 秒把額度用光（全程 6 筆收據），晚到者進不了 panel——改為三個雙向交易者讓 CC 循環。(c) 斷言「取回後押注必須為 0」，但取回只對**新的** pool 生效，在飛的合約仍會結算並託管——正確行為被判成 FAIL；改為「現有押注 ＝ 取回之後才發生的託管」。**退還本身帶出一條比 #38 更省事的攻擊，而第一版是通的**：取回押注後只要斷線**重新註冊**，就能帶著乾淨的受測紀錄（樣本 12、零失敗）回到 pool 而身上押注為 0——罰也罰不到東西，連換身分都不必。成因是「已退出」旗標掛在 agent 記錄上，而 `register` 會整筆換掉它。修法：退出的身分存在獨立集合並**可由事件流重建**（匯入時掃 `stake_release` 事件），於是 **取回押注 ＝ 結束這個 DID 的驗證生涯**；要再驗證就得用新身分，沒有受測紀錄、也重新暴露在沒收規則下。閘門第四階段實測：V1 取回後重新註冊，pool 仍只有 V3／V5／V6。**唯一不該存在的組合是「乾淨的受測紀錄 ＋ 身上零押注」**，沒收、退還、退出即永久三件事合起來就是在守這一句。紅隊補兩案（`redteam.js` 45 案）：S19 非 verifier 的取回被指名拒絕；S20 一個**簽章完全有效**的退還請求從別的連線送出時押注分文未動（0.0713 → 0.0713）。**退還的拒絕理由也修了一次**：原本先檢查「有沒有押注可退」再檢查樣本數，於是一個還沒託管到押注的新 verifier 得到的是「nothing held」——拒絕的理由變成偶然的託管時序，而真正的規則從回覆裡消失 |
| 39 | **P2** | `demo-canary.js` 證明的是**機制**（偷懶被辨識、押注被沒收、誠實者未受罰），不是**威懾充足性**：原型中「偷懶」不省下任何成本（mock adapter 無論如何都是即時的），所以無法回答「罰款是否大於作弊收益」。該問題只能由模擬器回答（目前：偵測率 72–87%）| 未修，屬定義問題而非缺陷。已在 demo 檔頭與此處註明，避免把機制驗證誤讀為經濟結論 |
| 35 | **P0** | Hub 沒有任何斷線處理，`agents` 只增不減——因此**離線的 Verifier 仍會被抽選**：panel 由合約時釘住的 pool 推導，選到已消失的 verifier 就永遠拿不到 attestation，低於 2 份可歸責 PASS 即拒絕結算，網路靜默卡死。發現時機：要把 panel 從機器 1 搬到 Windows 機器（#31），停掉舊 panel 會讓 pool 變成「6 個裡 3 個是死的」，多數 panel 的 quorum 將不可能成立 | **已修**：socket `close` 時標記 `online = false`（不刪除——stats 餵信用額度，刪掉會讓重連後標準歸零而餘額留存），`list_verifiers` 只提供在線者，同 DID 重新註冊時沿用既有 stats。已簽署合約仍可能含離線成員，但 2-of-3 容許一個 |
| 33 | **P0** | 協議訊息完全沒有版本欄位，混版節點以**崩潰**而非拒絕收場。實測：pre-W9 provider 收到 W9 合約後在 `c.verifiers.length` 拋例外並死在合約中途 | **已修**：`lib/wire.js` 定義 `PROTOCOL_VERSION`，`sendLine` 在信封層自動蓋上 `v`（呼叫端無法漏），`attachLineReader` 拒絕版本不符或無版本的 frame 並給出「請所有機器一起升級」的可操作訊息。**刻意放在信封層而非簽署內文**——`receipt`／`contract`／`pre_auth` 的簽章是對那些物件本身計算的，加欄位會讓所有既有簽章失效且需雙方就位置達成一致；信封是未簽署的轉發 metadata，本來就是版本協商該待的地方。Hub 與 Agent 啟動 log 都顯示版本；`demo.js` 有 v99 被拒＋v1 正常的斷言 |
| 34 | **P0** | #13 的 frame 層 try/catch 只接住**同步**拋出。`agent.js` 的訊息 handler 是 `async`，任何 async throw 都變成 unhandled rejection 並殺掉進程——#21 只針對 adapter 那一條路徑修，通用情況一直開著。#33 的崩潰就是這個機制放大的：一個欄位不存在，整個 agent 死在合約中途 | **已修**：`attachLineReader` 對 handler 回傳的 promise 掛 `.catch`，async throw 降級為 `[wire] dropped frame`。單元測試：async handler 拋例外後進程存活且後續 frame 正常處理 |
| 32 | **P2** | `configs/verifier.example.json` 從第一個 commit 就在版控裡，但 `verifier.js` 只讀 `AGENT_CONFIG` 環境變數——沒有任何程式路徑能載入那個範本。連帶後果：每個 Verifier 都必須用 shell 引號的 JSON 啟動，而 bash 的單引號形式在 PowerShell 不成立，Windows 機器實質上無法照文件操作 | **已修**：`verifier.js` 與 `agent.js` 採同一套解析（env 優先，否則讀 `process.argv[2]` 指定的檔案）；範本改為 `verifier-{1,2,3}.example.json`，INSTALL 第 6 節附 PowerShell 指令 |
| 31 | **P1** | 試點的三個 Verifier 全部跑在機器 1：同一台機器、同一個 OS、同一個 process owner、同一個故障域。因此「panel of 3 verifiers」與「2-of-3 quorum」在**部署層級上是裝飾**——單一機器故障或單一 Owner 被入侵即可同時控制整個 panel，而 FR-041 的隨機抽選正是為了防這件事。`demo.js` 與 `demo-autonomous.js` 同樣把全部角色跑在一台 | 未修。需把 Verifier panel 分散到獨立的機器／Owner；試點若有第三台機器，最高價值的用途就是承載 panel 而非再開一個交易 Agent |
| 28 | **P2** | 押注並未真正託管：slashing 取 `min(押金×10%, 餘額)`，導致威懾強度與費率耦合——費率越低威懾越弱，這是實作產物而非設計意圖 | **已修（node 端）**：新增 `protocol:stake` 帳戶，每筆驗證費按 `HUB_STAKE_ESCROW_FRAC`（預設 50%）託管至 `HUB_STAKE_TARGET_CC`（預設 5 CC）為止。**設計決定並非照抄提案**：proposal-C 假設 verifier 先繳押金，但原型中 verifier 起始 0 CC 且無信用額度，「先繳後做」不可能；改為從收入累積——新 verifier 風險小、收入也少，靠工作累積到完整地位，與信用額度同構。託管走獨立的 posting set（不折進收據，因為收據是雙方簽署的內容，Hub 不得事後改動），且是收據流的**確定性函數**，所以 §20-4「可由簽署事件重建」仍成立——`demo.js` 的重建套用同一條規則後通過。模擬器端仍為舊模型 |
| 29 | **P2** | 金絲雀比例的單位與提案不符：proposal-C §7 指定占**全網量** 2–5%，模擬器的 `canary_rate` 是每 tick 注入機率，實測只產生 0.5–1.3% | **已修**：`canary_rate` 改為「占結算量的目標比例」，注入以該比例為控制目標（含抖動避免可預測的節奏）。實測追蹤精確：2%→2.00%、3%→2.99%、5%→4.99%。**代價被揭露**：按提案真正的比例，金絲雀從約 12 筆增至 72–176 筆、沒收從 22 CC 增至 106–120 CC——提案的設定比模擬器原本的行為激進得多 |
| 30 | **P2** | 偷懶者被抓率與 pool 大小強耦合，9 個 Verifier 的數字不可外推 | **已修（並推翻原本的擔憂）**：修好 #29 後金絲雀**絕對數量隨網路量成長**，pool 放大到 90 時每人仍看到 6.4 個樣本，偵測率維持 72%（pool 9/30/90 分別 71%/87%/72%）——#8「抽查率撐不起威懾」在「占量比例」的定義下不成立。但揭露了新問題：pool 90 時每人樣本僅 6.4，25% 的比例門檻在小分母上失去統計意義，誠實者被沒收回升到 25.85 CC，**#27 的修復在規模化時自我失效**。已加上絕對失敗次數要求（`slash_min_failures = 3`），誠實者誤罰降到 4.55 CC （pool 9/30 為 0.00），偵測率不變。**縮放律**：每人樣本 ≈ 金絲雀數 × panel_size ÷ pool_size，故 `canary_rate` 必須隨 pool 規模上調才能維持證據門檻——大型網路不能沿用小池子的比例 |
| 26 | **P2** | Verifier 報酬按**推導出的 panel** 均分，而非按實際出具 attestation 者。2-of-3 quorum 成立時，缺席的第三位仍領到報酬 | **已修**（W9 commit-reveal）：Hub 只付給「揭示內容能開啟事前簽署之承諾」的 verifier（`validAttesters`），沉默者零收入；少於 2 份可歸責的 PASS 即拒絕結算 |
| 24 | **P0** | 未來-checkpoint 抽選的 bootstrap 死鎖：週期 checkpoint 原本以 `if (chains.size)` 為條件，而 `chains` 只在第一筆結算後才有內容。全新網路若第一筆任務就要 quorum，種子 checkpoint 永不產生 → 無法驗收 → 沒有第一筆結算。`demo.js` 未踩到（第一筆是 `dsl-local`），只有 `demo-autonomous.js` 全 quorum 才暴露 | **已修**：無條件鑄造 checkpoint（空 heads 的 checkpoint 完全合法，root = hash of `{}`）|
| 21 | **P0** | `agent.js` 的 `provide` 若沒有可解析的 adapter，得標後才在 `adapter.complete(null, ...)` 崩潰——而合約當時已雙簽，requester 只能等強制結算。同一個 await 在 async handler 內，**任何** adapter 錯誤（含真實端點回 500）都會變成 unhandled rejection 殺掉整個 agent | **已修**：無 adapter 則不武裝供給（不能執行者不得出價）；執行失敗改為記錄並標記合約 `failed`，不再毀掉進程 |
| 22 | **P1** | `agent.js` 出價完全不檢查自己剩餘額度，可以賣出根本沒有的算力。模擬器 `collect_offers` 一直有 `remaining_quota < 1.0` 守門，node 端沒有——W8 的無人運行才暴露（額度模型存在但不約束供給側） | **已修**：出價前要求 `quota.remaining >= units`，得標時扣除；Console 新增 `sold_units` |
| 23 | **P2** | 選標同價時以到達順序決勝（`bids.sort` 為穩定排序），使先啟動的 Agent 在同質價格市場中系統性勝出。三節點 demo 中最後啟動者永遠拿不到單，是啟動順序而非市場性質 | 未修。選標是 requester 自身職權（FR-012），先到先服務可辯護，但同質市場會產生贏者全拿。建議改為以 `sha256(contract_id + did)` 決勝（驗收 panel 抽選已用同一手法）。**2026-09-20 稽核發現這一列本身過期了**：修法其實早就上線——`agent.js` 的選標在價格與信譽相同時以 `sha256(task.task_id + provider)` 決勝（`agent.js:1173`），正是本欄建議的那個手法，而 `redteam.js` 的 **G15** 以 block 等級守著它。**但那條閘門是原始碼層的**（regex 比對 `agent.js` 的字串），它自己的註解也承認「行為版——兩個 provider 同價同歷史、反覆看誰贏——屬於下一批」。所以正確的狀態是「**修法已交付、閘門偏弱**」，而不是「未修」。這一列的存在本身就是 #69／#75 那個教訓的另一個面：**盤點表的狀態欄也會過期**，而它過期的方向是「把已經做完的事記成沒做」——那比反過來安全，但一樣會誤導排序 |
| 20 | **P1** | W7 的還債策略參數三處不一致：還債折價 proposal-B §8.4 寫 10%、`sim/amcn_sim/market.py` 一直用 35%、node 原型照提案實作 10%；band 下界 §8.4 寫 −0.3×CL、模擬器用固定 −5.0 CC；band 上界 §8.4 寫 +100、模擬器用每 agent `uniform(30,120)`。同 #1「新戶信用額度數字三處打架」的形態 | **已修**：`amcn_sim.sweep_repay` 掃描 折價 {0,10,20,35,50}% × band {fixed, −0.15CL, −0.30CL} × {baseline, high_default} 共 30 組。結論：**折價 35% ＋ band 下界 −0.15×CL**。§8.4 的兩個數字都被推翻——10% 在 high_default 下 G3 全滅（壞帳 6.5–7% > 保險收入 4.6–4.7%），−0.30×CL 每個折價檔都 G4 FAIL（還債週期 33–40 天）。35% 勝過 50% 在還債週期（14d vs 25d）與末週價（0.69 vs 0.53，50% 壓垮價格 41%）。node `lib/strategy.js` 與模擬器預設均已對齊，CSV 在 `sim/out/sweep_repay.csv` |
| 40 | **P0** | **沒有任何重連機制**：`resolveHubTarget` 只在啟動時解析一次，`dialLazy` 取得 channel 後不再管它。Hub 一消失，`chan.send()` 只回傳 `false`，而所有呼叫端都忽略回傳值——agent／verifier／canary 從此**靜默失聯**：不重連、不重新發現、不出聲，Console 仍顯示最後已知餘額。發現時機：評估 W10 拔線演練可行性時追「Hub 被拔線後 agent 會怎樣」。連帶後果：#14 交付的「發現與輪替」在運行期不可用——輪替只有在**所有進程同時重啟**時才生效，而這正是輪替該避免的事；#19 的「WARNING: not connected」只覆蓋啟動期，斷線後不會再說話 | **已修**：`dialLazy` 改為持有**解析函式**而非一次性 promise，每次重連都重新解析目標——所以 `hubHost: "discover"` 會重新聽信標，搬移過的 Hub 不改設定檔就會被跟上（#14 的輪替第一次在運行期成立）。退避 500ms→15s（±20% 抖動，否則整個 panel 會同步重試）、斷線與重連都有 log、重連時呼叫 `onOpen` 重發 `register` 並重新索取 `list_verifiers`（pool 與 checkpoint lock 在離線期間都動過）。**離線期間的送出改為丟棄並記數，不排隊**——原本計畫排隊，但沒有任何協議物件帶有效期（另案登記），長時間離線後重播一批過期的 bid 與 attestation 比丟掉更糟，而且在要跑數小時的進程裡無界緩衝本身就是缺陷；關鍵是「丟棄」現在會被計數並在重連訊息中報出，而非隱形。`demo-reconnect.js` 8 項斷言：SIGKILL 掉 Hub、6 秒後以同一 seed 從自動匯出重啟，六個 client 全部自行重連、重新註冊、餘額延續（實測 A 的 −5.93 CC 未歸零）、並在接手的 Hub 上產生新結算——全程無人重啟任何進程。**修的過程自己踩到兩個坑**：(a) 重試計時器原本 `.unref()`，而 Verifier 除了那個 socket 沒有任何 handle 撐住事件迴圈，於是 Hub 一死 verifier 進程就靜默結束——正是試點那個「panel 消失」的失敗換一件衣服；(b) 移除連線前排隊後，`canary.js` 模組載入時就送出的 `list_verifiers` 被丟棄，金絲雀因此看到空 pool——所以「重連要重建哪些狀態」必須明列，不能靠隊列補 |
| 50 | **P1** | **L_boot 的 Sybil 維度沒有證據**：#1 裁決 50 CC 的依據是 GATE-0，而 GATE-0 的人口是**固定的 400 個 agent**，deadbeat 是其中的外生比例。那個模型量的是「既有身分違約後，壞帳占結算量多少」——它**無法定價「攻擊者鑄造 N 個身分各領一份 L_boot」**，因為 N 在模型裡不是攻擊者的選擇變數。實作上每個新身分 t=0 即有 46.25 CC 無擔保信用，而身分是免費的（`genIdentity()`），所以單身分攻擊收益上界 46.25 CC、成本趨近 0。#38 已登記同一形態的另一半（棄置身分比累積押注便宜），紅隊盤點的 E1 標為 known-open 也是這條 | 未修。需要把身分創建變成**內生**的模擬：攻擊者選 N、每身分有成本（押金／擔保人連帶／時間），量「期望收益 < 0」的門檻。這是 proposal-C §10 六種經濟攻擊的框架要做的事，但 `sim/amcn_sim` 目前沒有攻擊者 agent。在那之前，50 CC 這個值只在市場健康維度上有證據，在反 Sybil 維度上沒有——**裁決時就要說清楚，而不是讓它看起來兩邊都成立**。**2026-09-21：身分數已內生化，而答案是「N 不是槓桿，只是重複」**。`sim` 新增 SYBIL 行為：一個攻擊者控制的 N 個身分，**產能近乎零**（不提供真實服務，提供了就等於做了工、不是白拿）、高需求、**協同同日退場**、環狀互相交易以試圖灌 E_eff。N 做成參數而非人口比例——它是攻擊者的選擇變數，這正是既有模型量不到它的原因。**實測（200 agents、84 天、三種子）：每身分白拿 45.79（N=1）／45.86（N=20）／45.88（N=100），離散 ±0.02——完全不隨 N 變動**。環狀互相交易沒有灌大 E_eff，與 #70 量到的「環狀有效率 0%」一致。所以協同**不提高單位收益**，攻擊只是線性重複。**但網路損害隨 N 線性成長**：G3（壞帳 ≤ 風險費收入）在 **N ≥ 20** 就破（壞帳 917 vs 保險 573），N=100 時壞帳率 0.20。**即使攻擊對攻擊者無利可圖，規模仍能打穿保險池**——池子是按有機違約率設計的，不是按協同攻擊。**防禦已驗證**：白拿 ＝ 45.86 − 保證金×(1−LTV)，每一個測點都對得上；LTV 0.5 時保證金 92 讓白拿降到 **0.01**，攻擊完全歸零。**量測過程修正一個會讓攻擊隱形的陷阱**：第一版的退場時點抽到第 1981 tick（全長 2016），而沖銷要等 14 天——落在模擬結束之後，於是**攻擊確實發生了、報表上卻是 0**。那不是「攻擊不划算」，是量測窗口把它切掉了 |
| 53 | **P2** | **強制結算的授權鏈從未被離線驗證**：`demo.js` 的簽章斷言把收據過濾成 `kind === 'dual'`，所以 forced 收據（`sigs.requester` 存的是 `pre_auth:<sig>`，requester 從未簽收據）的整條授權鏈——pre_auth 簽章是否有效、是否綁到這份合約——**沒有任何測試從匯出檔驗過**。發現時機：`flaky-relay` 情境產生了第一筆**自然發生**的強制結算（demo 只有腳本安排的拒簽），我的不變式檢查器把它誤判為「requester 簽章壞掉」，追下去才發現是檢查器沒覆蓋 forced 路徑 | **已修**：`lib/invariants.js` 的 `receiptSignatures` 對 forced 收據驗 provider 簽章＋`evidence.pre_auth` 的簽章，並檢查 pre_auth 綁定的 `contract_id`／`requester`／`provider`／`condition`——「有效簽章但授權的是別的合約」才是有意思的攻擊，畸形的不是 |
| 52 | **P1** | **協議每一步都是射後不理，沒有端到端重送**。TCP 給的是**逐跳**可靠，而每個訊息都經 Hub 中繼：Hub 重啟、對端重連（#40 依設計丟棄離線期間的送出）、或中繼層丟訊息時，那一步就永久消失。實測（`flaky-relay` 2% 中繼丟包）：一開始 180 秒 **0 筆結算**，合約卡住 153 秒。逐一補完才恢復——缺口依序是：合約授權（`contract` 丟失＝provider 不知道自己得標，requester 等一個不可能來的交付）、交付（`delivery` 丟失＝工作做了但沒人收到）、**panel 種子 root**（checkpoint 廣播丟失＝無法推導 panel，且沒有任何逾時，因此連放棄都不會發生）| **已修**：三個握手都改成「確認＋重送」，與 #49 同一形態——(a) `contract` 重送至收到 `contract_ack`，provider 對重複得標**只重發確認、絕不重複執行**（並附帶重發已有的交付）；(b) `delivery` 由 requester 以新增的 `delivery_request` 索取重送；(c) 新增 `checkpoint_request`，requester 可向 Hub 索取遺失的種子 root（Hub 稀疏儲存，故回覆帶 `for_seq`，見 #41）。三者都有重試上限與明確放棄，逾時常數全部進 `policy.timing`。修後同條件 14 筆結算、不變式零違反 |
| 51 | **P2** | **chaos 的 `loss` 模式不忠實**（工具缺陷，非產品缺陷）：原本以位元組區塊為單位丟棄，而一個區塊可能切在 JSON 行中間——那會造成**串流損毀**，是 TCP 保證不可能發生的事。所以它測的是一個不存在的故障，而且會產生誤導性的失敗 | **已修**：改以 frame 為單位丟棄，並在註解裡寫明它模擬的是**中繼丟訊息**（Hub 重啟、對端重連時 in-flight frame 消失），不是網路丟包——逐跳可靠仍由 TCP 提供。這條記在登記簿而非只改程式，是因為「一個無法讓目標失敗的故障工具比沒有工具更糟」的反面同樣成立：**會產生假故障的工具會讓人去修不存在的問題** |
| 49 | **P0** | **註冊是射後不理**：`register` 送出後沒有任何確認機制。故障注入下的實際後果：一個 verifier 在靜默中斷期間重連成功（TCP 連上了），它的 `register` frame 被丟棄；故障解除後雙方的活性 ping 開始互通，於是兩端的閒置計時器**再也不會超時**——連線活著、雙方都報健康，而 Hub 完全不知道對方是誰。那個 verifier 永久隱形，pool 停在 2 位、低於 `PANEL_SIZE`，Agent 因此拒絕得標（#37），全網停止成交。發現方式：`panel-blackhole` 情境在修好 #48 之後仍然「恢復後無成交」，逐一比對三個 verifier 的 log 才看出 V1 在某個時刻之後完全靜止——**這種比對只有在 log 落地時做得到**，也是 chaos-run 把每個子行程輸出寫檔的理由 | **已修**：`dialLazy` 新增 `ackType`，把註冊變成需要確認的握手——送出後若在 `handshakeMs`（預設 5s）內沒收到 `registered` 就重送，直到收到為止，並記錄「連線已建立但匿名」。一個丟失的 frame 本來就足以造成這個狀態，而丟包只需要做到這件事 |
| 48 | **P1** | **VERIFYING 沒有任何逾時**：合約在驗收階段卡住時，requester 與 provider 都會無限期持有它。實測（`panel-blackhole`）：panel 連線被靜默切斷期間得標的合約全部永久開啟——三個 Agent 各累積 12–18 筆，最舊的 155 秒且持續成長，因為 `verify_request` 送進虛空而沒有任何重試。連帶後果：`asRequester`／`asProvider` 兩個 Map 只增不減，長時間執行即記憶體洩漏。§2.3 第 1 點要求「VERIFYING 逾時的備援 Verifier 與 default 分支」，原型一項都沒有 | **部分已修**：requester 在 `verifyTimeoutMs`（預設 15s）後重送 `verify_request`，重試 `verifyRetries`（預設 2）次後放棄——標記合約結束、計入 Console 的 `abandoned`、並印出明確原因；provider 端有對應的放棄計時器。**刻意只做有界的那一半**：panel 由釘住的 pool＋種子推導，重試打到的是同一批（可能已死的）verifier，真正的修法是 §2.3 的**備援 panel（改用後續 checkpoint 重新推導）**，那需要 Hub 端配合驗證新種子，屬協議變更。放棄時 provider 已完成工作卻拿不到報酬，這是目前的誠實代價，log 明說 |
| 47 | **P1** | **時間常數以 loopback 校準**：選標視窗是固定的 500ms。在 80ms±40 的跨地點延遲下（兩端各注入一次，單跳約 160ms），task→bid 來回就要 320ms＋抖動，於是**每一筆任務都 `no bids`**——同一個拓撲無延遲時 60 秒成交 27 筆，加上延遲後 90 秒成交 **0** 筆。與 #36 同型（1500ms 寬限期在真實延遲下卡住），但後果更糟：不是卡住而是市場根本不存在。而 Owner 裁定的驗收環境正是不同地點的機器（見 §7 於 fault-injection-plan）| **已修**：選標改為事件驅動而非猜測——bid 停止到達 `bidQuietMs`（預設 250ms）後才決標，但不早於 `bidWindowMs`（500ms，所以區網行為不變）也不晚於 `bidWindowMaxMs`（3s）。「必須同時適用於 loopback 與 WAN 的常數」這種東西應該被移除而不是重新調校。`forceAfterMs` 與 `verifyTimeoutMs` 也一併變成 `policy.timing` 可設。實測修後同條件 90 秒 **33 筆** |
| 55 | **P2** | 活性 frame 原本叫 `ping`／`pong`，與應用層型別共用命名空間——任何名為 `ping` 的協議訊息都會被 channel 層靜默吞掉。發現方式：測試裡把應用訊息命名為 `pong`，結果收不到，追了一輪才發現是撞名 | **已修**：改為 `_ping`／`_pong`，底線前綴保留給 channel 層。靜默吞掉別人的訊息是最難查的失敗形態之一（同 #46／#49 的教訓），命名空間要在協議層就分開 |
| 67 | **架構裁決題**（Owner 提議，2026-09-18）| **動態產生帶美元上限的 API key，直接出借給借方**。Owner 的原話是「讓 Agent 自己操作 API 去動態生 key，帶限定多少美元抵用額度，常駐 agent 在有人要的時候動態設定 key 然後把它出借過去」。**這個提議與 SDD 三處明文衝突，而且是本專案自己列為「不合格」的那一條**：P-02「模型 API Key、瀏覽器登入狀態及個人帳密**不得離開資源擁有者的裝置**。協議交換的是請求、成果與簽章，**不是帳號或金鑰**」；§1 開篇「這是一套多邊互惠信用清算機制，**不是帳號出借、API Key 買賣**」；P-10「不得把禁止轉售或禁止共享的消費型帳戶包裝為可交易資產」。CLAUDE.md 的判準也寫明「任何方案若破壞 Key 不離機（P-02）即不合格」。**但這個提議指出了一個 SDD 沒處理的區分**：P-02 的危害模型是「長效、無上限的憑證洩漏 → 無界損失＋帳號被封」，而一把 $1 上限、10 分鐘到期、專案隔離的子 key 的危害是**有界的**——它更像 OAuth 的 scoped token 或預付憑證，不是帳密共享。所以它不該被一句「違反 P-02」擋掉，而該被當成**修憲提案**處理 | **建議：不採用「出借廠商 key」，改採「出借自家 gateway 的 key」**。理由不是純粹的原則潔癖，而是三個具體代價：**(a) 供應商條款（P-10／§16 威脅 15）反而更糟**——以子 key 轉供第三方正是多數廠商條款直接禁止的行為，而且比「provider 在本機代跑」更容易被歸因；**(b) §16 威脅 2 放大**——借方送什麼內容不再由出借方控制，而帳單與法律責任仍在出借方；**(c) 驗收機制整組失效**——交付物從「成果」變成「capacity」，DSL assert 沒有輸出可驗，quorum 與金絲雀（它測的是 verifier 有沒有真的看）都沒有對象，§20-7 需要另一套驗收方法（探測 key 有效性與額度），結算基礎也要從「單位」改成「授予預算」或「事後實際用量」，後者還需要一條全新的爭議路徑。**中間路線保住 P-02 且達成同樣的人機效益**：出借方在本機跑一個 OpenAI-compatible 的 gateway，對外發的是**指向自己 gateway 的 key**（帶 CC／美元上限、到期時間、用量記錄），廠商 key 永不離機、上限由自己精確執行而不依賴廠商是否支援硬上限。原型的 `adapter.baseUrl` 本來就是 OpenAI-compatible 端點，借方的 agent 不需要任何改動就能用。**已查證（2026-09-18，`docs/evaluation/key-lending-verification.md`）——而決定性的不是技術面**：硬上限確實普遍存在，OpenAI 專案層有「Enforce a hard limit」達標回 429、Google 的分層月上限甚至是**強制**的、OpenRouter 的 `POST /api/v1/keys` 帶 `limit` 就是提議要的那個原語（Anthropic 例外：workspace 花費上限只在控制台，Spend Limits API 限 Enterprise）。**但 OpenAI 的條款明文禁止這個用法**：「customers cannot buy, sell, or transfer API keys from, to, or with a third party」、「You may not make account access credentials available to third parties… or resell or lease access to your account」。所以**硬上限解決的是財務風險，不是契約風險**，而契約風險無法用工程手段繞過——這正是 P-10 與 §16 威脅 15 早就寫下的，查證只是把「原則顧慮」變成「條款明文」。**同時要誠實**：gateway 中間路線解決的是憑證問題（P-02），**不解決轉售問題（P-10）**——用自己帳號替第三方跑推理在多數條款下仍可能構成轉售，那是 AMCN 從第一天就有的前提，P-10 的處理是把舉證責任放在節點擁有者身上。出借 key 只是在原有問題上**再多違反一條明文的 key 轉移禁令**。**唯一可能乾淨的組合**：上游是把「per-user 帶上限 key」當產品功能的聚合型服務，且其條款允許再交付第三方（尚未逐條查證）|
| 68 | **P1**（由 #67 的查證衍生，2026-09-18）| **代他人執行的上游呼叫沒有歸因，而 P-10 的舉證責任從未在程式裡留下痕跡**。#67 的查證（`docs/evaluation/key-lending-verification.md`）確認出借 key 被條款明文禁止，但也發現禁令那一句自己透露了關鍵：「resell or lease access to your account **or any End User Account**」——**條款本來就預設客戶會有終端使用者**。被禁止的是共用憑證與轉售存取，不是「替自己的使用者跑推理」。所以節點替他人執行工作時真正該做的不是讓流量混進自己的流量裡（那是規避，被抓到比誤判嚴重得多），而是**讓它帶著身分抵達**。原本 adapter 對上游一個字都不說：沒有 end-user 識別、沒有合約關聯，而且 `provide` 只要 key 解得出來就上膛，P-10 要求的「擁有者已確認其上游條款允許」在設定檔與程式裡**完全沒有位置** | **已修**：(a) `adapter.complete` 收 `{endUser, contractId}`，代他人執行時把 requester 的 DID 放進 OpenAI-compatible 的 `user` 欄位（`attribution: 'openai'` 另加 `safety_identifier`，`'none'` 供本地模型；`user` 在該 schema 裡存在多年所以第三方相容伺服器容得下，新欄位改為選配以免嚴格伺服器拒收）。DID 是公鑰雜湊，本身就是穩定且假名的識別碼——正是濫用歸因欄位想要的形狀，而且讓節點擁有者能把供應商的報告對回自己帳上的合約。(b) 真實上游＋代他人執行必須 `adapter.terms.attested === true`，**而且閘門設在上膛而非執行**——與 #21 同一個理由：執行時才拒絕會讓 requester 對著一個從頭就不該接單的 provider 強制結算。mock 與本地模型豁免（沒有上游協議要遵守）。(c) 回歸閘門兩道：`demo.js` 從**接收端**讀回歸因（3 次呼叫、3 次帶 DID、0 次未標示，且 DID 皆為帳上 requester）——斷言「供應商真的收到了」而不是「設定檔有寫」；`redteam-agents.js` B3 讓一個未聲明條款、掛全場最低價 0.2 CC/unit 的 provider 進場，實測**一單未得、上游 0 次呼叫、log 明寫拒絕上膛**（兩個獨立證人，因為「沒得標」也可能只是輸在價格）。**誠實的界線**：(b) 是**聲明而非驗證**，沒有任何東西能確認擁有者真的讀過合約；它的價值在於把 P-10 的責任下推變成**有紀錄、可稽核**的設定檔事實，而不是假設。**它也沒有解決 #67 的根本問題**——「代第三方執行推理是否構成轉售存取」仍未解，見該文件 §3 |
| 69 | **P0**（W11 第三批 F6 發現，2026-09-18）| **§2.2 的「排序器不是信任根」在 checkpoint 這一層是空話**。盤點表把 F6（equivocation）標成 `block`，理由寫「per-account hash chain＋checkpoint，分叉可離線偵測」——**寫了測試才發現那句是假的**，而且兩種攻擊都不需要偽造任何東西：**(a) 截短的歷史**：把最新一筆結算從 `receipts` 與 `events` 裡拿掉，其餘產物全部真實、簽章全部有效，`rebuild()` 照樣回 `ok: true`。checkpoint body 裡明明有 `receipts_count` 自報當時有幾筆，但**沒有任何地方拿它跟收據數對照**。**(b) 同 seq 兩份互斥的 checkpoint**：排序器持有私鑰，所以它永遠能簽出兩個 root 都有效的版本——這件事攔不住，而 §2.2 賭的從來不是「攔得住」而是「偵測得到」。但 checkpoint body 是 `{seq, heads, root, receipts_count}`，**彼此之間沒有任何連結**，所以兩個分支完全等價，除非你剛好同時持有那組相撞的 pair | **(a)(b) 已修**：`makeCheckpoint()` 加 `prev_root`，`rebuild()` 加兩項檢查——checkpoint 自報的 `receipts_count` 不得大於匯出檔實際收據數（`<=` 而非 `===`，因為 checkpoint 按計時器鑄造，最新那筆結算合法地還沒被 checkpoint 到）；相鄰**已存**條目的 `prev_root` 必須接上前一個的 `root`。鏈接在「已存」而非「已鑄造」的條目上，是因為稀疏儲存（#41）會丟掉未變動的 root，連到一個從未保留的 root 等於連到觀察者驗不了的東西——鏈覆蓋的範圍剛好是任何人真的檢查得到的範圍。效果：分叉不再需要持有相撞的 pair 才看得出來，**任何一份對面分支的後續 checkpoint 就夠了**（其 `prev_root` 對不上）。舊匯出檔沒有這個欄位，缺欄位不算錯（與 `checkpoint_seq` 同一條相容規則）。回歸閘門 F6a／F6b。**(c) 已於 2026-09-18 交付（`lib/cpwatch.js`）**：(a)(b) 讓分叉「對持有雙邊產物的觀察者」可偵測，而協議裡**沒有任何東西會讓誰持有雙邊產物**——每個節點都從同一個 Hub 單向接收 root，節點之間沒有任何訊息載送 checkpoint 視角。所以偵測可能、但無人偵測。修法就是原本寫的那個：節點在**既有的**對等訊息上附帶自己見到的最新 `(seq, root)`，收到的一方與自己的比對，對不上就出聲。掛在信封上、不進簽署本體，所以不影響任何既有簽章；統一在連線層加戳而不是逐個 case 改，因為漏掉任何一種訊息就等於在那條路徑上沒有偵測。verifier 也納入——`attestation` 是它唯一送給對等節點的訊息，也正是串謀最在意的那一條。**刻意不是共識**：沒有人投票、沒有任何東西被否決，主張只是「等價於隱形」不再成立。**一個會製造假分叉的陷阱**：鑄造是無條件的但儲存是稀疏的（#41），所以廣播於 seq N 的 root 可能從未被儲存；之後**詢問** seq N 的節點會拿到「≤N 的那一筆」，而 agent 把它記在被問的 seq 上。兩個誠實節點因此可以對同一個 seq 持有不同 root——一個是鑄造值、一個是更早條目的別名。`cpwatch` 把兩者分開，**只比廣播來的**，別名永不參與戳記或比對。這個陷阱在健康網路上會持續誤報，所以 `demo.js` 的閘門是**零誤報**而不只是「抓得到」。**F6c 因此從 known-open 翻成 block**：餵一個真實 agent 一份帶衝突 root 的對等訊息，它回報 1 次分叉（Console 的 `checkpoint_forks`，Owner 看得到）。**這一案測的與沒測的**：它端到端驗證偵測路徑，**不**模擬一個對兩個對等節點供應不同分支的 Hub——那需要一個刻意說謊的排序器，是下一步該做的，不是本案主張的東西。一個收方從不檢查的戳記會通過舊版（讀原始碼）的 F6c 而在新版失敗。紅隊現在是 block 37、known-open 1（只剩 #16）。**這一條的教訓值得單獨記**：F6 在盤點表裡被標成已防禦，而那個判斷是讀程式讀出來的——鏈與 checkpoint 都確實存在，看起來就該能偵測分叉。只有把攻擊真的做出來才看到中間少了一環。與 #60（跨語言 CL 逐步比對推翻我自己的裁決）同型：**自洽的程式不會告訴你它少了什麼** |
| 70 | **P1**（W11 E3 量測，2026-09-18）| **擋住大規模洗量的不是 F-1 折減，是手續費——而 #61 的方向正是調降手續費**。E3 在盤點表裡記著「block？…環狀是否被壓制**未驗證**」，`node/wash-sweep.js` 量完得到兩個相反的結果：**(a) 環狀完全被壓平**。A→B→C→…→A 每人只付下一個，有效率在 N=3 到 24 全部是 **0.0%**——因為每個成員只有一個付款方、而那個付款方把 100% 的支出都給他，w = 1 − 1 = 0。規避成對偵測的直覺做法**一點用都沒有**。星狀（單一金主）同樣 0.0%。**(b) 團狀幾乎完全漏掉**。每人付給每一個其他人時，w = 1 − 1/(N−1)，而 20% 集中度上限在 N≥8 之後不再咬住，有效率從 N=3 的 10.0% 一路升到 N=24 的 **95.7%**；`diversity` 的分母又剛好是 8，所以 8 人以上的團同時拿滿多樣性加成。**F-1 的成對折減對密集互刷是漸近失效的** | **已量測，未修（判斷是不需要修 F-1）**。理由是第三個數字：**槓桿（買到的額度 ÷ 付出的手續費）在所有團狀規模都 < 1**，最高是 N=8 的 0.83。攻擊者最後會違約，所以額度是收益、手續費是成本——製造 560 CC 的假流水買到 21.0 CC 的額度，卻付掉 25.2 CC 手續費。**所以真正的防線是手續費而不是 F-1**，而這帶出必須登記的那件事：**打平費率是 3.76%，實付 4.50%，餘裕只有 1.20×**（N=8，團內成員互為 8 個對手所以不是 thin，付的是 2.5%＋2% 而非 2.5%＋6%）。**手續費總率只要降 17%，8 身分的團狀洗量就開始有利可圖。**而 #61 記的正是「每筆 8.5% 永久離開流通，小網路因此鎖死」——#61 想降費率，E3 說費率是唯一擋住團狀洗量的東西，**兩個需求直接對撞**。任何調降費率的提案都必須連帶回答 E3：可行的方向是**讓折減對團狀也有效**（例如把 w 從成對比例改為看付款方的支出熵，或把 20% 上限的基數從「其他對手加權量」改為「其他對手中最大者」），而不是單純降費率。**兩個實作算出完全相同的數字**（node `lib/eeff.js` 與 sim `agents.py`，N=3…24 逐項相符），所以這不是實作瑕疵而是公式性質。**附帶的小發現**：星狀 N=3 的槓桿 1.10（餘裕 0.91×）是唯一有利可圖的格局，但它跟貢獻無關——有效貢獻是 0，那 1.9 CC 全部來自 `quality` 把 completionRate 從 0.9 的先驗推到 1.0。兩三筆交易就買到，一次性、有上限、金額是 #50 記的免費 23.13 CC 之上的雜訊，所以不單獨處理 |
| 71 | **P1**（#61／#62／#66 的共同根，2026-09-18）| **protocol 帳戶只進不出，所以它們持有的每一塊 CC 都是永久借出去的信用**。推論：在沒有結構性淨賣方的小網路裡，交易者的總負債只會隨成交量單調成長，直到全部貼上信用上限——成交量被 `Σ額度 ÷ 費率` 封頂。原型 40 分鐘 soak 正是這個結局（292 筆後最後 12 分鐘零成交，三人全部貼牆，protocol 帳戶 +122.53 CC）。§2.2「Treasury 啟動」把回流列為設計的一部分（金絲雀、逆週期收購、L_boot 補貼），從未啟用 | **已在模擬器實作並量測，結果是否定的——而過程中我自己的兩個假設都被打掉**。**(1) 第一個目標選錯了**：我原本要退還 Treasury，量測顯示模擬器裡 Treasury 在 N=3／10／50 **全部是負的**（金絲雀支出大於費收）。真正累積的是**保險池**與 **verifier 持有量**（N=50：52.93 與 121.75 CC，後者是最大的吸收端，即 #62）。原型 soak 的 122.53 CC 裡保險池佔 **75.90（62%）**，因為三個 agent 永遠是 thin、每筆 6% 進保險池。所以 `protocol_rebate` 做成取 `source` 參數而非寫死 Treasury。**(2) 第二個假設也錯了**：我寫「一個只會成長的準備金就是稅」，但量測顯示模擬器裡保險池只有曝險的 **4.8–9.4%**——它不是超額準備，是**還在往足額爬**。正確的目標要綁預期損失（曝險 × 違約率），而 #65 量到的壞帳率是 0.08–2.11%，安全係數 3 倍約是曝險的 6%，而 4.8–9.4% 剛好落在那個帶裡。**單一種子曾顯示 pinned 降 31–37%**，而 5 種子組平均把它推翻（種子離散是均值的 60%，組間差異 <1%——同 #66 的形態）。改用**配對比較**（同種子有無機制，10 種子）後結論明確：Δsettled **+1.16%**（標準差 5.74，即 0.64 SE）、Δpinned **+0.0060**（只有 2/10 改善，方向偏壞）。**沒有效果。** 而且 N=3 一次都沒觸發，因為池子從未達到目標——**那正是整件事的動機所在**。**(3) 真正有用的產出是診斷**：模擬器**沒有重現原型的那個區間**。sim N=3 在 84 天結算 73.5 CC（額度翻轉 1.6 次）並在池子累積起來**之前**就鎖死；原型 soak 結算約 1265 CC 對 122 CC 額度（翻轉約 10 次），池子長到曝險的 62% 才鎖死。**同一個症狀、兩個機制**——與 #61 早先發現「原型 100% 貼牆 vs 模擬 66.7%」源於人口組成同型。**因此這條的閘門不在模擬器而在原型 soak**，也就是說 CLAUDE.md 的「經濟參數以模擬為回歸閘門」在這一條上給出錯的答案，因為模擬器到不了那個區間。明寫出來而不是默默繞過。模擬器程式保留（預設關閉、`insurance_target_frac`／`treasury_rebate_frac`），因為否定結果本身是證據，而且兩邊需要共用定義（FR-083）。**(4) 原型三組並排（2026-09-18，各 40 分鐘、seed 41、完全相同的故障時間軸）——這裡有效果，而且很大**：
| 72 | **P1**（#69c 交付後的實測發現，2026-09-18）| **equivocation 不只是可偵測，它同時是一個拒絕結算的攻擊——而偵測完全不構成緩解**。#69c 讓說謊的排序器不再隱形（`hub-equivocates` 情境：Hub 對一半節點供應分叉的 checkpoint，兩個分支都用真私鑰簽署、都驗得過，三個 agent 全部回報分叉）。但補上 `hub-equivocates-panel` 之後才看到第二層後果：**分叉的 root 讓 requester 與 Hub 推導出不同的 panel**，於是 requester 對著錯的三位扇出，Hub 以自己的視角重新推導（#6／C5 的防線）並拒絕。同樣 300 秒、同樣的說謊 Hub，唯一差別是 pool 5 > PANEL_SIZE 3：**結算 103 → 55（吞吐砍半）、期末開啟合約 1 → 14**，Hub 拒絕理由 80 次 `verifier postings`、40 次 `quorum not met`。**原情境測不到這件事**，因為 pool 3 = PANEL_SIZE 3 時 `derive` 不論 root 為何都選中全部三個——只有順序變、panel 沒變。這是「情境的形狀讓它無法觀察到它要觀察的東西」的又一例（同 #64 的同質人口、#51 的假故障）| 未修，且**修法不在偵測那一側**。好消息有兩個：七項不變式在 equivocation 下全程成立（帳沒有被打壞，只是做不成生意），而且失敗是**明確拒絕**而不是靜默卡住（#46／#49 的教訓已內建），`noStuckContracts` 也通過——14 份開啟合約都比 `maxOpenS` 新，表示 #48 的放棄路徑在回收它們。**缺的是「偵測到之後做什麼」**：§2.1 的退場三件套第一件就是協議內發現與輪替，而基礎設施已經齊全（`dialLazy` 每次重試都重新解析、rendezvous #45、UDP 發現 #18），所以自然的修法是**偵測到分叉即視該 Hub 為不可信並重新解析**。沒有現在就做，理由是它需要第二個 Hub 才測得出有意義的結果（`hub-moves` 情境的範圍），而且假陽性的代價是節點離開一個健康的 Hub——`demo.js` 的零誤報閘門是這件事的前提，已經有了但只跑過短 demo。**2026-09-23：缺的那一半有答案了（#95）**——偵測到分叉之後可以做的事就是**輪替到一個被授權的後繼者**，而那條路現在存在（事先簽好的接手憑證＋待命機自動升格＋client 憑委派鏈跟過去，`demo-succession.js` 11/11）。仍然沒做的是把兩件接起來：`lib/cpwatch.js` 偵測到 equivocation 時**自動**視該 Hub 為不可信並改跟後繼者——那需要第二個 Hub 才測得出有意義的結果，而現在第二個 Hub 終於是一個常態存在的東西。**這一條也是「中央撮合」這個裁決的真實代價的第一個量化數字**：§2.1 允許撮合中央化的條件是「狀態可由公開簽署事件重建」，而那個條件成立時排序器仍然可以**在所有人都看著的情況下**把市場砍半 |
| 73 | **P0**（紅隊 S17／S18，2026-09-18）| **兩個串謀身分可以在自己簽的收據裡多塞一筆，把未參與的第三方抽走——實測旁觀者餘額 −3 CC，而它從未簽署任何東西**。結算驗證器做的是「該有的分錄都在且金額正確」（逐項比對 `expect`），**沒有人問「有沒有多的」**；而 `postings.find()` 只取第一筆，所以同一帳戶的第二筆完全不受約束。Σ=0 仍然成立（攻擊者自己吸收另一半）、雙方簽章全部有效、費率表全部對得上。**這是 #53 那一類的最惡形態：簽章有效，但授權的是別的東西。**發現方式值得記：它不是讀程式讀出來的，是在做 #62 合併角色的第二階段時去確認「付給 verifier 帳戶的分錄就是驗證費」這個代理判斷有多脆弱，順手問了一句「`expect` 有被完整強制嗎」——答案是沒有 | **已修**：分錄集合必須**恰好**等於費率表算出來的集合。兩道守門——(a) 重複帳戶即拒（`postings` 內同一帳戶只能出現一次）；(b) 白名單：任何不在 `expect` 裡的帳戶即拒。`expect` 同時補上 **requester 本人**（金額是恆等式，但白名單只有在完整時才是白名單）。守門放在 `validateSchedule`，雙簽與強制結算兩條路共用。**教訓寫在程式註解裡**：逐項比對永遠只能證明「至少有這些」，要證明「只有這些」必須用白名單——這兩件事在授權檢查裡是完全不同的強度。回歸閘門 S17（旁觀者餘額變化必須是 0）與 S18（重複帳戶）；**S9「攻擊後帳本完全沒動」在修好前確實是紅的**，那是這個洞被證實有效的直接證據。副作用：S5（dsl-local 硬塞驗證費）現在被白名單擋下而不是被費率表擋下——同一案、更強的拒絕理由。全套回歸重跑（結算驗證器的改動）：redteam 41/41、redteam-agents 13/13、demo 26/26、transport 8/8（三種傳輸 fingerprint 仍相同）、autonomous 12/12、canary 6/6、rebuild 7/7、reconnect 8/8、chaos baseline 2/2 |
| 74 | **P1**（#62 階段 4 的副產物，2026-09-18）| **自動匯出每 2 秒把整份歷史重新序列化一次（O(全部歷史)），而成長報表原本會把它誤報成記憶體洩漏**。兩個缺陷疊在一起，先說量測工具那個：`chaos-run` 的成長報表取**首尾兩點相減**，而 `soak-dual6` 的 RSS 序列是 50→151→60→236→108→253——**掉下來就證明記憶體被回收了**，首尾相減卻把它報成 5.3 MB/分的洩漏；反過來，一個無趨勢的鋸齒可以被同一個算法報成任何數字，取決於最後一點落在哪裡。**#41 就是靠這張報表找到的，所以它給出假數字的代價是下一個 #41 找不到**。產品那個缺陷是：`HUB_DUMP_MS` 預設 2 秒，每次都 `JSON.stringify` 整份匯出。雙角色拓撲讓它現形，因為成交更多、匯出檔長得更快（296.9 vs 204.9 KB/分）| **量測工具已修，產品缺陷已定位未修**。**(a) 報表改成三個數字**：最小平方斜率（趨勢）、**地板**斜率（前後半段最小值——GC 之間的最小值才是活躍集）、峰值。真洩漏讓地板單調上升，鋸齒只讓峰值跳動。順手修掉「Hub 被殺掉的取樣點記成 0」——0 會把地板壓到 0，讓這個指標變成「Hub 有沒有死過」。**(b) Hub 自報堆與保留狀態**（`heap_used_mb`／`retained`：事件、checkpoint、鏈分錄、rawLog KB），因為外部 `ps` 看到的 RSS 分不出「活躍集長大」與「V8 沒把已釋放的頁還給 OS」，而那正是判斷洩漏的關鍵。**(c) 結論：沒有洩漏。** 三個拓撲的堆地板都是 8.8–10.7 MB、迴歸 0.04–0.40 MB/分；`rawLog` 停在 2048KB，它的上限確實在生效。**(d) 因果已用實驗確立**：同樣的雙角色拓撲，只把 `HUB_DUMP_MS` 從 2 秒改成 30 秒，**RSS 迴歸從 4.82 降到 1.79 MB/分、地板斜率從 5.96 降到 1.79，而堆完全不變**（8.8→10.7）。RSS 成長是匯出造成的配置 churn。**(e) 未修的那一半才是真問題**：4 小時後匯出檔約 50MB，每 2 秒重寫就是每秒 25MB 的序列化——**Owner 裁定的 4 小時演練會死在這裡**，而這正是 #41 的形態（稀疏 checkpoint 只修掉其中一個實例）。兩個候選修法：**放慢間隔**（最便宜，但代價是崩潰時損失的視窗變長，目前上限 2 秒）；**快照＋附加尾檔**（快照不常寫、每筆新事件即時 append，匯入端重播快照＋尾檔）——那是正確的修法，成本 O(新增) 而非 O(全部)，而且**耐久性比現在更好**（逐筆而非每 2 秒），代價是恢復路徑要改、風險集中在 `rebuild()` 與 `HUB_IMPORT`。選後者。**2026-09-20：本欄三個推估全部兌現**（`scenarios/soak-4h.json`，四小時）：匯出 **51.1 MB**（推估「約 50MB」）、100 KB/s 的預算把快照間隔拉到 **523 s ≈ 8.7 分**（推估「約 8 分鐘」）、尾檔因此保存 0.9 MB 歷史；而「大尾檔重播從未在那個規模被測過」也補上了——第二次重啟重播並驗證 **11,532 筆收據／12,683 個 checkpoint，簽章與鏈全部通過**，`noHistoryLoss` 實測殺掉當下 11,504 → 期末 13,026 且指定的 `contract_id` 仍在帳上。**記憶體的結論在四小時尺度上仍然成立但要補一句**：RSS 地板斜率 −0.10 MB/分（無洩漏），而堆地板從 16.1 長到 43.9 MB——那是**真實的活躍集成長**（事件 579→14,283、鏈分錄 2,324→99,196），因為排序器把全部歷史留在記憶體裡。所以它的上界是「跑多久」而不是「跑多大」，長期部署前需要一個答案 |

**(f) 已交付（2026-09-18）**：快照不常寫（chaos 從 2 秒改為 30 秒），每筆新的 event／receipt／checkpoint／**公鑰**立刻附加到 `<file>.tail`（JSON-lines）。崩潰安全的順序是「寫快照 tmp → rename → 才清空尾檔」，若在 rename 與清空之間崩潰，尾檔會含快照裡已有的記錄——所以每行都帶**目標陣列索引**，只在「剛好是下一筆」時套用，重播因此幂等；寫一半被截斷的最後一行由 `JSON.parse` 守衛跳過。**合併在驗證之前**，所以尾檔一樣要過簽章、鏈、checkpoint 與本條 (a)(b) 的檢查——附加檔不是信任邊界的破口。快照就位後，過期的衍生欄位（`balances`／`chains`／`collateral`／`credit_lines`／`metrics`／`checkpoint_seq`）被刪除讓 `rebuild()` 重算，否則 `collateral` 會直接讓驗證失敗。

**三個錯都是測試抓到的、讀程式抓不到**：(i) 第一版**沒帶公鑰**，而公鑰無法從事件推導、快照又寫在任何人註冊之前 → 439 筆重播成功但 96 個「provider signature invalid」。`demo-rebuild` 照不出它，因為那支的匯出檔本來就完整；要靠「歷史只存在於尾檔」的情境（`tail-recover`）才會現形。(ii) 第一版的斷言用整輪樣本的 `Math.max`，包含重啟後的樣本，於是「歷史沒有倒退」**恆真**。(iii) 改成殺掉當刻的筆數之後仍然不足——關掉尾檔實測「殺掉當下 41 筆、期末 58 筆」，**數量沒倒退所以照樣會過**。最終斷言改成記下一個**具體的 contract_id**，期末必須找得到它；反向驗證（`HUB_TAIL=0`）時它報「不見了」而紅。同一輪的 `settlementResumesWithin` **通過了**，證實那個期望會在一個歷史被吞掉的帳上通過。為了做反向驗證而加的 `HUB_TAIL=0` 開關，**第一次執行就因 `path.basename(null)` 打掉了 Hub**——那個 crash 是我引入的，被「證明測試會失敗」這個動作抓到。

**量測結果，以及不能宣稱的部分**（雙角色拓撲、11 分鐘、同設定）：

| | dump 2s（原本） | 快照 30s ＋ 尾檔 |
|---|---:|---:|
| 堆迴歸 | +0.40 MB/分 | **−0.04 ～ −0.05** |
| RSS 迴歸 | **4.82 MB/分** | **2.02 / 2.75**（兩輪）|

堆已經平了。**但頻寬預算（`HUB_SNAPSHOT_KBPS`，預設 100）在這個規模沒有被觸發**：3.5MB 算出來是 36 秒，只比 30 秒的地板多一點，所以 2.02 與 2.75 的差別落在同設定兩輪的噪音裡（1.79／2.02）——**這個規模的量測分辨不出預算有沒有幫助**，它要到 4 小時（約 50MB → 間隔 8 分鐘）才會咬住，而那還沒量。剩下的 RSS 成長是 allocator 沒把頁還給 OS（堆是平的）。

**(g) 預算旋鈕與大尾檔已在同量級驗過（`scenarios/tail-large.json`，2026-09-18）**。4 小時後匯出檔約 50MB、100 KB/s 的預算會把間隔拉到約 8 分鐘——而那意味著**尾檔要累積 8 分鐘的歷史，而尾檔重播從未在那個規模被測過**。所以把預算調到 `HUB_SNAPSHOT_KBPS=10`，讓 3.8MB 算出 **394 秒**的間隔（同一個量級），20 分鐘的跑程在第 15 分鐘殺掉 Hub。三個結果：**(i)** 間隔確實被拉長到 394s；**(ii)** 重播正確——殺掉當下 479 筆、期末 697 筆、殺掉前的 `c-t-A-9e7bae0a-100` 仍在帳上，而**更強的證據是七項不變式跑在恢復後那本含重播歷史的帳上**（697 筆），所以簽章、鏈與守恆都在重播的歷史上驗過；**(iii)** RSS **地板斜率從 2.97 降到 1.27 MB/分**（地板才是活躍集的代理指標）。**這仍然不是 50MB 的證據**——它證明的是機制在同量級的間隔下成立，而真正的 50MB 檢驗是 4 小時演練 |

| 設定 | 結算 | 成交量 | 退還 | protocol 期末持有 | 佔成交量 | 未償負債 | 最長空窗 |
|---|---:|---:|---:|---:|---:|---:|---:|
| **關閉**（對照，`HUB_REBATE=0`）| 304 | 1244 CC | 0 | 105.76 CC | **8.5%** | 155.5 | **969s ← FAIL** |
| **回流 6%**（預設）| **509** | 2066 CC | 168.18 | **7.42 CC** | **0.4%** | 90.1 | **183s** |
| 全額退還（準備金目標 0）| 462 | — | — | — | — | — | 244s |

對照組的 8.5% **正好等於費率**（2.5% Treasury ＋ 6% thin 風險費），也就是說 protocol 帳戶持有的就是全部費收，而那筆錢就是交易者負債的成長——本條開頭那個推論在原型上量到了。對照組在 T+1462 之後停了 **16 分鐘**，`tradingContinues` 因此 FAIL；這是這個情境第一次有東西該紅而且真的紅了。

開啟後 96% 的毛吸收回到流通，成交量 +66%、未償負債 −42%。**而且準備金目標不是形式**：全額退還（把保險池退到零）比 6% 目標**較差**（462／244s vs 509／183s），所以「退回去」與「退多少」是兩個不同的決定。

**(5) 我在這條上犯的錯與量測方法**：第一版對照組用 `HUB_INSURANCE_TARGET_FRAC=0` 當「關閉」，而那個值的語意是「準備金目標為零」→ **把保險池全額退還**，比預設更激進。那一輪跑了 23 分鐘我才發現它不是對照組。已加明確的 `HUB_REBATE=0` 總開關（0 是合法政策，不能同時當關閉的意思），那一輪不丟棄、改標為「全額退還」變體並保留在表裡。
**誠實限制**：兩個變體的情境 `name` 相同，所以它的 ledger 匯出檔被對照組覆寫，表裡只剩得自 log 的結算數與空窗——工具缺陷，同名情境會寫到同一個輸出路徑。

**(6) 順帶補上一條不變式**：`default-writeoff` 原本只檢查瀑布的**順序**，所以「保險池幾乎空了＋36 CC 丟進 `protocol:loss`」照樣 PASS——期望寫在它能通過的地方（同 #62 的「只看交易 agent 不看 verifier」）。新增第七項 `writeOffWaterfall`：**只有在保險池被榨乾之後才允許動用 `protocol:loss`**。兩個方向都驗過（三份真實匯出檔通過；把保險池提領的一半改記到 loss、Σ 仍為 0 的竄改檔被抓到）。
**瀑布風險本身沒有結論**：三種子 × 開/關，沖銷 20.38／9.69／24.48（開）對 38.61／32.05／20.35（關），種子 91 反轉且組內離散超過組間差距——**我擔心的傷害沒出現，好處也沒被證明**。六輪都顯示的是另一件事：N=3 時一個 agent 違約約是曝險的 40%，而池子目標是 6%，**單一名目集中度按定義就超過池子**——那是小網路的性質，不是退費的缺陷，也是 #61「最小可行約 50 agent」的另一個面 |
| 75 | **P0**（追查 F8 的可達性時發現，2026-09-18）| **盤點表的 F8「以 `hub_pub` 驗章」只存在於離線重建，活著的節點對 `checkpoint` 廣播完全不驗簽**。節點從來拿不到 Hub 的公鑰——`hub_pub` 只在匯出檔裡——所以廣播來的 `cp` 直接進 `cpRoots`，而 `cpRoots` 是 panel 種子（#6）的唯一來源，也是 #69c 跨節點比對的基準。後果有兩層：任何能在線上插一行的人（信封 metadata 無加密，#44）或冒充的排序器就能**決定別人的 panel**；而一份未簽署的戳記同時能製造假分叉指控，把 #69c 的偵測變成阻斷攻擊的入口。發現時機：#69c 交付後回頭問「F8 這條防線在哪條路徑上」——**`lib/rebuild.js` 有，`agent.js` 與 `verifier.js` 的 `checkpoint` handler 沒有** | **已修（2026-09-18）**：Hub 在 `registered` 交出 `hub_pub`。**交出公鑰不是信任宣告**——DID 是公鑰的雜湊，所以釘了 DID 的節點可以自己核對，而 #69c 的分叉指控也因此變成可證明的。驗簽收在 `cpwatch.observeEntry()` 一處，agent 與 verifier 共用：驗不過就丟、記數、寫 log，且**不進比對基準**（一個驗不過的 root 若拿去比對，健康網路會被外人餵出假分叉）。Console 同時新增 `checkpoint_bad_sigs` 與 `checkpoint_latest`——只報拒絕數看不出「拒絕之後還剩什麼」，而種子正是從後者長出來的。分叉紀錄改帶 `proof`（自己那一側的已簽署條目），第三方持有同一個 `hub_pub` 就能複驗指控，不必相信指控者。**開機窗口刻意不阻塞**：拿到 `hub_pub` 之前無法驗章，那段時間由 `hubPin`（DID＝公鑰雜湊）在連線層擋冒充，因為 panel 種子需要盡早開始累積 root。閘門：`redteam.js` **F9／F9b**（對抗旋鈕 `HUB_CP_FORGE`，實測拒絕 10 份、採信 0 份、誤報 0 次、七項不變式全過）；`demo.js` 的健康網路閘門同時守**零誤拒**與「確實採信到 root」——誤拒一份真 checkpoint 的後果不是少一個稽核訊號，而是 panel 種子整條斷掉，比漏驗更難查。**寫這一案時量到的第二件事（已登記、未設閘門）**：只偽造推播時，`checkpoint_request` 那條拉取路徑（#41 稀疏儲存的副產物）仍以真章回答，於是節點**自己把自己救回來**——實測種子晚 8 秒到、合約照樣結算。要讓驗章真的變成拒絕結算，排序器必須**兩條路一起說謊**，所以旋鈕分 `broadcast`／`all` 兩檔而 F9 走 `all`。**殘留缺口**：`all` 之下的代價與 #72 同形（實測 1 筆合約開著 9 秒等種子，最終放棄）——驗章擋住的是污染，不是可用性，偵測與拒絕都不構成緩解。**教訓與 #69 同型**：盤點表把 F8 標成 block，而那個判斷讀的是**離線驗證器**的程式碼。同一個檢查存在於某一條路徑上，不等於存在於被攻擊的那條路徑上——要證明一條防線，測的必須是攻擊真的會走的那條路 |
| 76 | **P1**（`soak-n20` 首輪的副產物，2026-09-19）| **取樣端在匯出越過 16MB 之後就瞎了，而每一條期望都繼續讀那份凍結的舊匯出**。`chaos-run` 每個取樣週期用 `export` 訊息拉**整份**帳本，而 `lib/channel.js` 的 `MAX_LINE` 是 16MB。N=20 跑到約 T+1870s 時越過門檻（期末快照 17.6MB，`export` 訊息還多帶 ~2MB `raw_log`），於是取樣端每輪丟一次連線（`[wire] oversized frame`）、`ask()` 5 秒後回 `null`、`lastExport` 就地凍結。**後果兩個，方向相反**：**(a) 一個假 FAIL**——`tradingContinues` 報「最長空窗 590s（自 T+1835s）」，而 Hub 自己的快照顯示那段時間成交了 **578 筆**（1996 → 2574），市場從未停過；同一批樣本裡的 `合約開啟` 還在 21/25/13/7/2 之間跳動，**證據就印在同一行**，只是沒有任何期望在看它。**(b) 一個空心 PASS**——`invariantsHold` 宣稱「1996 筆結算下七項不變式持續通過」，但最後 590s（全程的 24%、578 筆收據）一次都沒被檢查，因為 `checkLedger(ex)` 在 `if (ex)` 裡面；連它報出的筆數本身都是凍結值。指標與人口報表同樣讀 T+1835s 的舊匯出（印 30.1% 而期末 28.9%、正餘額 1057 而期末 1224 CC）| **已修（2026-09-19）**，並在弄壞它的那個規模上驗證：N=20 同一情境，wire **10 次**拿不到（匯出 19.7MB > 16MB），取樣端 10 次改讀 Hub 自己的磁碟快照，**零次「活著卻讀不到」**，結算追到 2915 筆（前一輪凍結在 1996）。三件事：**(1) 三分法取代二分法**——讀到／Hub 停機（沒有帳可讀，那是情境自己安排的停機，不是覆蓋率缺口）／**Hub 活著卻讀不到**（本條的洞）。`invariantsHold` 改名為「且全程都真的檢查過」，只要出現一次第三類就 FAIL；`tradingContinues` 把未觀測的區間排除在空窗計算之外並在訊息裡明講。**修完之後同一輪的最長空窗從「590s」變成 131s**，而 131s 正是 Hub 停機那段——先前那個數字從頭到尾不存在。**(2) 第二條路**：wire 仍是第一順位（那份匯出是當下且自洽的），失敗時改讀 #74 的磁碟快照，尾檔只用來回報「這一份落後幾筆」（本輪最多 199 筆）。**(3) `AMCN_SAMPLE_DISK=1` 對抗旋鈕**強制走第二條路——**它在第一次跑就抓到第二條路自己是壞的**（健康網路上三次 `checkpoint root mismatch`），那就是 #78。一條只在 16MB 之後才會跑到的後備路徑，等於一條沒有人測過的路徑（同 #74 的 `HUB_TAIL=0`）。順帶把尾檔的**讀取端**抽到 `lib/tail.js`，Hub 與 `chaos-run` 共用一份格式定義，否則是 #60 的形狀。**殘留缺口（未修）**：16MB 的 `MAX_LINE` 本身沒動，所以**任何真實客戶端**向 Hub 要整份匯出，在 N=20 約 40 分鐘後一樣拿不到——取樣端現在有第二條路，第三方沒有。匯出成長實測 443 KB/分，所以 #41 的增量／分頁匯出仍然必須做，而 W12 的試點會比 40 分鐘長得多 |
| 77 | **P1**（`soak-n20` 首輪，2026-09-19）| **§20-10 的成交率、供需深度與違約率代理在 Hub 重啟之後全部失效**：它們由 `market = { tasks, bids, contracts }` 這組**記憶體 session 計數器**算出，而 `HUB_IMPORT` 的還原區塊補回 balances／chains／stakes／receipts／events／checkpoints／stats／pubkeys／collateral——**就是沒有 `market`**。重啟之後分子（收據＝已還原的全部歷史）與分母（得標＝重啟後才開始數）來自不同的生命期。實測：成交率 **2.242**（1148 得標／2574 收據）、`default_proxy_rate` **−1.242**、`tasks_broadcast` 1234 < 收據 2574、供需深度 8.63（只數了重啟後的出價）。**觸發條件正是 §20-5 要求系統做到的那件事**（殺掉排序器、從匯出重建接手），而 `demo.js` 從不重啟 Hub，所以它的 §20-10 斷言結構上看不到這個缺陷。**我新加的 `marketMetrics` 期望第一版也放過了它**——只設下界 `minFillRate ≥ 0.9`，2.242 照樣 PASS，又一次「期望寫在它能通過的地方」（同 #62／#69 的形狀）| **已修（2026-09-19）**，而修法是 (a) 與 (b) 的分層，因為那三個數字站得住的腳本來就不一樣——把它們當成同一種「指標」正是這一條的成因。**成交率走 (b) 的可檢查版本**：得標的合約 id 持久化進匯出**與尾檔**（只放快照不夠——崩潰之後收據會從尾檔回來、得標不會，成交率就再一次 >1，那是 #78 剛教過的形狀），匯入時還原。合約是雙方簽署的物件，所以「每一筆收據的 `contract_id` 都在得標集合裡」是第三方也能做的對照，`demo.js` 與 `marketMetrics` 都守這一條（孤兒收據必須為 0，得標集合不得為空）。**但集合的完整性仍是排序器的主張**——漏報會讓成交率偏高，這一點寫在匯出裡而不是只寫在文件裡。**供需深度誠實降級**：tasks／bids 兩個計數器跟著快照持久化（崩潰最多損失一個快照間隔），並明確標成**排序器觀測值**——只有中繼看得到所有出價，要它可驗證就得持久化每一筆 bid（N=20 四十分鐘 10,646 筆），那與 #41／#76 的匯出成長直接衝突。**匯出新增 `provenance` 欄位**把三層寫清楚（`signed`／`sequencer_set`／`sequencer_observed`），因為讀匯出的人不會去讀文件；`s20-evidence.md` §20-10 那句「全部由簽署狀態導出」同步更正。**實測**：`hub-kill-takeover`（殺掉排序器、從匯出重建接手——當初讓成交率跑到 2.242 的正是這條路）成交率 **0.98**（49 得標／48 結算）、5/5；**負向對照 `HUB_AWARDED_PERSIST=0`** 回到舊行為，實測成交率 **1.244**、違約代理 −0.244、得標集合為空，三句都被閘門指名。`soak-n20` 因此從 5/6 變成 **6/6**：40 分鐘、N=20、跨一次 Hub 重啟，成交率 **0.969**（2805 得標／2718 結算；同一輪含尾檔共 2823 筆，差額是取樣讀到的快照落後 181 筆，見 #76），孤兒收據 0，供需深度 9.51（排序器觀測）。閘門：`demo` 26/26（該案標題同步改掉過度宣稱）、`hub-kill-takeover` 5/5（負向對照 4/5） |
| 78 | **P0**（修 #76 時用 `AMCN_SAMPLE_DISK=1` 強制走後備路徑而發現，2026-09-19）| **從快照＋尾檔恢復之後，`rebuild()` 重算出來的鏈雜湊與任何一個已簽署的 checkpoint 都對不上，而它回報 `ok: true`**。`at`（每一筆鏈分錄的時間戳，屬於被雜湊的內容）**只存在於 `chains`**——events 完全沒有時間欄位（`kind,ref,postings,receipt_idx,tx_class`），所以它無法從簽署歷史推導。而 #74 的 `mergeTail` 依設計在套用尾檔之後刪掉 `chains`（它們過期了），於是 rebuild 把每一筆的 `at` 填成 **0**，全部鏈雜湊改變。實測（拿 `tail-recover` 的真實匯出檔照 Hub 匯入路徑處理）：`ok=true`、「重算出的 head root 對得上任何一個鑄過的 checkpoint 嗎？**否**」、`at` 全為 0。**為什麼 rebuild 看不到**：它驗 checkpoint 的**簽章**與 `prev_root` 之間的鏈接，但**從不把 `cp.root` 與它應該概括的 chain heads 對照**——那條對照只存在於 `lib/invariants.js` 的 `chains` 檢查裡，而且只對**最後一個** checkpoint，只有 `chaos-run`／demo 會跑。**後果三層**：(1) §20-4「全部餘額變動可由簽署事件重建」跨過一次尾檔恢復之後，在**雜湊與 root 這一層**不成立——餘額還原得出來，鏈與 root 還原不出來；(2) `rebuild()` 的定位是「驗證而非信任、竄改即拒絕啟動」，而它對這種**靜默分歧**沒有意見（同 #69／#75 的形狀：檢查存在於某條路徑，不等於存在於被走的那條）；(3) 第三方驗證一份**恢復後**的帳會算出與已簽署 checkpoint 不同的 root，而 root 正是 #6 的 panel 種子與 #69c 分叉指控所依賴的東西 | **已修（2026-09-19），採 (b)＋(c)**。**(b) 尾檔改帶鏈分錄本身**（`{a, ce}`，幂等條件用分錄自己的 `seq`，寫在 checkpoint 記錄**之前**——checkpoint 承諾的就是這些 head），`tail.merge` 因此不再刪 `chains`；它仍然刪 balances／collateral／credit_lines／metrics，那些都能從事件重算，只有 `at` 不能。**這不動簽章涵蓋的內容**：鏈分錄從不出現在任何協定訊息裡（`agent.js`／`verifier.js`／`lib/wire.js` 對 chain 零引用），它是 Hub 的衍生狀態、只透過已簽署的 checkpoint root 被承諾，而尾檔是本機耐久性檔案。實測（`tail-recover` 的真實匯出檔照 Hub 匯入路徑處理）：併入 1124 筆、其中 **666 筆鏈分錄**，`at` 回到真實時間戳，重算出的 head root **對得上**一個鑄過的 checkpoint（修之前：全部 `at=0`、對不上任何一個）。**(c) 閘門同時補上**：`rebuild()` 現在把最後一個保留的 checkpoint 的 root 與重建出來的 chain heads 對照，不符就拒絕啟動（只比最後一個，因為 root 一動就會被保留，見 hub 的「root 變動或 heartbeat」條件）。**負向對照 `HUB_TAIL_CHAINS=0`** 回到舊行為，實測讓 Hub 拒絕啟動並指名問題：`checkpoint #319 commits to root d682f26ba59c but the chains rebuilt from the signed history hash to 82a589a8db53`。**寫這個負向對照時又抓到兩個 harness 缺陷，兩個都是同一種**：(i) 取樣端分不出「活著的 Hub」與「拒絕啟動的 Hub」——`startHub` 已經把 `hubAlive` 設回 true，於是取樣把磁碟上那份（沒有尾檔的）快照當成活著的帳，`noHistoryLoss` 報出**根本沒發生**的「歷史不見了」；改為監看子行程的 `exit`。(ii) 更糟：`noHistoryLoss` 在 Hub 沒回來時比對的是**重啟之前**那份匯出，於是「歷史沒有倒退」恆真（同 #74 的教訓）；現在以「重啟之後至少讀到一次帳」為前提。修完之後負向對照紅在正確的兩案上（「恢復後無成交」與「重啟之後一次都沒讀到帳」）。**既有閘門當初為什麼沒抓到**：`tail-recover`／`tail-large` 斷言的是收據數不倒退與某個 `contract_id` 仍在帳上，`demo-rebuild` 用的是**完整**匯出（`chains` 還在）——兩邊都沒問「`chains` 被刪掉之後還原得對不對」，又一次「期望寫在它能通過的地方」。閘門：`tail-recover` 4/4（`HUB_TAIL_CHAINS=0` 時 2/4，紅在正確的案上）、`hub-kill-takeover` 4/4、`demo-rebuild` 7/7、`demo-reconnect` 8/8、`demo` 26/26、`redteam` 43/43、`redteam-agents` 13/13、`tail-large` 4/4——**這一支才是 #78 的規模證據**：預算把快照間隔拉到 401s（3.9MB ÷ 10 KB/s），尾檔因此保存約 400 秒的歷史，重啟時重播 494 筆，殺掉當下的 479 筆收據與指定的 `contract_id` 全部還在帳上；同一份檔案離線重驗併入 2070 筆、其中 **1454 筆鏈分錄**，`rebuild` ok 且 root 對得上。**代價已量**：鏈分錄約占尾檔體積 **37%**，尾檔因此約 **1.59×**。這一項要在四小時演練裡再看一次——#74 的預算會隨匯出成長把間隔拉長（本輪 401s），而「間隔 × 尾檔密度」就是崩潰時要重播的量。**2026-09-19 後續：這個修法自己帶了一個會讓 Hub 拒絕啟動的 bug，而它是被 #80 的診斷抓到的**。`tail.merge` 當初寫成「套用了尾檔記錄但 `chainEntries === 0` 就丟掉 `chains`」，理由是「舊格式的尾檔沒帶鏈分錄」。**但零筆鏈分錄不等於舊格式，它通常只表示鏈沒有變動**——尾檔最後那一批若只有公鑰或得標記錄（例如恢復前最後發生的是一次註冊），本來就不會有鏈分錄，而快照裡的 `chains` 仍然正確。丟掉它之後 rebuild 只能把 `at` 全填 0，於是**本條自己新增的 root 閘門擋下一個完全健康的恢復**：實測 `tail replay: 2 applied` 緊接著 `REFUSING to start`。這是負載相依的——取決於恢復前最後一批尾檔記錄是什麼——所以單獨跑 `demo-reconnect` 看不到，並行時 0/3 通過。條件已改為「有事件或收據被套用、卻一筆鏈分錄都沒有」＝真正的舊格式。修後並行 3/3 通過，而 `HUB_TAIL_CHAINS=0` 的負向對照**仍然 2/4**（閘門沒有被放鬆，只是不再誤擋）。**教訓**：一條「拒絕啟動」的防線，它的誤報成本是整個節點起不來，所以它的**否定條件**要和它的肯定條件一樣被測——我當初只測了「該擋的有擋」 |
| 79 | **P2**（量 #41 的組成時發現，2026-09-19）| **`chains` 比它獨有的資訊大約 20 倍，而移走 `heads` 之後它變成匯出裡最大的一塊**。一筆鏈分錄 297 B，欄位是 `account`／`seq`／`prev_hash`／`receipt_idx`／`delta_cc`／`balance_after`／`at`／`hash`——其中**只有 `at` 無法從簽署歷史推導**（events 沒有時間欄位，這正是 #78），其餘全部是重放 events＋receipts 就能重算的，而 `rebuild()` 對 `ex.chains` 的唯一讀取就是 `atOf()` 取那個時間戳。`at` 是 13 位毫秒數（約 15 B），所以為了保住它而攜帶整筆分錄的代價約 **20×**。實測 N=20 四十分鐘：chains 6.02MB／21,270 筆分錄；移走 heads 之後匯出約 10.2MB，chains 佔其中約 59% | 未修，且**刻意不在同一輪動**。可能的修法是匯出只帶每個帳戶的 `at` 陣列，由 `rebuild()` 重算整條鏈——估計 6.02MB → 約 0.35MB，與 #41 的 heads 合計可把那份匯出壓到約 4.5MB（−76%）。**但風險與收益不同量級**：`lib/invariants.js` 的七項檢查直接讀 `ex.chains`（雜湊鏈結、餘額、checkpoint root），改格式等於讓 `checkLedger` 依賴 `rebuild`，而那兩者目前是**互相獨立的兩條驗證路徑**——一條壞掉時另一條會發現（#78 就是這樣被抓到的：rebuild 說 ok、invariants 說 root mismatch）。把它們耦合起來省下 5.7MB，可能買回一個看不見的共模失效。**先量再改**：要做的話得先有一條「兩條路徑仍然獨立」的論證或閘門 |
| 80 | **P2**（修 #41 時連續誤判兩次才查清，2026-09-19）| **`demo-reconnect` 在與其他 demo 並行時會把「接手的 Hub 還沒起來」報成「0/6 個 client 自行重連」**。實測並行跑 `demo.js` 時穩定 5/8（單獨跑則 4/4 全綠），失敗的三案是重連、重新註冊、與「新增 −2 筆」——最後那個負數就是線索：接手的 Hub 回報的收據數比斷線前**少**，也就是它根本還沒把帳載起來。子行程 log 顯示 `tail replay: 2 applied`、**沒有** `REFUSING to start`，而 client 端是連續 15 秒的 `ECONNREFUSED`——所以 Hub 最後有起來，只是慢過這支 demo 固定的等待窗。**我自己先後誤判兩次**：第一次說「CPU 競爭」（對，但沒證據）、第二次說「demo.js 與 demo-reconnect 都綁 47180 的埠衝突」（錯——換成 `DEMO_PORT_OFFSET=300` 之後仍然 5/8）。而 CLAUDE.md 明文說每支 demo 都可用 `DEMO_PORT_OFFSET` 與跑中的試點並存，所以「並行」是被支持的用法，不是誤用 | 未修。方向：接手的 Hub 起來之後**先等它真的在 listen** 再開始量，而不是等一個固定秒數；或至少在斷言訊息裡區分「Hub 沒起來」與「client 沒重連」。**這一條的價值不在那三個 FAIL，而在它讓我連續兩次把啟動延遲讀成協定失敗**——與 #76 同型：失敗被報成了另一件事。在修好之前，這幾支 demo 要單獨跑。**已修（2026-09-19），而且它立刻證明了自己的價值**：重連窗改從「Hub 真的在 listen」算起（輪詢 `list_verifiers`，上限 25s），不再是 spawn 之後固定睡 14 秒；Hub 若沒起來，demo 會**印出 hub2 自己最後幾行log** 而不是把它報成 client 沒重連。第一次跑就問出了真正的原因——`REFUSING to start: ... the reconstruction is not the ledger that was signed`——也就是 #78 的修法自己帶的 bug（見該列）。**所以這一條不只是 demo 的時序問題，它是一個真實缺陷的遮蔽物**：在它被修好之前，一個會讓 Hub 拒絕啟動的bug 在並行測試裡長得像「client 沒重連」。修 #78 的 merge 條件之後並行 3/3 通過 |
| 81 | **P1**（#41 的分頁匯出自己帶進來的，由 N=20 長跑抓到，2026-09-19）| **「凍結的快照」根本沒有凍結**。分頁匯出的設計重點就是各頁必須來自同一個瞬間（否則重組出來的帳不對應任何真的存在過的狀態），我為此寫了註解、也照做了——但 `buildExport()` 是**淺拷貝**：`checkpoints` 就是 Hub 那個陣列本身，`chains` 的每一條鏈也是同一個陣列參照。單頁匯出在同一個 tick 內序列化完，看不出差別；分頁跨好幾個來回，於是第 1 頁的 receipts 序列化於 t1、第 3 頁的 chains 序列化於 t3，而 t3 的鏈已經長過 t2 送出去的 checkpoint。**症狀**：收方重算出的 head root 對不上**任何一個** checkpoint，`chains: checkpoint root mismatch`——與 #78 的症狀一模一樣，但成因完全不同（#78 是 `at` 遺失、全部雜湊改變；這裡 `at` 正常、273 筆鏈分錄零個 at=0、帳戶數與 `receipts_count` 全都對得上，只是**時刻**不同）。**只有在長跑裡才會現形**：N=20 四十分鐘那一輪 5 次違反，而 `demo-rebuild`（20KB 預算、每次取帳都分頁）9/9 全綠——因為它的匯出小、來回快，而且沒有持續的交易在改鏈 | **已修（2026-09-19）**：凍結時把四個陣列各複製一層（收據／事件／checkpoint／每條鏈）。複製指標就夠——這些項目一旦 push 進去就不再被原地修改——所以不必深拷貝十幾 MB。**閘門 `scenarios/page-kill.json`**：分頁 × 排序器重啟，`HUB_EXPORT_PAGE_BYTES=20000` 讓每次取帳都必須分頁。**這個組合是必要的**——單獨分頁（`baseline` 加小預算）乾淨、單獨重啟（`hub-kill-takeover`）也乾淨，只有合起來才紅（修前 4/5、修後 5/5）。**教訓**：我在註解裡寫對了「頁必須來自同一個瞬間」，卻沒有驗證我拿到的那個物件真的是一份拷貝——**寫下意圖不等於實作了意圖**，而 JS 的淺拷貝讓兩者看起來一樣 |
| 82 | **P0**（三台真機試點的拔線演練第一次嘗試時發現，2026-09-21）| **在生產預設之下，排序器接手永遠不可能成功**。`lib/rebuild.js` 重算信用額度時呼叫 `eeff.creditLine(did, stats, statsOf)`——**漏傳 `ageFactor` 與 `collateralCc`兩個參數**，而 `ageFactor` 的預設值是 `1`。額度公式是 `STARTER*(0.5 + 0.5*age) + …`，所以 age=0 與 age=1 之間差整整一倍（25 CC）：重建一律把每個帳戶當成**已完全成熟**，而 Hub 用的是真實年齡。實測待命 Hub 兩次拒絕啟動，四個帳戶全部差約 24.9 CC（`rebuilt 55.024 vs export 30.115`）。**為什麼活到現在才被發現**：`HUB_AGE_RAMP_MS` 預設 30 天，而 **demo、demo-rebuild、chaos-run、redteam 全部都設 `=1`**——斜坡在一毫秒內走完，兩邊都得到 age=1，這個分歧在 harness 裡**結構上不可能出現**。三台真機是第一次用生產預設跑的，也是第一次看到它。後果是 §20-5「官方 Indexer 停止後仍可恢復」在**任何帳戶滿 30 天之前**都不成立，而 W10 的拔線演練正是為了這種事存在 | **已修（2026-09-21）**。三件事：**(1) 年齡因子改由 `lib/eeff.js` 提供**（`ageFactor(joinedAt, now, rampMs)`），hub 與 rebuild 共用同一份定義——兩邊各算各的正是成因（#60 的形狀）。**(2) 匯出新增 `exported_at`**：額度含年齡項，而重建必然發生在匯出**之後**，沒有這個時間戳就只能算出一個比較大的值。這與 #78 的 `at` 同一個道理——衍生值需要產生它的輸入。**(3) 缺欄位時 warn 而不是 fail**：#82 之前格式的舊匯出沒有 `exported_at`，那就跳過額度比對並說出來（新增 `warnings` 與 `fail` 分開，同 #76 分「未測到」與「違反」），而不是拿一個必定不同的值去判它有罪——試點那份舊 dump 因此仍能匯入，185 筆收據、簽章與鏈全過。順帶補上漏傳的 `collateralCc`，否則有抵押品的帳戶會以同樣的方式被誤判。**閘門 `demo-ageramp.js`（5/5）刻意不設 `HUB_AGE_RAMP_MS`**——任何人想「讓它跑快一點」而加上那個變數，就是把閘門變回看不見缺陷的狀態；它同時斷言「額度確實在斜坡上（不是已成熟的 50）」，避免斜坡被悄悄壓平。**負向對照**：還原 `rebuild.js` 之後閘門紅在相同症狀（`46.250 vs 23.125`，正好是 age=1 對 age≈0 的兩倍）。**這一條的教訓比缺陷本身重要**：所有 harness 為了跑得快而壓縮的那個參數，正是唯一能讓這個缺陷現形的參數。**被壓縮掉的維度上，沒有任何證據** |
| 83 | **設計輸出**（Owner 提議「AI 會頭」，2026-09-21）| **逆週期收購是唯一動得到集中度的槓桿**，而它是 §2.2「Treasury 啟動」早就列好的位置。Owner 的原話是「假設沒有人想要發任務的話，是不是有可能像是標會的機制，例如有個 AI 會頭可以自己決定想什麼任務來讓其他人去做」。**標會那一半不成立**（重新分配信用不創造需求，而且撞上 INV-C1 與 #38／#50 的 pay-to-play），但「沒人發任務時要有人發」那一半正好對準 #64——它動的是**結構**，而 demurrage、流入費、雙角色三支都只搬動存量（#66 量到留存率被費率精確控制、集中度完全不動）。**§5.2 的界線**：會頭發的任務必須是真實需求，「不以虛假任務製造市場熱度」是明文非目標；所以它標 `tx_class=subsidy`、計入有治理上限的創世補貼額度、**不進市場指標** | **已實作並掃描（2026-09-21）**。`ledger.counter_cyclical()` ＋ `drought_day`（第 N 天起全網需求砍到 20%，用來造出「沒人發任務」那個狀況）。300 agents、84 天、第 42 天乾旱、三種子。**乾旱讓結算量腰斬（21,521 → 10,623）**，而會頭的效果是：**貼上限 15.0% → 2.1%、首位佔比 5.3% → 2.8%**，兩者都隨額度單調下降。**這是今天唯一動到集中度的槓桿**——流入費在同樣的量上是 19.7% → 22.3%（無效果）。**機制**：它向**任何有餘裕產能的人**買，包含貼著上限的赤字者，讓他們賺錢還債；其他三支是向持有者**課稅**，這一支是給赤字端**收入**。**結算量不變是正確的**（10,623 → 10,635）：補貼標 subsidy、不計入市場量，所以它沒有偽造成交率——一個靠補貼撐起來的成交率不是市場數據，同 #63 的關聯方。**創世補貼額度的第一個實測值**：上限開到 8,000 只花掉 **4,392**，**需求在那裡飽和**——約 **0.35 CC／agent／天**（300 agents × 42 天乾旱），而 §2.2 那一格原本是空的。守恆全程成立，Treasury 在額度內為負（§2.2 設計如此）。**未做**：真實需求的判定（誰認定會頭的任務不是虛假的）屬治理，不是模擬能答的 |
| 84 | **P1**（Owner 決定開始推廣，2026-09-22）| **這套東西沒有一條「外人可以走」的路。**程式面早就跑得通（§20 十項裡 9 項有自動化證據、45＋13 案紅隊、三台真機試點），但一個外部參與者要加入時會撞到四件事，而每一件都足以讓他停在原地：(a) **範本沒有 `seed`**，所以照著文件複製設定檔的人**每次重啟都是新身分**——舊 DID 的正餘額沒人能花、負餘額沒人會還，而且信用紀錄從零開始。要求每個人自己想一個隨機字串填進去，是把預設值的責任推給使用者，而推廣路上的人只會照做最短的那一步。協定 v7 之後這件事對 verifier **有價**：未被金絲雀測夠就換身分，押注不退（#38）。(b) **沒有任何指令可以問「Hub 給我的這本帳是真的嗎」**。核心宣稱是「第一個排序器不是信任根」，但能做這件事的只有一支自帶拓撲的回歸閘門（`demo-rebuild.js`）與一個函式（`lib/rebuild.js`）。(c) **診斷工具在別人的機器上會說謊**：`pilot-doctor.js` 對 `127.0.0.1` 報「不在同一網段」的假 FAIL（單機起步時第一個看到的就是紅字），而它檢查設定檔時**寫死三個試點檔名**，所以在外人的機器上那一節永遠跳過——偏偏那正是「有沒有固定 seed」最該被檢查的地方。(d) **起步路徑只有回歸閘門**：`demo.js` 跑完就結束，留不下一個可以操作的網路。 | **已修（2026-09-22）**。(a) 以**設定檔路徑**啟動而檔裡沒有 `seed` 時，`agent.js`／`verifier.js` 產生一個並**寫回那個檔案**；`panel.js` 把 panel 身分存成 `configs/.panel-seed`(0600)。用 `AGENT_CONFIG` 環境變數啟動時**不改行為**——所有 demo 與情境走那條路，它們刻意要每次都是乾淨的新身分（否則斷言會沿用上一次的餘額）。`.gitignore` 同步把這兩類檔案當私鑰排除。(b) 新增 `verify-ledger.js`：與第二個排序器啟動時同一份 `lib/rebuild.js`，逐筆驗簽、pubkey 自證 DID、餘額由事件重放、鏈重算、額度重放、checkpoint 比對鏈頭，並可 `--pin` 指定排序器身分。實測：207 筆的試點帳本通過；竄改檔被拒（3 項不符）；錯的 `--pin` 被拒。(c) `pilot-doctor.js` 改成「目標是本機就不談網段」，並掃 `configs/` 找**這台實際在用**的設定檔——單機起步從 7/8（一個假 FAIL）變成 8/8。(d) 新增 `quickstart.sh`：一個指令在單機拉起 Hub＋3 verifier＋兩個 Agent、跑完第一筆結算，然後**留著**讓人操作，並印出「接下來可以做什麼」（發任務、查帳、還債、自己驗帳、診斷、停掉）。實測一輪：結算 10 CC → provider 8.75／treasury 0.25／insurance 0.60／verifier×3 各 0.13，重啟後同一個 DID 續在 −10.00 CC 並自動進入還債模式（折價 35%），再供給一次工作後 −10.00 → **−5.45**。**文件**：`README.md` 改成三條路徑的入口（五分鐘單機／加入別人／自己開），新增 `node/JOIN.md`（可直接轉給參與者：三種角色的門檻、押注的四條規則、CC 是什麼／不是什麼、義務、跨網段 `secure`、自檢與查帳），`INSTALL.md` 增營運方章節（備份還原、金絲雀、回流、招人要講的三件事）。**順手修掉的過時數字**：新身分額度是 **23.1 CC 而不是 46.3**（年齡斜坡，#60／#82 之後沒人回去改文件）、紅隊 26→45／9→13 案、demo 21→26 項、「verifier 無報酬」與「age factor 固定為 1」兩條誠實清單早已不成立、UDP 跨機「尚未驗證」早已驗證（#18 已撤銷）。**仍然沒解決、而且不是程式能解決的**：**零個外部參與者**。三台真機試點的三台都屬於同一個人，所以帳上每一筆都是關聯方（#63），市場數字不代表有外部需求。這一列讓「外人走得完」成立，但**走不走**是另一件事 |
| 85 | **P0**（由 #84 的身分預設變更暴露，2026-09-22）| **身分延續 ＋ 帳本延續 ＝ 重啟後的前幾筆任務靜默作廢。**`task_id` 是 `t-<名字>-<did 後 8 碼>-<本行程的第 N 筆>`，而 `contract_id` 由它衍生、又是 Hub 的冪等鍵。所以同一個 DID 重啟之後，第一筆任務的 id 會與上一輪的第一筆完全相同，Hub 正確地回 `duplicate contract_id: already settled`——**任務被拒，而發起方以為自己成交了**（它的 log 印 `quorum PASS → settling`，帳上什麼都沒有）。要兩個條件同時成立才看得見：身分跨重啟不變（#84 之後是預設）與帳本跨重啟延續（`HUB_IMPORT`）。在身分是一次性的年代永遠撞不到，所以 12 週以來的每一支 demo、情境、紅隊、三台真機試點都沒有碰到它：那些 harness 用 `AGENT_CONFIG` 環境變數（每次新 DID），而試點每次都是新帳本。**發現方式是照著自己剛寫的推廣文件跑第二次 `quickstart.sh`**——第一筆任務沒有結算 | **已修（2026-09-22）**：`task_id` 加一個每行程的啟動標記（`Date.now().toString(36)`），變成 `t-<名字>-<did 後 8 碼>-<啟動標記>-<第 N 筆>`。`contract_id` 是不透明字串，所以這不是協定變更，沒有動版本。實測：同一組身分＋接續的帳本，重啟後第一筆任務結算成 `c-t-buyer-2bac8066-muc5oev9-1`（−5.07 → −15.07 CC），修前是兩次 `duplicate contract_id` 被拒。**沒有改的是冪等鍵本身**：Hub 拒絕重複的 `contract_id` 是對的（那條規則防的是重送結算），錯的是 id 的生成方式。**這一條值得記住的不是修法而是它怎麼被找到的**：一個「照文件走第二次」的動作，比 12 週的閘門更早碰到這個組合——因為閘門從來不重用身分，而真實使用者一定會 |
| 86 | **P1**（Owner 的中期目標暴露的缺口，2026-09-22）| **推廣的對象不在你的區網，而「跨網際網路參與」從來沒有人跑過。**Owner 的計畫是「這台電腦常駐跑系統，然後用 OpenClaw 去推廣」——推廣的觸及面是訊息通道（不限地理），但目前能加入的人**必須在同一個廣播網段或同一個 IP 網段**。三個零件都在，卻沒有一個被端到端驗證過：(a) `AMCN_TRANSPORT=secure`（#44）有閘門（`demo-transport.js` 三種傳輸同一本帳），但那是**同一台機器上的三個行程**；(b) `lib/rendezvous.js`（#45）有情境（`hub-moves`），但同樣是單機；(c) 真實的可達位址（port forward／Tailscale／VPN）**完全沒有碰過**——而它同時是威脅面：Hub 綁 0.0.0.0 暴露到公網、`tcp`／`http` 的信封是明文、Console 只綁 localhost 這件事在遠端協助時會變成障礙。所以現在的招人半徑是「家裡第二台、朋友來你家、同一個辦公室」 | 未修。**要做的事有順序，而順序本身是判斷**：(1) 先讓**兩台在不同地點**的機器用 `secure` 跑通一次借用→驗收→結算，量的是「握手在真實 RTT 與 NAT 下會不會成立」而不是密碼學（那一層有閘門）；(2) 位址用 Tailscale 之類的 overlay 先驗，**不要**一開始就 port forward——把 0.0.0.0 的排序器直接掛上公網，等於把一個沒有速率限制、沒有 TLS、沒有 DoS 防護的服務暴露出去（§16 威脅 9 與紅隊 G 組都只在區網假設下成立）；(3) rendezvous 記錄要有一個真的主機，而那個主機**不受信任**（它能扣住或給舊的，但無法冒充——這一點已由設計保證）；(4) 遠端協助的問題要另外解：對方的 Console 綁 localhost 是對的，所以「幫他看一下」只能靠 `pilot-doctor.js` 的輸出與 `verify-ledger.js`，不能靠連進去。**2026-09-22 部分關閉，而關掉它的不是 secure 而是 #89**：`AMCN_TRANSPORT=tor` 讓可達性問題消失（Hub 零入向埠，onion 的 rendezvous 負責把人帶進來），同日實測第一次有客戶端從網際網路註冊並進入 panel 完成結算。**剩下的那一半仍然是零次**：兩端還是同一台機器，所以「兩台在不同地點」要量的東西（真實 RTT 下的長連線穩定度、電路重建、時鐘、以及真正獨立的故障域）都還沒有資料。secure 傳輸也仍然只在單機三行程驗過——不過走 onion 時它的必要性下降了，因為 tor 本身就對信封加密（防的是路徑上的第三方，與 secure 同一個威脅）。**2026-09-23 又關掉一塊，而那一塊是「常駐」**：#89 的入口原本是一個前景腳本，所以跑著三個常駐服務的機器上「外面的人能加入」這件事實測是 0（見 #91）。現在入口是第四個服務（`com.amcn.onion`）、Hub 在該模式下綁回送位址、兩種模式都發布簽署過的位址記錄，遠端 verifier 只要記錄與 pin 就能進來。**這台機器仍刻意留在 `lan` 模式**：把入口打開會改變暴露面，那是 Owner 的決定 |
| 87 | **P0**（Owner 要求正式上線時發現，2026-09-22）| **Hub 對陌生人沒有任何資源預算。**全域只有一個 `MAX_LINE`（16MB frame 上限），而且它對**未註冊**的連線一樣適用：所以任何人不必先簽任何東西，就能用 1000 條連線 × 16MB 把記憶體吃光；沒有連線數上限、沒有每 IP 上限、沒有訊息速率、沒有註冊節流、沒有帳戶總量上限，而註冊會建立**永久狀態**（帳戶、統計、餘額），身分又是免費的（#50）。紅隊那 45 案全部在「受信任區網」的假設下成立，所以這一條在區網裡不是缺陷；它是「把 0.0.0.0 的排序器接上公網」這個動作把它變成 P0 | **已修（2026-09-22）**。分界點是**註冊**，因為它是唯一需要簽章的入口，而所有昂貴的訊息（收據、證據包）都在分界之後：(a) 未註冊的連線 frame 上限 **64KB**（`AMCN_PREAUTH_MAX_BYTES`）、訊息數 **20** 則（`AMCN_PREAUTH_MAX_MSG`）；`register_ack` 之後才升級到 `MAX_LINE`——選 ack 而不是 register 是因為前者才證明對方**聽得到**（#49）。(b) 每連線令牌桶 **200 則/秒**（`AMCN_MAX_MSG_PER_SEC`），ping 也算在內。(c) 連線上限 **200**、每 IP **50**，而 **loopback 完全不算**（`HUB_MAX_CONNS`／`HUB_MAX_CONNS_PER_IP`）。**每 IP 這個值第一版設 10，結果自己的 demo 先死**：`demo-forfeit` 在一台機器上有 6 個 verifier、3 個 agent、canary 加上取帳的連線，實測 14 條被拒 12 條。而同一個錯在真實網路上更糟——**NAT 會把一整個家庭或辦公室塌縮成同一個位址**，所以低的每 IP 上限擋掉的是合法參與者而不是攻擊者；真正的後盾是全域上限（它同時限制記憶體與 fd），每 IP 只是讓單一來源不要一口氣吃光。註冊節流與匯出節流同樣豁免 loopback（那是營運方自己的行程）。(d) 新身分註冊 **10/分/IP**、帳戶總量 **500**（`HUB_MAX_REGISTER_PER_MIN`／`HUB_MAX_AGENTS`）；**重連不受限**——同一個 DID 已經在帳上時直接放行，否則這條規則會把斷線變成永久離線（反過來打壞 #40 與 #17）。**限流一律送出指名的理由再關連線**：一個被限流關掉的連線與網路故障長得一樣，分不出來的人只會一直重試（#76 的同一個教訓）。**唯一刻意不受未註冊額度限制的是 `export`**：「任何人都能把整本帳拉下來自己驗」是這個設計的公共財（§20-4、`verify-ledger.js`），而大帳本是分頁取回的，十幾頁就會撞到 20 則上限——它改由每 IP **30 次/分**的起始匯出節流管，而**跟著游標的後續分頁不計數**（四小時長跑的 50MB 匯出要幾十頁）。閘門：`redteam.js` 新增 A1–A5（超大未註冊 frame、40 則未註冊訊息、限流後陌生人仍拿得到整本帳、限流後帳本沒動，以及**本機 14 條連線不得被擋**——最後那一條就是為了盯住上面那個錯不要再犯），**50/50** |
| 88 | **P0**（同上）| **全量流量記錄會隨匯出送給任何人。**`buildExport()` 的 `includeRawLog` 預設 `true`，而 `export` 訊息對陌生人開放。帳本揭露的是「發生了什麼」，`raw_log` 揭露的是**所有人講過的每一句話**：包含**落選的出價**（那從來不進帳本，而 §20-10 明言供需深度只有排序器看得到）、每一筆任務的 metadata、所有錯誤文字。區網試點裡那是功能（NFR-005 的明文掃描靠它），對公開的 Hub 那是把整個市場的私有資訊送出去 | **已修（2026-09-22）**：預設關閉，`HUB_EXPORT_RAWLOG=1` 才隨匯出出去。自動快照（災難備份）本來就是 `includeRawLog: false`，所以備份不受影響。三支需要掃流量的閘門自己打開它（`demo.js` 的 NFR-005 明文掃描、`demo-autonomous.js` 的「內部推理不外洩」、`redteam-agents.js` 的 A 組）——**方向是「預設安全、閘門自己要求」，而不是「預設開著、上線前記得關」**。|
| 89 | **P0**（Owner 質疑架構走偏，2026-09-22）| **「跑一個節點」被我做成了「把家裡的機器掛上公網」，而那不是這個設計要的形狀。**Owner 的原話：「我本來的理想狀態是一個 AI agent…安裝了這個服務之後他就可以去調用目前有什麼任務或者是自己發任務…所以才之前問你要不要上區塊鏈？或者 P2P 軟體之類的做法，怎麼現在變成還要自己開放網站？」**他是對的，而問題被我定位錯了**：協定側的理想流程早就成立（一個設定檔、agent 自己偵測額度耗盡、發任務、出價、結算、還債），缺的**只有一件事**——連線的兩端必須有一方是可達的，而 Hub 模型把那一方指定成 Hub。實測開發機所在的網路是**雙層 NAT**（ASUS 的 WAN 是 192.168.1.103，上游 192.168.1.1 沒有 UPnP、web 介面也不回應），所以 port forward 這條路連做都做不到，於是我先提了 VPS——那等於要求每個想跑節點的人先去租一台機器，完全違反「安裝之後就能用」。**而換技術不會解決它**：libp2p 的答案是 relay ＋ hole punching，也就是**還是有人要跑 relay**；鏈解決的是「目錄由誰託管」（一份簽署過的記錄放在任何靜態主機上就解決了，見 `lib/rendezvous.js` 支援 https），解決不了「兩台 NAT 後面的機器怎麼連上」，而且每筆結算上鏈違反 NFR-009 與 P-04 | **已修（2026-09-22）**：ITransport 實作 5，`AMCN_TRANSPORT=tor`（`lib/transport-tor.js`）。**Hub 只聽 127.0.0.1，對外零入向埠**，由 onion service 的 rendezvous 把外面的人帶進來——雙層 NAT／CGNAT 兩邊都不必設定，不必租機器、不必開 port、沒有人要營運基礎設施。三件事同時對上：(1) 零入向埠；(2) **位址本身就是公鑰**（v3 onion ＝ ed25519 公鑰編碼），而 AMCN 本來就要求客戶端釘 `hubPin`，所以這裡的信任錨與協定的信任錨是同一種東西，不需要 CA；(3) 免費。**實作幾乎沒有新東西，這正是 ITransport 這個接縫的價值**：`listen` 完全沿用 tcp（綁回送位址），`dial` 只是把 socket 換成「經 SOCKS5 以 ATYP=0x03 連到 `<addr>.onion`」——`.onion` 在 DNS 裡不存在，必須交給 tor 解，而那正是「客戶端不需要知道對方在哪」的原因。framing、信封版本閘門、handler 隔離全部仍在 `lib/channel.js`。閘門 `node demo-tor.js`（**7/7**）**不需要安裝 tor 也不啟動任何 onion service**：自帶一個最小 SOCKS5 代理，驗的是交握寫對了、**經這條路拿到的帳與直連 tcp 的 fingerprint 完全相同**、Hub 確實只聽回送位址（用本機區網位址去連，連不上），以及兩條**指名失敗**的負對照（不存在的 onion → REP=0x04「主機不可達」；本機 tor 沒起來 → 「連不到本機的 tor」——這兩件事長得一樣但成因完全不同，#76 的同一個教訓）。**閘門自己先錯過一次**：用 `0.0.0.0` 去探測「對外有沒有開埠」，而 macOS 的 `connect(2)` 把它當回送位址，所以那條斷言永遠是綠的。**現場已驗（同日，Owner 同意後）**：`service/run-onion.sh` 起了真的 onion service（`o344qap4…u5yd.onion`），而**第一次有人從網際網路加入這個網路**。取帳：第一次 9.15 s（含建電路）、之後 2.48／2.55／2.57 s（131.8 KB、366 個 checkpoint），`verify-ledger.js --pin` 通過。一個 verifier 全程走 tor 註冊（約 2.1 s）並**進了 panel**：實際結算 `c-t-wan-buyer-8fbb5010-…` 的三個分潤裡有一個是它（`did:demo:e196d19a1`＝0.13 CC）——收到 verify_request、跑斷言、送承諾、揭示裁決、拿到錢，全部經一條 tor 電路，而 Hub 從頭到尾沒有開任何對外埠（實測從區網位址連不上）。買方事後供給三次把 −10.00 還到 +3.78，帳上沒留下無人償還的洞。**仍不宣稱的**：兩端還是同一台機器，所以 #86 只被部分關閉（可達性解決了，地理與故障域的獨立性沒有）；長連線穩定度與 tor 之下的長跑情境都還沒量。詳見證據包〈第一次有人從網際網路加入〉 |
| 90 | **P0**（Owner 問「我們不是有設計保證金嗎」時發現，2026-09-22）| **保證金唯一擋不到的人，正是它要擋的那個人：全新身分。**`collateral_post` 要求 `balance >= amount`（否則就是用信用額度抵押信用額度、無擔保放大——那條守門是對的，見 #65），但一個剛註冊的 DID **餘額是 0**，所以它**付不出保證金**。於是實際狀況是：既有參與者（已經賺到 CC 的人）可以抵押換更高上限，而想賴帳的人只要用新身分進來就完全繞過整個機制。#65 量到的「保證金是必須而非選項」因此目前只對錯誤的族群生效。**而且 Owner 說的「$10 保證金」與原型裡的保證金是兩種東西**：原型的保證金是 **CC**，而 CC 沒有固定現金贖回（FR-054／P-04）——要收真錢就得補上支付、保管與退款三條腿，那是改 P-04 的決定 | 未修，而**修法的選項不是等價的**：(a) **擔保人連帶**（建議）：既有成員替新人背書，背書額度從**擔保人自己的上限**扣除，違約時先吃擔保人的額度。零金流、可在帳本內執行、而且讓「創造一個新身分」對**某個有信用的人**有成本——那正是 #50 缺的那一項（身分免費）。代價：新人必須認識一個既有成員，網路成長變成邀請制。(b) **營運方代墊保證金**（Owner 原本的提議）：Treasury 以 `tx_class=subsidy` 為新人存入保證金，落在 §2.2 的創世補貼額度裡。零摩擦，但**攻擊者的成本仍是零**——它只是把損失從保險池換到 Treasury，#50 量到的「N 不是槓桿只是重複」在這裡直接適用：N 個身分就是 N 份補貼。(c) **收真錢**：唯一真的讓身分有成本的做法，但需要改 P-04 與 FR-054，並帶進金流／退款／法遵，而且與「不是帳號買賣、不發幣」的定位衝突面要重新論述。(d) **維持現狀**（不給新人任何無擔保額度）：那會讓「裝完就能用」不成立——新人第一天無法借用，而 §27 閉環的第一步就是「額度耗盡去借」。**現況的替代防線是三條，都不依賴保證金**：新戶第一天只有 **23.1 CC**（starter 50 × 新戶品質 0.925 × 年齡斜坡 0.5，要 30 天才到 46.25——斜坡本身就是對有耐心攻擊者以外的延遲）、離線 14 天的沖銷瀑布（抵押→保險→損失）、以及洗量抑制（實測從單一對手賺 36.6 CC 額度零成長）。**Owner 當日否決了 (a)**，理由是對的而且比我的提案更根本：「擔保人架構很奇怪，網路上都是匿名的誰可以替誰擔保？而且理想狀態是 AI agent 自己去收發任務，agent 要怎麼去幫另外一個 agent 擔保」——擔保假設了社會網絡，而這裡沒有；就算有，攻擊者也只要開兩個身分互相擔保。**(a) 作廢**。**取而代之的第五個選項，而它是量出來的：不要送額度，去買新人的第一份工作。**新人的困境是「它什麼都沒有」，而現行做法是送它**無擔保信用**（starter＋L_boot）——身分免費，所以 N 個身分就是 N 份禮物。反過來：額度**全部只能賺**（starter 0），而 Treasury 向「還沒有任何賺得紀錄的身分」**購買真實工作**（`onboarding_cap_cc` 每身分上限＋`onboarding_total_cap_cc` 治理上限，買有餘裕產能的人，標 `tx_class=subsidy` 不進市場指標）。不需要錢、不需要關係，而 **Sybil 的經濟反轉**：N 個身分要拿到 N 份 CC 就得交付 N 份真實工作——那不是攻擊，那是供給。**實測（200 agents × 56 天 × 3 種子）**：送額度 starter=20 → 結算量 3995.7／壞帳 81.9；零額度什麼都不做 → 結算量 **666.1（剩 17%，市場被殺掉——bootstrap 問題是真的）**；零額度＋入門採購 20 → 結算量 2937.3（74%）、**成交率 0.936 反而最高**（付不出錢的需求不會變成掛著沒人接的任務）、壞帳 **1.0**。**Sybil 那一格是決定性的**（一個攻擊者 20 個身分）：送額度每身分白拿 **17.49 CC**、壞帳率 9.25%；零額度＋入門採購每身分白拿 **0.00 CC**、壞帳率 **0**。白拿不是變小，是歸零——因為那份無擔保的禮物就是整個攻擊面。**而它成立有一個條件，必須寫在這裡**：入門採購只有在「買到的工作值那個錢」時才是反 Sybil 的。模擬裡 Treasury 只是消耗掉對方的產能；真實版本若買的是做工給你看的東西，攻擊者就是每身分白拿 20 CC、只是換個記帳名目。所以買的必須是**答案已知、可驗證的工作**（金絲雀的同一個機制，既證明新人會做事又是稽核樣本），這樣攻擊成本＝真的做那份工作的成本，防線退回「驗證會不會被騙」——那是這套設計本來就在守的東西。**GATE-0 首跑 6/8，所以還不是決定**（`--starter 0 --onboarding 20 --onboarding-total 40000 --risk-thin 0.06 --risk-base 0.02`）：G1／G3–G7 全過（壞帳 0 vs 保險收入 435–1264、還債中位 3 天、成交率 0.879–0.943、首位持有者 9–11 個帳戶輪替）；**G8 verifier 留存 0/12 全敗**——留存 **100%**，也就是 verifier 收到的一分都不流出，雖然它們持有的**絕對佔比反而下降**（377/9722 ＝ 3.9%，對照 8/8 那組的 883/6208 ＝ 14%）；**G2 全程不適用**——沒有信用可用時洗量幾乎不存在（洗量 12.6 vs 同活躍度誠實 0.0），所以那條判準量不到東西。G8 的門檻是在「每個人都有開機額度」的體制下訂的，而 verifier 在模擬裡沒有需求模型，零額度時它連想花也花不掉。**加上 `--dual-role` 之後 8/8**（#62 階段 3：驗證是交易者的角色而不是另一個物種）——G8 留存 10.4–24.6%，證實 100% 那個值是「verifier 是另一個物種」的模型產物而不是機制問題。**成本**：500 個身分、84 天，入門採購共 **9,884 CC（每人 19.8，上限是綁住的）**，Treasury 因此為 −9,935。**但 8/8 有兩件閘門看不到的事，而它們比那個分數重要**：**(1) G2 的通過是空的**——四個情境裡三個標「不適用」，wash_heavy 只有一個種子可評。它比的是「洗量者的額度 vs 同活躍度誠實者的額度」，而後者是 **0.0**：零額度體制下沒有任何誠實帳戶達到可比的量，所以那條判準量不到東西。洗量者的額度收在 13.9 CC（對照 starter 50 的體制），看起來是好的，但那不是 G2 驗出來的。**(2) 洗量佔成交量的比例從 40.3% 升到 66.3%**（wash_heavy、300 agents、56 天、2 種子）。而拆開來看不是洗量變多——**洗量幾乎沒動（9,657 → 9,828）**，是**誠實量塌了**（14,281 → 5,002）。原因很直接：**洗量者不需要信用**，它們把手上的 CC 在自己之間轉圈就好；誠實的買方需要信用才買得下去。所以零額度對誠實買方比對洗量者更狠。壞帳換來了（58.1 → 0.0），代價是**可見成交量裡有更大一塊是洗量**，而那正是 #63 的同一個教訓：**成交量不是需求的證據**。這件事的處置是報告紀律（不能拿總量當市場證據），不是回去送額度。**建議採用方向、但附兩個條件**：(i) 入門採購買的必須是**答案已知、可驗證**的工作（金絲雀機制），否則攻擊者只是換名目白拿 20 CC；(ii) §20-9／§20-10 的成交量報告必須把洗量與關聯方分開列，否則零額度體制下的量會比現在更容易被誤讀。**(b)–(d) 可以下桌了。條件 (i) 已於同日在原型落實**：`onboard.js`（＝`canary.js` 的 `mode:"onboard"`，同一個流程與同一個問責標準，只有斷言可通過、正確裁決是 PASS 兩處相反）＋ Hub 的 `handleOnboardResult`，三道門缺一不可：**答案可判**（只接受 `sha256_eq` 這類確定性斷言——發樁者自己算得出正確答案，所以「有沒有真的做」是判的而不是信的）、**面板真的判過**（與結算同一個 commit-reveal 標準、quorum 要足）、**收款方真的是新人**（從來沒有收到過任何 CC），外加每身分 `HUB_ONBOARD_CAP_CC`（預設 20）與全網 `HUB_ONBOARD_TOTAL_CC`（預設 2000）兩個治理上限，因為錢出自 §2.2 的創世補貼額度。閘門 `node demo-onboard.js` **8/8**：新人連續六次交付各 2 CC 至 12/12 上限、第七次被指名擋下；四條負對照分別紅在正確的地方（弱斷言、無可問責 PASS、被市場付過錢之後就不再是新人、四次偽造後帳一分未動、Σ=0）。**兩個 bug 是閘門逼出來的，而兩個都關於「什麼叫已經賺過錢」**：(a) 資格條件原本寫 E_eff＝0，但驗證費不進 `stats.earnedBy`（那張表只記結算的買賣雙方），所以**每個 verifier 都永遠符合新人資格**；改成「從來沒有收到過任何 CC」。(b) 而那個新規則第一版把記錄掛在 `applyPostings`，可是**結算的分錄不走那條路**（收據是雙方簽的物件，另一條路徑），於是貨款與驗證費全都不算收入——兩處都補上，並可由事件流重建。**同日再發現條件 (i) 不夠，而補上的那一條是真正有效的旋鈕**。「答案已知」只讓**單一次**交付判得出來；它擋不住「一直試、矇中一次就拿錢」。模擬器改成「通過驗收才付」之後量到的形狀：**面板是準的**（整場 1128 次裁決，真值 True 945 次、面板判 PASS 943 次，誤差 2/1128；而面板加倍、兩成偷懶、金絲雀率 3%→10% 四組的攻擊者收益幾乎不動：5.52／5.30／5.37／4.67 CC），所以漏水不是驗證誤判——是**交假東西的攻擊者每個身分矇中一次就拿到 4 CC**，而身分免費，於是 N × 4 CC 線性漏。**失敗上限是錯的旋鈕**：一次出局讓攻擊者拿 4.89 CC（三次出局是 5.52，差距落在種子離散內），卻讓 200 人裡 **26 個誠實新人進不了門**（誠實交付的失敗率 10.6%，一次不走運就永久排除）。它限制的是「失敗幾次」，限制不了「矇中幾次」。**正確的旋鈕是「要連續 N 次通過才付一次」**，因為它對攻擊者是平方壓制、對誠實者幾乎無感（0.095³≈0.0009 對 0.9³≈0.73）。實測：連續 1 次 → 攻擊者每身分 **5.52 CC**、誠實 196 人／每人 19.2；連續 2 次 → **1.33 CC**、191.7 人／17.7；連續 3 次 → **0.00 CC**、187 人／16.1。**預設定為 3**——那 9 個沒領到的是品質低到連續三次都過不了的節點，而「連續交出三份通過驗收的工作」正是「證明你做得出來」本身，不是誤殺。GATE-0（`--dual-role`、連續 3 次）**7/8，第八條是 G2「量不到」而不是「沒過」**：四個情境全部標不適用，因為它比的是「洗量者的額度 vs 同活躍度誠實者的額度」而後者是 0.0。**原型同步改完（協定加法）**：發樁者連**失敗也要報上來**（`report.outcome` 是這次面板的實際裁決，與 `expected_verdict`「正確答案是什麼」是兩件事），因為連續次數由 Hub 自己數——只報成功的話 Hub 永遠算不出連續，那個旋鈕就不存在。`HUB_ONBOARD_STREAK` 預設 3。閘門 `demo-onboard.js` **10/10**，新增兩條：「要連續 N 次才付一次」（實測 3 次付款，一次一付會是 6 次）與「交付未通過時連續次數歸零」。後者第一次跑是紅的，而**紅在閘門自己的接線上**：Hub 做對了（log 寫著歸零），但偽造工具只等 `error`／`onboard_paid`，沒收 `onboard_progress`。**信任邊界要寫明白**：發樁者是營運方授權的身分，所以它**能夠多報失敗來卡住某個新人**——那個權力本來就存在（它也可以乾脆不發任務給你），制衡是它的支出全部在帳上、可稽核、有治理上限。**條件 (ii)（成交量報告要把洗量與關聯方分開列）仍未做，所以這一列仍記未修** **2026-09-23 補上裁決用的對照，而它改了兩個數字與一個結論**：`docs/evaluation/credit-regime-ab.md`（重跑指令 `cd sim && python3 -u -m amcn_sim.regime_ab`，四體制 × 兩種攻擊、三種子中位數）。(1) **基準換了**：先前那張表拿 `starter=20` 當現況，而常駐節點實際跑的是 **50**——對 50 來說「零額度＋入門採購」只拿回 **28%** 的結算量（2,585 對 9,168），不是 74%。(2) **A 的白拿是 44.17 CC／身分，而 20 個身分就把保險池打穿約 2 倍**（壞帳 889 對風險費收入 450，G3 破）；沒有攻擊時 A 完全健康（73.8 對 388.9），所以這不是參數調錯。(3) **多了一個量出來的中間格 D**：`starter=10`＋入門採購，白拿 **8.56**（白拿 ≈ starter × 0.86–0.88）、結算量回到 45%、壞帳 171 **仍在**風險費收入 201 之內。**建議 D 先行**：C 的反 Sybil 性質整個押在「驗收不會被騙」這個還沒被現場試過的前提上，D 讓那個前提不是唯一的防線，代價是一個可以直接從 starter 讀出來的上界。**2026-09-23 已落實到常駐服務**：`run-hub.sh` 預設 `DEMO_STARTER_CC=10` ＋第五個服務 `com.amcn.onboard`（見 #98，實測第一天額度 4.6 CC 對 23.1 CC）。條件 (ii)（§20-9／§20-10 把洗量與關聯方分開列）**仍未做**，而它現在有了一個具體的數字：洗量佔成交量 6.3%（A）→ 21.4%（C），而拆開看是**誠實量塌了**而不是洗量變多 |
| 91 | **P0**（Owner 問「離上線還差什麼」時量出來的，2026-09-23）| **「常駐跑這個網路」與「外面的人能加入」是兩回事，而登記簿把它們當成同一件事。**#89 把可達性解決了（onion service，零入向埠），但那條路是一個**前景腳本**（`service/run-onion.sh`）：常駐的 `launchd` 只有 hub／panel／agent 三個 label，而 `service/run-hub.sh` 又把 `HUB_BIND` 寫成 `0.0.0.0`。所以 2026-09-23 上午實測這台機器的狀態是——三個服務都活著、Hub 從 09-22 23:04 起正常重建並服務、`./install.sh invite` 照樣印得出邀請，而**此刻從網際網路要加入這個網路，沒有任何東西在聽**（入口隨那個終端機一起結束了），同時排序器反而暴露在區網上。第二個缺陷在同一條路上、而且更難看見：`HUB_RENDEZVOUS` 的記錄在 `listen` 那一刻把要發布的主機名**解析一次就定住**，而常駐服務的順序必然是「Hub 先起來、tor 後起來」（位址要等 tor 建好目錄才存在），所以記錄會永遠帶著回送位址或區網位址——**而它帶著正確的簽章**，於是客戶端老實地去連一個連不上的地方。這與 #40（`dialLazy` 持有一次性 promise）、以及 `HUB_ADVERTISE_PORT` 那個修正（見本列上游）是同一個形態的第三次：**持有解析結果而不是解析方式** | **已修（2026-09-23）**。(a) **入口變成第四個服務**：`./service/install.sh --onion` 裝 `com.amcn.onion`（`run-onion.sh --service`，KeepAlive），並把 Hub 綁回 `127.0.0.1`；`--lan` 切回去時會**真的把入口收掉**（「我以為我沒有對外」與事實不一致比從來沒開過更糟）。模式存在 `configs/.home-mode`，所以它跨重開機成立。(b) **發布位址改成每次重發都重新解析**：`HUB_ADVERTISE_HOST_FILE`（onion 模式預設 `var/onion/hostname`）——入口比 Hub 晚起來或換了位址，記錄都會在一個重發週期內跟上，且只在位址**變了**的時候印一行（每 60 秒印一次會把唯一值得看的事埋掉）。(c) **兩種模式都發布記錄**（`var/rendezvous.json`）：把那個檔放到任何靜態主機就是 §2.1 要求的輪替機制，而承載它的主機不受信任。(d) **遠端 verifier 可以只知道記錄**：`node panel.js rv:<path|url>`＋`AMCN_HUB_PIN`，位址不必給他——原本的邀請是手貼 `.onion`，Hub 一換位址就斷。**閘門 `node demo-rendezvous.js` 11/11**（不需要 tor、不啟動 onion service、不碰你的 launchd）：讀的是 `run-hub.sh --print-env` **算出來的值**而不是原始碼裡的字串（「模式是 onion 但又綁回 0.0.0.0」的回歸只有這樣抓得到）、入口後來才出現、入口換位址、以及三條負對照（別人簽的記錄不跟隨、過期記錄被指名拒絕、區網位址連不上）。**修之前跑同一支是 9/11**，紅的正是「入口後來才出現」與「入口換位址」那兩條。**仍然沒有解決的**：#86 的後半（兩端還是同一台機器），以及**這台機器目前刻意留在 `lan` 模式**——把入口打開是一個會改變暴露面的決定，屬於 Owner，不屬於安裝腳本 |
| 92 | **P0**（Owner 第二次質疑架構走偏，2026-09-23）| **「一個 agent 安裝了這個服務之後就能去收發任務」在程式裡從來不成立，而缺的不是傳輸層。**Owner 的原話：「不是照理說哪個 agent 安裝這個服務之後他就應該就有能力去收發任務嗎，為什麼還要去打開對外入口」。實測三件事：(a) `node agent.js` 不帶設定檔**直接死在 `JSON.parse(undefined)`**，連錯誤訊息都沒說它要什麼；(b) `resolveHubTarget` 的預設是 `127.0.0.1:47180`，也就是「連你自己」；(c) 全 repo **沒有任何 bootstrap／well-known 位址**。所以每一次安裝都是**開一個新的空網路**（`quickstart.sh` 正是這個形狀），而加入別人的網路只能手貼位址與 pin。#89／#91 解決的是「有沒有人連得到」與「入口是不是常駐」，**而這一條是「裝完之後要連去哪」——兩個角色被混在同一個 `install.sh` 裡**：參與者（agent／verifier）只往外撥、永遠不必開埠，要有人可達的只有排序器那一個，而在這個 repo 裡安裝等於成為排序器 | **已修（2026-09-23）**：`lib/bootstrap.js`。預設只需要兩個值——**一份簽署過的位址記錄放在哪**（`rendezvous`）與**要釘住誰**（`hubPin`）；位址本身刻意不是其中之一，因為位址會變而記錄不會。解析順序：`AMCN_BOOTSTRAP`／`AMCN_HUB_PIN` → `node/network.json`（隨 repo 發布的那一份）→ **一個說得出話的結果**（三條可以走的路：加入別人的網路、`./quickstart.sh` 自己開一座島、`--standalone` 連本機），不是例外堆疊。身分自動產生並存成 `configs/.<role>-seed`（0600、已在 .gitignore），所以跨重啟不變（#17／#84 的同一個理由）；`AMCN_CONFIG_DIR` 可覆蓋，讓閘門不碰到這台機器真正的身分。`agent.js`／`verifier.js` 不帶參數即走這條路。**閘門 `node demo-bootstrap.js` 8/8**：沒有預設網路時說出三條路（而不是 `JSON.parse`）、不帶任何參數就註冊成功、記錄解析且比對過 pin、身分跨重啟不變、seed 檔是 0600、**pin 不符就不連**（記錄是真的、簽章有效，但簽的人不是被釘住的那一個）、verifier 同一條路、Hub 那一側真的看到兩種角色註冊。**`network.json` 目前是空的**，而那是誠實的狀態：填進去就等於「發布一個網路」，它會讓 Owner 那台成為事實上的入口（濫用預算 #87、`HUB_MAX_AGENTS`、以及 #90 那份給新身分的額度全部對網際網路生效），所以那是一個要 Owner 點頭的決定 |
| 93 | **P2**（做 #92 時自己撞到兩次，2026-09-23）| **`demo.js` 的埠被佔用時不會失敗，而是永遠掛著。**這台機器有常駐的試點在聽 47180，而 `demo.js` 預設就用那個埠：Hub 印出 `EADDRINUSE` 並退出 1，但 demo 沒有人在看子行程的死活，於是它停在一個永遠不會到的斷言上（實測 300 秒無輸出）。CLAUDE.md 明寫可以用 `DEMO_PORT_OFFSET` 與試點並存，所以這是被支持的用法下的失敗；而失敗被報成了另一件事——**與 #80 同型** | **已修（2026-09-23）**：`spawnProc` 監看 `exit`，子行程非零退出即指名停止並印出最可能的原因（埠被佔用、該用 `DEMO_PORT_OFFSET`）。實測從「300 秒無輸出」變成 1 秒內說出 `hub.js 以 1 退出` 與埠號。`procs` 同時提到模組層，否則那個 handler 會在 `main()` 之外參照不到它 |
| 94 | **P0**（Owner 問「是不是限定在 10 美金以內」時發現，2026-09-23）| **「替別人做事會花掉自己的 token」這件事，原型從來沒有量過，也沒有任何上限。**Owner 以為已經有一個 $10 的上限——查證結果是三個沒有：(a) `adapter.js` 拿到供應商的回應**只讀 `choices[0].message.content`，`usage` 整個丟掉**，所以 token 用量一筆都沒有被記錄過；(b) 唯一的上限是 `policy.quota` 的「unit」（範本 40u／週期）＋ #22 的「沒有餘量不得出價」，而 **unit 與 token、美金之間沒有任何換算**；(c) **美金在程式裡是零**——`1 CC = US$0.05` 只活在文件裡，Owner 記得的 $10 是 #65 的**保證金**提案，而 #90 已經指出那是 CC 不是美金。回報也沒有：Owner Console 有 `quota` 區塊但那是 units、沒有 per-task 用量、而且是**拉取式**（要人去開網頁）。所以一個裝起來、接了真實上游、又武裝供給的節點，可以無上限地把主人的額度賣給網路換 CC——而主人不會被告知任何一筆 | **已修（2026-09-23）**：`lib/spend.js`＋`adapter.js` 回傳 `{content, usage}`。三個責任刻意分開——**計量**：拿得到 `usage` 就記真值，拿不到就估（4 字元≈1 token）並**一律標成估計值**（把估計值當事實混進帳目是本登記簿反覆記過的錯）；**上限**：`policy.spend.{dailyTokenCap, dailyUsdCap, usdPerMTokens}`，每天 UTC 歸零、**跨重啟不歸零**（否則「重啟一下」就是繞過上限的辦法），token 上限不需要價目表就能執行所以一定有預設值（2,000,000），**美金上限沒有價目表就不生效而且啟動時會明說**；上限到了的行為是**停止出價**（同 #21／#22 的同一道守門）——先接單再交不出來是違約，會吃掉對手的 CC 與保險池；**回報**：每筆做完印一行給人看，並寫一行 JSON 到 `out/owner-notices.jsonl` 給主人的 agent 讀（主人通常是另一個 agent），內容是 Owner 要的那三個數字：這筆花多少、今天花多少、還剩多少（含「大概還能接幾筆」）。**上限必須在本機**，理由是查證過的：`key-lending-verification.md` 量到 OpenAI 的硬上限只在控制台（Admin API 沒有那個開關）、Anthropic 的 `spend_limits` 只給 Enterprise。**閘門 `node demo-spend.js` 10/10**，其中最重要的是一組正／負對照：同一個任務、同一個賣方，唯一差別是今天還有沒有預算——有預算成交、用完則**不出價**（實測 `今日token上限已到 (5,000/1,000)——停止出價`）。`fake-provider.js` 現在也回 `usage`，並有 `FAKE_NO_USAGE=1` 當負對照。**一個當時寫錯的「待辦」，2026-09-23 撤回**：原本這裡寫「仍未做：unit 與 token 的換算（1u = N tokens）」。"
"**那不該做，而且理由是定位層的**（見 #102）：把任務單位綁定到 token 數，等於把「買一份通過驗收的交付」變成「按量計費的存取」——"
"那正是 P-10／#67 要避開的形狀。unit 是任務大小，不是 token；CC 的價格是對一份**成果**出價，而 CC 沒有固定現金贖回（FR-054／P-04）|
| 95 | **P0**（Owner 第三次追問架構，2026-09-23）| **「離線的時候整個網路應該可以互相接手」——而實際的接手是把私鑰複製到第二台機器上。**Owner 的原話：「而且離線的時候整個鏈應該都是可以互相接手才對吧」。查證：`standby-hub.sh` 自己老實寫著「用同一個 `HUB_SEED`……代價要照實記錄：**排序器的私鑰從此存在兩台機器上**」，所以「任何人都可以接手」在字面上等於「任何人都有那把私鑰」；升格是人手動跑的（腳本註解：「只在拔線之後執行」）；拉帳是另一個 `pull-loop.sh`；防分叉靠 runbook 的一句話（「M1 不得再接回同一網段」）而不是協定；而 client 只釘**一個** DID（`pin !== did` 即拒絕），所以後繼者除了冒用前任的身分沒有別的辦法被相信。**已經成立的那一半是好的**：帳本可由簽署產物驗證式重建（#10／W10），client 每次重連都重新解析（#40）——缺的只有「誰接手」與「client 憑什麼相信他」 | **已修（2026-09-23）**：`lib/succession.js`＋`standby.js`，協定不變、**沒有任何私鑰移動**。(a) **事先簽好的授權**：現任趁活著時簽 `{successor, priority}`（`HUB_SUCCESSORS`），憑證隨**位址記錄**發布——「事先」是整個設計的關鍵，因為簽它的人之後要離線；client 釘的仍然是前任，但它接受「能從被釘住的 DID 走到你」的後繼者（委派鏈，深度上限 3，與 §2.2 已裁決的 UCAN 委派是同一種東西）。憑證**不在**位址記錄的簽署範圍內：它們是別人簽的，完整性來自各自的簽章。(b) **待命機自己拉帳、自己驗、自己升格**：`standby.js` 取代 `standby-hub.sh`＋`pull-loop.sh` 那一對人工流程，順序是「先驗過才留（`rebuild` 逐筆驗簽、重算鏈、比對 root，對不上就不留）→ 再確認現任連續連不上 → 才升格」，並按 `priority` 錯開（兩個待命機同時升格＝分叉，而錯開是唯一不需要彼此通訊的辦法）。(c) **舊排序器不能趁機搶回排序權**：後繼者活著時位址記錄是新的，而死掉的排序器發不出新記錄——所以「記錄的新鮮度」就是「現在誰是現任」的可檢查事實；帶著舊帳回來的 Hub 被**指名拒絕啟動**，並印出兩條路（改當待命／先取得它的帳再 `HUB_RESUME_AFTER_SUCCESSION=1`）。**閘門 `node demo-succession.js` 11/11**，而它逼出兩個 bug，兩個都比原本的功能重要：**(i) 接手會讓帳本在第三方手上驗不過**——`lib/rebuild.js` 只認一把 `hub_pub`，而後繼者的帳裡有**前任簽的 checkpoint**，於是 `checkpoint #0 signature invalid`（實測）。修法：允許的簽署者＝現任＋**能經委派鏈走到現任的**每一個前任，而鏈跟著匯出走（`ex.succession`）。沒有這一條，「任何人都可以自己驗這本帳」在接手之後就不成立了。**(ii) 待命機被停掉時，它升格出來的排序器變成孤兒繼續服務**（還繼續發布位址記錄）——那在生產上就是分叉。修法：child 的生命週期綁在 standby 上。閘門自己先被這件事咬了一口：上一輪留下的孤兒佔著埠，讓下一輪升格 `EADDRINUSE`，而報表把它讀成「升格失敗」。**四條負對照**：沒有授權的人（連自己簽給自己的憑證一起附上）client 不跟；沒有驗過的帳本**不准升格**（空白排序器會把所有人餘額歸零）；舊排序器被拒絕啟動；釘一個沒有授權關係的 DID 時同一本帳被拒絕。**(iii) 第三個被閘門逼出來的缺口**：`verify-ledger.js --pin <前任>` 在接手之後會失敗（`expectHubDid` 只比對現任），而那正是 JOIN.md 對參與者的宣稱——同一條委派鏈的判準因此也搬進 `rebuild` 的身分檢查，並在輸出裡**說出「接手」**（它是事實，不是警告：把正常的接手印成警告會訓練人忽略警告）。**不宣稱**：這是**有紀錄的 failover，不是共識**。兩個後繼者同時升格＝分叉，靠 priority 錯開與「後繼者延伸它驗過的最後一個 checkpoint」壓低機率，剩下的要人判（偵測本身已有，#69c／#72）。**這也是 #72 缺的那一半的答案**：偵測到說謊的排序器之後可以做的事，就是輪替到一個被授權的後繼者。仍未做：四小時多機拔線演練（#86／W10），因為這一輪全部是單機多行程 |
| 96 | **P0**（Owner 說明推廣方式時發現，2026-09-23）| **Owner 的推廣方式是「repo 公開、別人 clone、然後整個程式就起來」，而 repo 裡沒有那一個指令。**現有的兩支都不是它：`quickstart.sh` 在對方那台**開一座自己的島**（Hub＋驗收者＋兩個 agent，帳上全是他自己的關聯方交易），`service/install.sh` 把對方變成**主辦**（常駐排序器、要有人連進來）。#92 讓 `node agent.js` 有了預設網路，但那還不是「安裝」——缺的是**前置條件**：Node 版本、`.onion` 位址需要本機有 tor（`.onion` 在 DNS 裡不存在，所以這件事必須在啟動前確定而不是讓三個行程各自靜靜重試）、身分要存下來、以及「我到底連去哪、釘的是誰」要先講清楚 | **已修（2026-09-23）**：`node/join.sh`——**參與者**的那一支（與 quickstart／install 的差別是角色而不是規模：參與者只往外撥，永遠不必開埠）。`./join.sh` 預設當驗收者（門檻最低：不需要 key、模型、信用），`--verifiers N` 走 `panel.js` 讓每個驗收者有自己的身分（押注綁在身分上，#38），`--provider` 把網路那三個值**先填好**再交給人（手抄 hubPin 是最容易貼錯的一步）並指名還要補什麼（`adapter.terms.attested`、`policy.spend.usdPerMTokens`），`--check` 只做前置檢查、`status`／`stop` 收尾。tor 缺席時**說出怎麼修並且不啟動任何東西**；裝好了它會自己把 tor 當客戶端起來。`lib/bootstrap.js` 同時支援第二種發布方式：**靜態位址**（`network.json` 的 `hubHost`＋`transport`），因為位址記錄有 10 分鐘的新鮮度要求，而一份 commit 進公開 repo 的記錄十分鐘後就過期——**onion 位址本身幾乎不變**，所以靜態位址＋pin 才是「commit 進 repo」這個發布方式的正確形狀，rendezvous 留給「之後要搬家」。**閘門 `node demo-join.js` 9/9**（含兩條負對照：沒有網路可加入時說出三條路、`.onion` 而本機沒 tor 時指名拒絕）|
| 97 | **P1**（修 #96 的閘門時，三條紅的斷言都是它造成的，2026-09-23）| **這個 repo 的腳本滿是中文輸出，而 bash 會把 `$變數` 後面緊接的非 ASCII 位元組吃進變數名。**`echo "…（$SOCKS）…"` 取的是變數 `SOCKS）`，配上 `set -u` 就是 `unbound variable` 並**中止腳本**——而它最常出現在錯誤訊息裡，也就是**最需要腳本說話的那條路**。實測 `join.sh` 的 tor 缺席分支因此整段沒印出來，而閘門把它讀成「沒有說出怎麼修」；同型的還有 `service/deploy-hub.sh:42`（SSH 連不上的錯誤訊息）與 `service/install.sh:182`（`--onion` 安裝完的最後一段輸出，也就是**推廣路徑本身**）| **已修（2026-09-23）**：三個檔案共 9 處改成 `${VAR}`，並在 `demo-join.js` 加一條**掃整個 repo** 的斷言——它是一個類別而不是一個 bug，而這類問題會在別人的機器上、在錯誤路徑上、在沒有人看的時候發作。`bash -n` 抓不到它（語法合法），所以守門只能是這種掃描 |
| 98 | **P1**（把 #90 的裁決落實到常駐服務時發現，2026-09-23）| **每支程式都有閘門，但「這台機器實際上跑的是什麼」沒有。**新人的第一筆額度、發樁者有沒有被授權、花費上限開了沒——這三件都由 `service/` 的腳本決定，而它們從來沒有被守過。其中兩件會**安靜**出錯：(a) starter 是一個環境變數，而環境變數最容易在某次重構裡回到預設值，那種回歸不會讓任何 demo 變紅，但白拿 ≈ starter × 0.87，所以它就是對 Sybil 的曝險上界；(b) 發樁者的授權有**順序**：Hub 讀 `configs/.onboard-seed` 算出 DID 才能授權它，所以那個檔必須先存在——反了的話發樁者的每一筆都被拒絕，而那看起來只像「新人沒有工作可做」 | **已修（2026-09-23）**：`service/run-onboard.sh`（第五個常駐服務 `com.amcn.onboard`）＋`run-hub.sh` 落實 #90 的 **D 組**：`DEMO_STARTER_CC=10`、`HUB_ONBOARD_DID`（由 seed 檔推導）、每身分 20／全網 2,000 的治理上限、`HUB_ONBOARD_STREAK=3`。`install.sh` 在起 Hub **之前**先產生發樁者身分（順序即修法），並把它列進安裝完的輸出與 `status`。**閘門 `node demo-service.js` 6/6**，而關鍵那一條量的是**行為不是字面值**：同一個全新身分第一天拿到的額度 starter 10 → **4.6 CC**、starter 50 → **23.1 CC**（實測），所以那個旋鈕真的在作用 |
| 99 | **P0**（把 starter 調成 10 之後 live 節點直接停掉，2026-09-23）| **調整一個經濟參數會讓這台機器起不來，而且是整個網路停在那裡。**實測：`./install.sh` 把 `DEMO_STARTER_CC` 從 50 改成 10 之後，Hub **拒絕啟動**——`credit line mismatch did:demo:b31519066: rebuilt 5.160 vs export 25.798`。它拒絕得**對**：那本帳裡存的 `credit_lines` 是用舊政策算出來的，而重建方用新政策重算必然對不上（#82 的檢查正在做它該做的事）。問題在**政策被當成環境變數**：`lib/eeff.js` 的 `STARTER_CC` 是 module-level 常數，`rebuild()` 用的是**重建方自己的**值。這與 #82（年齡斜坡）、#60（兩邊各算各的）是同一個形態的第三次，而這次的代價是 live 停機——panel／agent／onboard 都活著，只有排序器在 launchd 的重試迴圈裡反覆拒絕啟動 | **已修（2026-09-23）**：**starter 跟著帳走**。`buildExport()` 加 `starter_cc`（與既有的 `age_ramp_ms` 並列，理由完全相同：它是**那本帳的政策**），`eeff.creditLine()` 收一個 `starterCc` 參數，`rebuild()` 用匯出裡的值重算。政策不同時 `rebuild` 回一條 **note**（不是 warning——接手一個政策換過的帳本是事實，不是瑕疵；把它印成警告會訓練人忽略警告），而 Hub 啟動時把 note 印出來，所以「這本帳的 starter 是 50、我設定的是 10」會出現在那幾行裡。**#99 之前的匯出沒有這個欄位**，所以對它們額度比對是**無從檢查**而不是不通過（與 `exported_at` 那一條同一個處置，warn 並略過一次；下一份匯出就帶著它）——live 節點靠這條豁免恢復服務（實測 03:25:51 起來，`starter CL 10`，Σ=0，4 筆收據完整，下一份快照已帶 `starter_cc: 10`）。**閘門在 `demo-service.js`（9/9）**：一筆真的成交之後把 starter 從 50 換成 10 再啟動（必須起得來並說出政策換過），外加一條負對照——**謊報**匯出裡的 starter（改成 999）時額度重算對不上（實測 `rebuilt 462.038 vs export 23.125`）。那條負對照第一版是空過的，兩個原因都值得記下：(i) 匯出的 `credit_lines` 只收 **agent 角色**，所以用 verifier 去等那個欄位永遠等不到；(ii) 額度比對只比對得出 stats 的帳戶，而 stats 由收據與事件重播而來——**一個只註冊過、沒有任何紀錄的帳戶根本不會被比到** |
| 100 | **P1**（發布網路之後看 live 節點的第一個十分鐘，2026-09-23）| **入門採購的資格是在「交付之後」才檢查的，所以不符資格的人白做一次工——而在真實上游上那是白燒自己的 token。**實測 live 節點發布後十分鐘：`task broadcast 10`、`SETTLED 0`、`REJECT（onboarding）10`。流程是發樁者廣播 → 唯一在線的賣方（Owner 的 home agent）出價並**真的執行** → 發樁者報告 → Hub 這時才說「onboarding is for identities that have never been paid anything」。兩個後果：(a) **噪音**：每分鐘一輪被拒，log 被灌滿，而真正的錯誤會埋在裡面；(b) **真實成本**：這一輪賣方是 mock adapter 所以不痛，但一個接了真實上游的節點會為一份**註定不會被付款**的合約燒掉 token——而那件事現在第一次量得出來（#94）。這也解釋了一個更大的事實：**這個網路目前做不出任何工作**，所以剛加入的驗收者賺不到東西（實測那個從 GitHub clone 進來的陌生人 verifier 餘額 0）| **(a) 已修（2026-09-23）**：發樁者收到那個理由就**指數退避**（上限 10 分鐘，`HUB_ONBOARD_BACKOFF_MAX_MS`），並且只說一次——而且說的是市場狀態而不是錯誤：「線上的帳戶都已經賺過 CC……有人加入就會自己恢復」。`onboard_paid` 一到就解除退避（沒有這條，退避就是一個只會變長的計時器，等於把發樁者關掉）。**(b) 未修，而修法在出價那一側**：資格（「從沒收過 CC」）是公開帳本推導得出的，所以發樁者應該在**決標前**就把不符資格的出價濾掉，而不是讓對方做完工才拒絕。那需要發樁者自己讀一份匯出（`lib/ledgerfetch`＋事件流重建，Hub 仍然是最終守門）。**(c) 更根本的那一條不是程式**：發布之後這個網路只有一個交易者（Owner 的供給端，`posts: 0`、無 `policy`），所以**沒有需求**——沒有需求就沒有結算，沒有結算驗收者就沒有收入，而入門採購也沒有合格的買賣對象。第一個真正的成交需要一個**帶著自己上游的 provider**（`./join.sh --provider`）或 Owner 自己再跑一個會買的 agent——而後者的量全部是關聯方（#63），必須照實標註 |
| 101 | **P1**（Owner 決定用自己的 key 讓節點真的賣算力，2026-09-23）| **`adapter.js` 只會講 OpenAI 相容的 `/v1/chat/completions`，而 Owner 手上的是 Anthropic 的 key。**兩家不是「換一個 baseUrl」的關係：路徑是 `/v1/messages`、認證是 `x-api-key`＋`anthropic-version`、回應是 `content[]` 而不是 `choices[]`、usage 的欄位叫 `input_tokens`／`output_tokens`、第三方歸屬欄位是 `metadata.user_id` 而不是 `user`。**最貴的那一個是 usage**：對映寫錯的症狀是「賣得出去但花費上限拿不到數字」，於是 #94 那層保護變成裝飾，而它正是「不要燒光主人的 token」唯一可強制執行的一層 | **已修（2026-09-23）**：`adapter.js` 認第二種上游形狀（`adapter.api: "anthropic"`），刻意用原生 `fetch` 而不是官方 SDK——這個 repo 的硬規則是**零第三方相依**，而那條規則的理由就是推廣路徑的第一步（clone 完不必 `npm install`）。`fake-provider.js` 同時學會第二種形狀，所以閘門能在**不花錢**的情況下驗它。**閘門 `demo-spend.js` 13/13**（新增三條）：回應解析得出內容、**usage 欄位對映正確**、第三方流量在這一家也具名。常駐節點的設定已寫好（Opus 5 的價目表 $5／$25 每百萬，美金上限才算得準），但**兩件只有 Owner 能做的事刻意留空**：key 放進 Keychain，以及 `terms.attested` 由他自己設 true（P-10／#67：那是聲明不是驗證）。`run-agent.sh` 也不再塞 `sk-home-placeholder` 假 key——解析不到就讓 agent 明說「supply NOT armed」，那比看起來武裝好、每一筆都失敗誠實 |
| 102 | **定位風險**（Owner 質疑，2026-09-23）| **「把模型的每百萬 token 價格寫進設定檔，會不會變成在賣 API 存取？」**Owner 的原話：「我們目前的定義不就是一個賞金獵人的狀態嗎？發任務接任務，應該不會把模型費用價格寫下去吧」。這個質疑指到真的地方：條款禁止的是**共用憑證**與**轉售存取**（`key-lending-verification.md` §3，OpenAI 明文：不得買賣轉讓 key、不得 resell or lease access），而「按 token 計價」正是轉售最像的那個形狀 | **查證結果：程式沒有踩到，但文件的寫法可以踩到，所以兩邊都處理。****(1) 事實**：`usdPerMTokens`／`dailyUsdCap` 只在 `lib/spend.js` 被讀到（grep 全 repo），它是**本機預算輸入**——用來決定「今天還要不要出價」，而不是報給任何人的價；收據裡只有 `contract_id`／`delivery_hash`／`acceptance_method`／`tx_class`／`postings(amount_cc)`，**沒有 token 數、沒有美金、沒有模型名**。也就是說合約買的是「一份通過驗收的交付」，那正是賞金而不是計量存取。**(2) 新的可檢查保證**（`demo-spend.js` 15/15，新增兩條）：帶著價目表跑完一筆真的結算之後，掃整本匯出**含 `raw_log`**，斷言美金、價目表數字、token 用量、模型名**零命中**；並斷言收據的形狀是交付＋驗收而不是用量。「成本不離開這台機器」從此是一條會變紅的斷言，而不是一句話。**(3) 撤回一個當時寫錯的待辦**：#94 原本記「未做：定義 1u = N tokens」——**那不該做**，因為把任務單位綁到 token 數就是把賞金變成按量計費。unit 是任務大小，CC 是對成果出價，而 CC 沒有固定現金贖回（FR-054／P-04）。**(4) 用字**：設定檔與文件一律稱它為「**你自己的花費上限／預算**」而不是「價目表／定價」，並且不得在對外文件裡把 USD/token 與 CC 並列——那會讓讀者自己推導出一個隱含的每 token 美金費率。**(5) 仍然沒有解決的那一條（本來就沒有）**：用自己的帳號替第三方跑推理**在多數條款下仍可能構成轉售**——那是 AMCN 這個構想第一天就有的前提，處理方式是 P-10 把舉證責任放在節點擁有者（`terms.attested` 由他自己設）。契約面最乾淨的兩條路仍然是：**本機模型**（零條款曝險），或**產品本身就支援為每個使用者發帶上限 key 的聚合型上游**（如 OpenRouter，需逐條讀它的條款）|
| 103 | **P1**（準備第二台機器加入時發現，2026-09-23）| **入門採購買的「答案已知的工作」是算 sha256，而那對任何人都是免費的。**`canary.js:45` 的題目是 `{op:'sha256_eq'}`，Hub 也強制只收確定性斷言（`hub.js:787`，理由是對的：答案不可判就無法定價身分）。但後果是**模擬器裡那個前提在原型裡不成立**：#90 量到「攻擊者每身分白拿 0.00 CC」，靠的是模擬中攻擊者**交不出正確答案**（品質 0.1）；而 sha256 不需要模型、不花錢、任何會跑 node 的人瞬間就能算。所以真實的曝險是：每身分 20 CC × 全網上限 2,000 CC ＝ 100 個身分可以幾乎零成本取得 2,000 CC。CC 不能換現金，所以損失不是錢，是**對別人未來工作的請求權**被稀釋（而那正是 CC 的全部意義）| **未修，而修法不是把題目變難，是讓它「值那個錢」**。三個方向，代價不同：(a) **把全網上限壓到與現況相稱**（第二台只需要 20 CC，`HUB_ONBOARD_TOTAL_CC=200` 就夠十個新人）——不解決性質，但把曝險綁在一個治理旋鈕上，是開放給陌生人之前最便宜的一步；(b) **題目改成需要真實推理、但答案仍可判**（例如「摘要這段文字，必須包含這三個事實」＋`contains`）——攻擊成本變成一次真實推理，但驗收從「確定性」退到「弱斷言」，而 Hub 目前**強制** `sha256_eq`，所以這是協議層改動；(c) **要求新人先完成一筆真實需求方的任務**才付入門採購——最貼近「證明你會做事」，但需要網路上先有需求。**在改之前，對外說法必須誠實**：入門採購現在是一個**有上限的水龍頭**，不是一個已證明的反 Sybil 機制。#90 那張表的 0.00 仍然成立於「攻擊者交不出正確答案」的情境，不成立於「題目誰都會做」的情境 |
| 104 | **P2**（同日，被兩條突然變紅的斷言暴露，2026-09-23）| **兩條閘門的負對照其實依賴「這台機器當時的狀態」，而狀態一變它們就空過或誤紅。**(a) `demo-join` 的「`.onion` 但本機沒有 tor」：Owner 今天把 onion service 開起來之後 `:9050` 有人聽了，於是 `join.sh` 正確地跳過缺 tor 的分支——那條負對照**先前會綠只是因為當時沒跑 tor**；(b) 同一支的「還沒有網路可加入」：`network.json` 今天被填好之後，只清環境變數已經造不出那個狀態。兩者都不是功能壞了，而是**測試把環境當成了固定值** | **已修（2026-09-23）**：(a) 負對照改指向一個**沒有人在聽**的 SOCKS 埠，與本機有沒有跑 tor 無關；(b) 改成自己造一份空的 `network.json`（`AMCN_NETWORK_FILE`），與 repo 有沒有發布網路無關。順帶一提，這一輪的第三條紅是 #97 的掃描器抓到我自己新寫的 `service/arm-supply.sh`（`${SERVICE}）` 少了括號）——那條掃描存在三小時就已經回收成本 |
| 66 | **P1**（Owner 提議，2026-09-17）| **保管費（demurrage）作為收益來源與正餘額的回流機制**。Owner 的原話是「除了交易產生的所得之外，有沒有可能藉由保管的費用來作為可能的收益」。架構裡**已經有這個位置**：§2.2「單方 posting 合法性」那一列明列 `demurrage` 為 Hub 可執行的非雙簽 posting 之一，條件是必須引用 Owner 加入網路時預簽的費率表 Grant——所以機制已設計、費率未定、實作為零。**它同時是今天量到的缺口的解法**：#62（verifier 純吸收）、#64（結構性順差者變成吸收端，實測 +73.97 CC 零流動）與 FR-056「正餘額無處可花」都是同一個形態，而保管費正是逼正餘額回到流通的那股力。**但「收益」與「回流」是互相拉扯的兩個目的**：收進 Treasury 就只是換一個吸收端（#61 量到 protocol 帳戶吸走結算量的 1.3–4.8% 且永不回流），真正要回流就得讓 Treasury 花出去（§2.2 設計的金絲雀、逆週期收購、L_boot 補貼），或把保管費直接重分配給活躍交易者 | **已在模擬器實作並掃描**（`amcn_sim.sweep_demurrage`，費率 {0, 0.2, 0.5, 1}%/天 × {全額→Treasury, 閒置→Treasury, 閒置→重分配} × N {10, 50}，**三種子**）。結論分三層，而且與提問的兩個目的方向不同：**(1) 流通效果測不出來。** 種子離散完全壓過組間差異（N=50 結算量 2687–3052 CC，而每組的半幅是 ±930–1380），流速同樣（1.91–2.16，半幅 ±0.4–0.56）。任何「保管費提高/降低流通」的說法在這個費率區間都沒有證據。**(2) 當收益是可量化且可觀的。** 1%/天、N=50 收到 197 CC／84 天，約為結算量的 **6.9%**，而現行基本費是 2.5%——也就是它能把協議收入翻倍以上。**(3) 但它不是 #62／#64 的解法。** 唯一單調的訊號是 verifier 持有量隨費率下降（116→106→96→78），而那筆錢流向 Treasury——#61 已量到 Treasury 不回流，所以這是**把一個吸收端換成另一個**。而且算得出為什麼打平：verifier 收入是成交量的 4%（≈114 CC），1%/天對 ~100 CC 收約 84 CC，兩者同量級。要真的抽乾累積者，費率必須**超過他們的收入率**，那是另一種力度的政策、有它自己的代價。**實作過程修正一個定義錯誤**：第一版把「閒置」定義為「最近沒有分錄」，而 verifier 每收一筆費用就算有動——儘管餘額只增不減，於是重分配模式對它們毫無作用（119±43 vs 116±36）。改為「最近沒有**流出**」後收取量升約 60%（54→90 CC）。**（2026-09-18 更新：write-off 路徑已補上，所以保險池現在真的會付出去——#61 的「protocol 帳戶只進不出」少了一個，但 Treasury 仍然只進不出。）** **真正的回流槓桿仍在 §2.2 設計的 Treasury 支出那一側（金絲雀、逆週期收購、L_boot 補貼），而不是向持有者收費**——#61 量到的「protocol 帳戶只進不出」才是未被處理的那一半。**2026-09-20 補上一個單位陷阱與一個可跨模型比較的量**：我試著用原型四小時跑量到的累積者收入率去錨定本欄掃描的 %/天 費率，得到 **615.80%/天**——那不是結論，是**單位錯誤**。原型的經濟時鐘比模擬器快約兩個數量級（需求每 900 ms 一跳 vs 模擬 84 天），「%/天」在兩邊不是同一個單位。**無因次的「留存率＝淨額÷流入」才能直接比**，而它同時說明本欄的修法對誰有效：純 verifier 留存 **99.4%**（收 859 付 5，設計上不消費）——保管費這種**存量**費對它對症；但 #62 階段 4 之後的累積者留存只有 **23.0%**（收 8,840 付 6,809），它的集中來自**吞吐量不對稱**而不是不肯花錢，存量費要抽乾它就得追上淨流入速度，那是**流量**問題。**所以「保管費是不是解法」取決於吸收端是哪一種**，而原型現在兩種都量到了。`chaos-run` 的人口報表固定輸出留存率。**2026-09-20：流量側已實作並掃描**（`amcn_sim.sweep_inflow`，費率 {0, 10, 25, 50}% 的**當期淨流入** × {Treasury, 重分配} × N {10, 50}，**七種子**）。基數是「這一期餘額長了多少」而不是「持有多少」，所以對高吞吐但會花錢的人近乎免費。`ledger.inflow_fee()` 與 demurrage 分開記帳，Σ 仍為 0；rate=0 時基準線逐位元不變（結算 1364.6299 前後相同，21 項測試通過）。**結論分三層，而第三層最重要**：**(1) 抽取效果成立且單調。** verifier 持有量 118.3 → 103.6 → 87.0 → **58.0**（→Treasury）與 118.3 → 109.0 → 97.0 → **69.6**（→重分配），四個費率級距單調、兩種去向一致，50% 時差距大於各自的半幅（58.0±26.7 對 118.3±62.1）。對照 #66 的存量費在其測試上限只做到 116→78。**(2) 回流效果「方向對但證不出來」。** 重分配模式下結算量 2886→3334、流速 1.93→2.40、貼上限 15.6%→13.6%，三個指標都隨費率單調改善——**但每一組兩兩差距都落在七種子的離散之內**（結算半幅 ±1372–1507）。按本登記簿對 #66 的同一個標準，這**不構成證據**，只構成值得再測的方向。**Treasury 與重分配的不對稱則很清楚**：Treasury 抽得更多（58.0 對 69.6）卻讓結算更少（2792 對 3334）——收進去不花出來就只是換一個吸收端，與 #61 一致。**(3) 這個掃描測的不是促成它的那個問題。** 模擬器的人口裡，累積者是**獨立的 verifier**，也就是留存 99.4% 的**存量**型；而促使我做流量費的是原型 #62 階段 4 量到的**留存 23.0%、高吞吐**的累積者，那種參與者**在模擬器裡根本不存在**（`n_verifiers` 是獨立角色，沒有雙角色人口）。所以本掃描證明的是「流量費也能抽乾存量型吸收端」，**還沒回答**「它對流量型吸收端是否比存量費有效」——那需要先把雙角色人口搬進模擬器，也就是把 #62 階段 3 的形狀補到 `sim/` 這一側。**這一點本身是個發現**：原型與模擬器現在描述的是**兩種不同的經濟**，而留存率是唯一能指出這件事的量。**2026-09-21：模擬器補上雙角色人口，而決定性的結果是「費率控制的是速率，不是結構」**。先前這個掃描測不到促成它的問題，因為模擬器只有獨立 verifier（留存 99.4% 的存量型）。現在 `dual_role=True` 讓 panel 從交易者裡抽（原型 #62 階段 3 的形狀，並補上「當事人不得驗自己的合約」），驗證費因此落在會花錢的帳戶上。**它重現了原型的方向**：verifier 持有 119.73 → 39.94（−67%），但 Gini 0.242 → **0.303**——集中度反而上升，吸收端從 verifier 換成一個交易者。**真正的發現在留存率上**（五種子，N=50）：首位持有者的留存率是 **100.0 / 90.0 / 75.1 / 50.3 %**，對應費率 0 / 10 / 25 / 50%——**留存率 ＝ (1 − 費率)，精確且半幅近乎為零（±0.0–1.1）**，而且**獨立 verifier 人口給出完全相同的關係**（90.1 / 75.4 / 50.0）。這是這條線上第一個不落在種子離散之內的效果，它是機械性的、與吸收端型態無關。**但首位佔比幾乎不動**（19.7% → 17.9%，落在 ±2.8 之內）：每個人的留存率都等比下降，相對份額因此不變。**所以流量費限制的是 CC 流向吸收端的**速率**，不改變**誰**是吸收端**——後者由需求／容量結構決定（#64），任何費率都碰不到。**模型差距也量得更精確了**：模擬器的首位持有者留存 **100%**（完全不買的純淨賣方），而原型四小時的累積者是 **23%**（高吞吐、把四分之三花掉）。加雙角色是必要的但不夠——剩下的差別在**需求模型**，不在角色結構 |
| 65 | **P1**（Owner 提議，2026-09-17）| **上線時以保證金制度限制壞帳，並由營運方先行代墊（約 $10）作為推廣**。Owner 的原話是「應該要有保證金制度，避免太多人使用然後都不還造成大量壞帳」「這個保證金其實我可以自己先發，當作推廣，可能就 10 塊錢就好，用了的人就是只能先用 $10」。**這兩半在架構裡都已有位置**：INV-C1 的 `collateral` 項就是保證金（`credit_limit ≤ k_earn × E_eff + collateral + L_boot`），而「營運方代墊、每人上限」就是 §2.2 的 L_boot＋Treasury「創世補貼額度」（tx_class=subsidy）。**而且它正好是 #50 的答案**：#50 記的是「L_boot 的反 Sybil 維度沒有證據，因為身分免費、單身分攻擊收益上界 46.25 CC 而成本趨近 0」——保證金讓身分創建有成本，那正是缺的那一項。#38（verifier 押注的 earn-your-stake vs pay-to-play）是同一個問題的另一面 | **已在模擬器實作並掃描**（`amcn_sim.sweep_deposit`：starter {50, 200} × 保證金 {0, 100, 200} × LTV {1.0, 0.5}，N=50、deadbeat 15%、三種子）。INV-C1 的 `collateral` 項與 `write_off` 的「保證金優先賠付」都補上了。**四個結論**：**(1) Owner 的直覺是對的，而且比「謹慎」更強——$10 的推廣額度在沒有保證金時 G3 只有 1/3 種子通過**（壞帳率 2.11%，每個違約身分淨賺 50.8 CC ≈ $2.54）；現行 50 CC 額度無保證金時 G3 是 2/3。所以那個額度不是「可以考慮加保證金」，是**必須**。**(2) 等額保證金不夠**：200 CC 保證金＋LTV 100% 仍讓每身分淨賺 26.1 CC，因為**保證金同時買到等額的額度**，對打算違約的人是損益中性的——抵押 D、借走 D+L_boot、違約、賠掉 D，淨賺 L_boot。**(3) 折扣率才是有效的旋鈕**：LTV 0.5 時淨賺降到 3.1 CC（壞帳 0.08%，G3 3/3）。推導出的規則並經掃描驗證：**淨賺 ≈ L_boot(t=0) − 保證金 × (1 − LTV)**，所以**保證金 ≥ starter × 0.5 ÷ (1 − LTV)** 時攻擊無利可圖（t=0 的有效額度是 starter 的一半，見 #60 的年齡斜坡）。**(4) 但這讓「推廣」變成「配對」**：要在第一天給 $10 可用額度，使用者必須以 LTV 50% 抵押 $10——那不是免費贈與。要真的免費（零保證金），額度就得留在現行 50 CC 附近才維持 G3。**副作用一併量到**：額度總量抬高會壓低成交率（200/LTV1.0 組成交 79.8% vs 零保證金的 97.0%），因為需求增加而供給端沒有——那是「供需深度」被拉開，不是撮合變差。**原型已實作（2026-09-17）**：`collateral_post`／`collateral_release` 兩個簽署訊息、`AMCN_COLLATERAL_LTV`（預設 0.5，取自本掃描）、抵押品由帳本事件重放並與匯出欄位核對（#20-4 要求餘額來自簽署事件，一個只存在於摘要欄位的鎖定額可能與產生它的分錄不一致）。**兩條守門**：(a) 只能抵押**自己的正餘額**——`balance < amount` 即拒絕，否則等於用信用額度抵押信用額度、無擔保放大；(b) 取回時必須讓**剩餘額度仍覆蓋現有負債**，否則「先借滿再抽走擔保」是兩步走的同一個攻擊。入口放在 Owner Console（`POST /collateral`）而非 agent 自主行為：要不要把正餘額換成額度上限是風險判斷，不該由需求模型代勞（FR-081）。協議 v5。紅隊 S13–S15 驗證三個攻擊面，demo 驗證「額度只升 amount × LTV 而不是 amount」。~~**仍未實作的那一半**：沒收~~ **已於 2026-09-18 交付**：原型補上違約偵測與完整瀑布（抵押品 → 保險池 → `protocol:loss`），觸發條件與模擬器一致（離線超過 `HUB_DEFAULT_AFTER_MS`、預設 14 天，且餘額仍為負）。用「離線」而非「逾期未還」是刻意的——還在線上交易的負餘額不是違約，那是 FR-052 的正常互惠信用。情境 `default-writeoff` 實測：沖銷 27.77 CC、違約率 0.089、瀑布依序、Σ 仍為 0、54 筆結算下不變式零違反。**§20-10 的違約率因此從代理指標變成真值**（`default_rate` ＝ 已沖銷 ÷ 結算量，與模擬器的 `bad_debt_rate` 同定義，兩邊第一次可比）。**2026-09-21：本欄的保證金公式低估了一倍，已由 #50 的內生化模型修正**。原公式 `保證金 ≥ starter × 0.5 ÷ (1−LTV)`（＝50）用的是 **t=0 的有效額度**（starter 的一半）。但攻擊者不必在 t=0 動手——**等 30 天讓年齡斜坡走完，拿到的是成熟額度 46.25 CC**。實測每身分白拿 45.86，而讓它歸零的保證金是 **92**，不是 50。正確的式子是 **保證金 ≥ 成熟額度 ÷ (1−LTV)**。**年齡斜坡對有耐心的攻擊者沒有防禦力，它只是延後**——這一點在原欄位裡沒有被說出來 |
| 64 | **P1** | **小網路存活取決於「需求／容量比」而不是異質性本身**，而所有既有情境與試點設定都是同質最壞情況。追 #61 的 33% 差額時發現：模擬 N=3 換五個種子，貼上限比例落在 0–66.7% 之間，取決於抽到的人口有沒有結構性淨賣方。於是把 chaos 的人口改成異質——**第一次改錯了**：讓需求／容量比也不同（40/5、55/3.5、30/6.5），結果比同質**更糟**，170→25 筆結算、最長空窗 433 秒。實測原因：容量 55／需求 3.5 那個成了永久債權人（**+73.97 CC、零次額度耗盡**，FR-056 的「正餘額無處可花」再一次），而容量 30／需求 6.5 那個 362 次撞到上限、永久貼牆。改成**只變規模、比例固定 8:1**（40/5、56/7、28/3.5）後恢復健康：170 筆結算、最長空窗 130 秒、額度耗盡 14／28／0，沒有人永久貼牆 | **harness 已修**：`chaos-run.js` 預設異質等比例人口，情境可用 `symmetric: true` 明確選擇最壞情況（`soak-short` 就是這樣標的），並新增 `soak-mixed` 做對照。**設計輸出**：小網路的存活條件不是「參與者要多樣」而是「每個參與者的需求與自身容量要相稱」——任何結構性順差者會變成吸收端、結構性逆差者會永久貼牆，兩者都讓市場變薄。**試點設定檔 `pilot-m{1,2}.example.json` 是同質的**，所以那份三台機器的觀測也是最壞情況，runbook 需要註明。**2026-09-20：它現在是四小時尺度上的主要瓶頸，而且被 #62 的修法反襯出來**。`soak-4h-dual`（20 個雙角色節點、沒有任何非交易帳戶）四小時之後，**單一 agent C 持有全部 2,102.39 CC 正餘額，第二高是 −0.52**，而 C 的峰值負債只有 11.2 CC／額度 500——結構性順差者不必是 verifier，只要需求長期低於容量就會變成吸收端。本欄原本記的是 N=3 時的 +73.97 CC，四小時 N=20 的版本大了近 30 倍。**2026-09-21 模擬器側確認它才是根因**：流量費（#66）能把首位持有者的留存率精確壓到 (1 − 費率)，卻幾乎不動首位佔比（19.7% → 17.9%，在離散之內）。**費率控制速率，結構決定對象**——而結構就是本條目說的需求／容量相稱性。換句話說，#62 的 (a)、#66 的存量費、#66 的流量費，三支槓桿都不解這一條。**2026-09-21：本條目的「結構性順差者」在模擬器裡並不是永久角色，而我一直用瞬時統計量在看它**。Owner 把框架拉回最初的定義——「它就是個 AI 賞金獵人館，有 AI 發任務也有 AI 接任務」——而那句話指出了模型的一個限制：`mean_daily_demand` 在 `build_population` 裡**一生只指派一次**（`utilization = r.choice([0.3 … 1.3])`），所以抽到低利用率的 agent 是**被指派**為永久淨賣方的。「永久低需求者會永久累積」離重述輸入很近。**加上需求漂移之後（每 N 天重抽利用率）量到兩件事**（五種子、N=50、雙角色）：**(1) 首位佔比完全沒降**——19.7% → 20.8% → 22.1% → 22.3%（漂移 28／14／7 天），全部落在離散之內，所以「換角色」不會讓集中消失；**(2) 但首位是輪流當的**——整輪 84 天裡當過「當日首位」的不同帳戶數是 **3.8（固定）→ 5.0（每 7 天漂移）**，也就是說**即使需求固定，也不是一個人永遠吸**。新增 `DailySnapshot.top_holder` 與 `distinct_top_holders` 才看得出這件事：**瞬時佔比在「同一人永遠吸」與「每月換人」之下長得一模一樣，而兩者對「經濟能不能運作」的意義完全相反**。流速則隨漂移單調上升 2.20 → 2.99（+36%）。**所以本條目要重新表述**：~~結構性順差者是吸收端~~ → **持有集中是庫存而非水槽，只要持有者會輪替**；真正只進不出的仍然是純 verifier（留存 99.4%，見 #62），而那一條有 (a) 在處理。原型四小時看到「一個 agent 拿走全部正餘額」很可能是**時間尺度太短**——84 天的模擬裡首位平均每 20 天就換人一次 |
| 63 | **P2** | `related-party` 是**自行申報**的：requester 在設定檔列出 `relatedTo`，收據就標成 related-party。Hub 完全無從查核兩個 DID 是否屬於同一個 Owner——沒有申報就是 `market`，而**想隱藏關聯方交易的人正好就是不會申報的人**。§20-9 因此在「可區分」這件事上只對誠實者成立 | 未修，屬設計層。可能的方向：(a) 由對手方共同簽署關聯聲明（仍可串謀）；(b) 從交易圖推論（同一對高頻互惠、資金環路——這正是 F-1 反洗量折減在做的事，可以共用訊號）；(c) 接受它，並在指標裡把「自行申報的 related-party」與「圖推論疑似關聯」分成兩欄。(b)＋(c) 最務實，但需要先定義推論門檻，屬模擬工作 |
| 62 | **P1**（2026-09-18 由 P2 升級，實測證據見右欄）| **Verifier 是純吸收端**：它們賺驗證費但沒有任何需求模型，永遠不買東西。40 分鐘 soak 實測：三個 verifier 各持 +11.9 CC（合計 35.7），那些錢永久離開交易流通。FR-056 點名的「正餘額無處可花」就是這個形態，但 `demo-autonomous` 的吸收端檢查**只看交易 agent、不看 verifier** | **2026-09-18 實測把它從 P2 推成 P1，而且選定了 (a)**。#71 的回流把 protocol 帳戶的吸收從成交量的 8.5% 壓到 0.4%，於是 verifier 成為**唯一**剩下的吸收端；`soak-n6`（6 交易者、3 verifier、40 分鐘、1192 筆）逐帳戶量到：

| 帳戶 | 餘額 | 角色 |
|---|---:|---|
| ×3 | **+70.49／+70.01／+69.94** | verifier |
| ×2 | +4.50／+0.90 | 交易者 |
| ×4 | −12.66／−69.05／−82.97／−84.30 | 交易者 |

**verifier 持有 210.44 CC ＝ 全部正餘額的 97.5%**，而交易者總負債 249 ≈ verifier 210.44 ＋ protocol 18.15 ＋ 押注 15。也就是說**交易者的負債幾乎全部就是 verifier 的持有量**。而且它隨規模惡化：N=3 時 67.63 → N=6 時 210.44，因為驗證費是成交量的 4% 而成交量翻倍。同一輪也**否證了 #64 的擔心**：六個交易者圍繞零到負分佈，沒有出現結構性順差者。

**選 (a) 而不是 (b) 收費，理由是三個**：(i) verifier 無處可花不是它們的問題，是原型把 verifier 與 agent 拆成兩種生物造成的——SDD 裡「驗證」是**角色**不是物種（FR-041 從 pool 抽選），擁有 verifier 節點的人一樣想要算力；(ii) (b) 等於「verifier 領得更少」，那是換一個政策不是修一個缺陷；(iii) (a) 順手把交易者從 6 變 9，**直接打到 #61 的主因**——同一輪也在原型上確認了 #61 的 N 主張：信用使用率 N=3 的 52.0% → N=6 的 **36.0%**，貼上限（≥90%）0/3 → 1/6，最長空窗 183s → 131s。

**(a) 的工作量與風險已盤清，分四階段，第一階段已交付**：Hub 目前每個 DID 只有一個 `role`，而有 8 處嚴格過濾 `role === 'verifier'`——其中兩處把「付給 verifier 帳戶的任何分錄」當驗證費去託管押注，所以**不能只是讓 verifier 也交易**，那會把 provider 的收款誤託管。
1. **（已交付）當事人不得進入自己合約的 verifier pool**。今天不可能發生（角色互斥、`list_verifiers` 只廣告 verifier），但 pool 是 requester 在合約時**自己釘的**、Hub 只比對雜湊是否自洽——**雜湊自洽不代表內容合法**，所以角色一旦重疊，「自己裁判自己」只差一個設定檔。守門放在 `validateSchedule`（雙簽與強制結算兩條路共用），agent 端同步排除以確保**雙方算出同一個 pool**（否則就是 #72 那種推導不一致導致結算做不成的形態）。紅隊 S16 驗證：兩個串謀身分送出**簽章全部有效**的收據把自己放進 pool，被明確拒絕。
2. Hub 的 role 改為可重疊的集合（`roles`），8 處過濾改成成員判定。
3. verifier 內核搬進 `agent.js`（`verify: true`），一個進程兼兩個角色，`verifier.js` 逐步退場。
4. 量測 3 個雙角色節點 vs 6 交易者＋3 verifier，看 #62 與 #61 是否一起改善。

(c) 的指標分離仍然值得做（否則「流通中的 CC」被高估），但它是報表而不是修法。**2026-09-19 的 N=20 量測把這條的嚴重性分成兩半**（數字取自期末快照，見 #76）：verifier 佔全部正餘額從 97.5%（N=6）掉到 **44.7%**（N=20），**但絕對持有量從 210.44 CC 漲到 547.52 CC**——占比下降只是因為交易 agent 之間也開始累積，不是因為 verifier 少吸。**而新長出來的那一半更難看**：單一 agent 持有 **673.07 CC**（全部正餘額的 55%），峰值負債只有 16.3 CC／額度 500 CC——它幾乎沒借過錢。這正是 #64 的結構性順差者在 N=20 的樣子，比 N=3 時量到的 +73.97 CC 大一個數量級，**也直接否證了本條目上面「六個交易者圍繞零到負分佈，沒有出現結構性順差者」那句話在 N=20 仍然成立的推定**。**所以提高 N 沒有解決吸收端，只是把吸收端從 verifier 換成了一個順差 agent**，而 (a)「讓 verifier 也參與市場」只處理其中一半。第二輪（#76 修好之後）同一個結論、數字更大：非交易角色 599.73 CC／41.2%，單一順差 agent **+856.85 CC**，峰值負債 11.1／額度 500。**2026-09-20 的四小時耐久跑把這一條從「多嚴重」變成「會走到哪裡」**（`scenarios/soak-4h.json`，N=20、14,400 秒、13,026 筆結算、7/7 PASS）：**verifier 持有全部正餘額的 100.0%（2,602 CC），交易 agent 手上的正餘額歸零**——最高的三個 agent 是 −20.64／−25.68／−36.15，沒有任何一個是淨債權人。對照 40 分鐘的 46.0%：**那 46% 不是一個性質，是一個還沒跑完的過程的中途值**，所以先前「提高 N 稀釋了吸收端」的讀法是錯的——verifier 賺費而永遠不買東西（FR-056），那是**單調的抽取**，N 只改變速度、不改變終點，而所有 ≤40 分鐘的長跑都系統性低估它。同期信用使用率從 29.1% 升到 **53.0%**（峰值 66.0%）、曾貼上限 ≥90% 從 10/20 升到 **14/20**、≥99% 從 0/20 升到 **4/20**。**這是目前為止支持 (a)「讓 verifier 也參與市場」最強的證據，也說明它不能再延**——§20 十項沒有任何一項因此失敗，但 §5 的「經濟閉環」在四小時尺度上依賴這一條被解掉。**2026-09-20 階段 4 已量測，結論是 (a) 必要但不充分**（`soak-4h-dual` vs `soak-4h`，同種子、同故障時間軸、四小時、兩邊 7/7 PASS）。**物種級的吸收端確實被移除**：沒有任何非交易帳戶，結算量 13,026 → **15,733（+20.8%）**、正餘額總額 2,602 → **2,102 CC（−19.2%）**、信用使用率 53.0% → **40.8%**（峰值 66.0% → 54.4%）。**但終局形狀沒變：仍然是一個帳戶持有全部正餘額**，只是從「三個 verifier」換成「一個交易 agent（C，2,102.39 CC ＝ 100%）」，而第二高的帳戶是 **−0.52**。關鍵在 C 的峰值負債只有 **11.2 CC／額度 500**——**它幾乎沒買過東西**，行為上與 verifier 無異。所以 (a) 解掉的是「這個角色在設計上不消費」，沒解掉的是「**這個參與者在需求上不消費**」，後者是 #64。**一個指標反而變差**：曾貼上限 ≥99% 從 4/20 升到 **8/20**（≥90% 則從 14/20 降到 13/20）——總量使用率下降的同時個別貼牆變多，因為驗證費在雙角色下也流向 C 這種淨賣方，加速集中。**建議**：(a) 收尾（有正面效果、已實作到階段 3），但**瓶頸移交給 #64／FR-056**，下一支槓桿要從 §2.2 的 Treasury 支出側或 #66 的保管費裡選——#66 掃描的結論是「費率必須超過累積者的收入率」，而 C 的收入率現在有實測值可以對照。**2026-09-21 GATE-0 v3 量到 (a) 的效果上界**：80 次全量跑（4 情境 × 10 種子 × 2 人口）顯示 verifier 留存率在獨立人口是 **99.9% [98.5, 100.0]**、雙角色是 **50.1% [3.2, 87.7]**。**平均降一半，但最差的種子仍到 87.7%，逼近水槽**——所以 (a) 是「大幅降低」不是「解決」，與同日四小時真機的 −67%（不是 −100%）一致。**GATE-0 的 G8 因此改用留存率並把門檻訂在 95%**（由這個分佈訂出，不是憑空選）：獨立人口 40 次跑全部會紅，候選組（風險費 6%/2% ＋ 雙角色）八判準全過 |
| 61 | **P1** | **小型網路會因反洗量折減而鎖死（原標題歸因於手續費，已由掃描修正）**。40 分鐘無人 soak 實測：292 筆結算後，三個交易者餘額合計 **−122.53 CC**、protocol 帳戶合計 **+122.53**（保險 75.90、Treasury 31.63、押注 15.00），三人全部貼著各自的信用上限（−52.2/54.8、−52.7/54.1、−53.2/55.2），**最後 12 分鐘零成交**。兩個機制疊加：每筆交易 8.5% 永久離開流通（Treasury 2.5%＋保險 6%，另加驗證費託管），而 F-1 的反洗量折減（單一對手 ≤ 其他對手加權量的 20%）在只有三個交易者時幾乎把 E_eff 壓成零——額度只從 50 爬到 54–55，抽乾速度快過額度成長。**模擬器有 `write_off`（保險吸收違約），原型一條回流路徑都沒有**；而 §2.2「Treasury 啟動」設計的回流（金絲雀、逆週期收購、L_boot 補貼）在 soak 中一個都沒啟用 | **已量測，且結果修正了本條目的診斷**（`amcn_sim.sweep_size`，N ∈ {3,5,10,25,50,100} × 費率 {0%,3%/1%,6%/2%}，84 天，無違約者）。**費率幾乎不是原因**：零費率對照組與 6%/2% 的「貼上限比例」完全相同（N=3 時都是 66.7%），protocol 帳戶只吸走結算量的 1.3–4.8%。**驅動力是 N**：貼上限比例 66.7%（N=3）→ 60%（5）→ 49%（10）→ 25%（25）→ 18%（50）→ 15%（100），單調遞減；觸底在 N≤10 時第 17–28 天發生，N≥100 在 84 天內沒有發生。機制是 F-1 的反洗量折減本身——對手太少時 E_eff 幾乎為零、額度長不大，與手續費無關。**所以這條的標題「手續費單向匯出」是錯的**：手續費是次要項，反作弊摩擦與冷啟動的張力才是主項（三份評審對 proposal C 都提過這個張力，這次有了數字）。**可用的設計輸出：目前參數下最小可行網路規模約 50 個 agent**。**2026-09-18 在原型上確認了 N 主張**（`soak-n6` vs `soak-rebate`，各 40 分鐘、同種子同故障時間軸）：信用使用率 52.0%（N=3）→ **36.0%**（N=6），貼上限 0/3 → 1/6，最長空窗 183s → 131s，結算 509 → 1192。方向與模擬一致。**但它沒有解決 #62**——同一輪量到 verifier 持有 210.44 CC、佔全部正餘額 97.5%，提高 N 反而放大它。原型 soak 觀察到 100% 貼上限而模擬 N=3 是 66.7%。**差額已分離驗證，兩個候選解釋都不是**：模擬的 `write_off` 只在 deadbeat 離線 ≥14 天時觸發，而掃描設 `deadbeat_frac=0`，所以模擬裡根本沒有 write-off 發生；verifier 在模擬裡同樣獨立於 agents、同樣是純吸收端。真正的原因是**人口組成**——N=3 在五個亂數種子下的貼上限比例是 66.7／66.7／66.7／**0**／33.3，有結構性淨賣方時小網路完全健康（131 CC 結算、零貼上限）。原型的 100% 是最壞情況，因為我把三個 agent 配成同需求同容量、只錯開相位，**結構上沒有人是淨賣方**——也就是說我自己的 soak 在不知情的情況下一直在量最壞情況。見 #64。**2026-09-19 的第三個點推翻了上面那句「方向與模擬一致」的一半**（`soak-n20`，N=20，同種子、同故障時間軸、同回流設定；數字取自 Hub 期末快照而非 runner 報表，原因見 #76）。**兩個指標指向相反的方向**：總量信用使用率延續下降，52.0%（N=3）→ 36.0%（N=6）→ **28.9%**（N=20）；但用本條目自己那個 **≥90%** 的定義，曾貼上限是 0/3 → 1/6 → **9/20**，也就是**上升**。兩者能同時成立，而其中一個是統計假象：N=20 的額度總計 4605.7 CC，被幾個結構性順差者**沒用到的**額度灌大（一個 +673 CC 的順差者帶著 500 CC 額度、峰值負債只有 16.3），**所以總量使用率不是「網路會不會鎖死」的指標，逐帳戶的峰值分佈才是**——九個赤字 agent 的峰值使用率密集落在 93–98%。**同時記下一個會翻轉結論的定義問題**：同一輪在 ≥99% 門檻下是 0/20（沒有人真的碰到天花板），所以 `chaos-run` 現在兩個門檻都報；而模擬 `sweep_size` 的貼上限比例是**第三個定義**，因此 9/20 既不能宣稱證實、也不能宣稱推翻掃描在 N=25 預測的 25%。**可靠的新資訊在額度那一側**：N=20 時單一 agent 的額度長到 274–500 CC（starter 50），因為 E_eff 隨對手數成長——「N 變大就不鎖死」的機制是額度長得比抽乾快，而那個成長對赤字者並不夠快。**同一情境第二輪**（修好 #76 的取樣之後）：10/20、期末使用率 32.3%、額度總計 4869.4 CC——與第一輪（9/20、28.9%、4605.7）同一個圖像，但兩輪**同種子**仍有這個幅度的差距，所以這些數字要當區間讀，不是點估計。**順帶修正一個讀法**：本欄記的「最長空窗 183s（N=3）→ 131s（N=6）」看起來像 N 的效果，而 N=20 的兩輪一樣是 131s，且那一段正是 Hub 停機的窗口——**那個指標量到的是故障時間軸，不是市場性質**。**2026-09-20 補上時間維度，而它與 N 維度方向相反**：同樣 N=20，40 分鐘的期末信用使用率 29.1%、曾貼上限（≥90%）10/20；**四小時是 53.0%（峰值 66.0%）、14/20，且 4/20 曾貼到 ≥99%**。也就是說 N 變大讓額度長得比抽乾快（本欄的結論成立），但**時間讓抽乾追上來**——兩者是不同的軸，而先前所有結論都只量了一個軸。成因見 #62：verifier 的單調抽取 |
| 60 | **P1** | **原型第一天就給兩倍的開機信用，而且這推翻了我對 #1 的裁決**。E5 的跨語言對照（同一組流水餵 `sim/amcn_sim/agents.py` 與 `node/lib/eeff.js`，逐步比對）顯示：**每一步都不一致，差距恆為 25.00 CC**。掙來的部分（E_eff、多樣性、quality）兩邊完全相同——分歧 100% 來自模擬器有年齡斜坡 `starter × (0.5 + 0.5 × min(1, age/30d))` 而原型把它固定為 1。於是新帳號在模擬器是 23.13 CC、在原型是 46.25 CC。**因此 §2.2 的「L_boot 上限 25 CC」從來不是過時數字**：它描述的是 t=0 的有效額度，而 GATE-0 的「starter 50」講的是參數。兩者都對，是 #1 把它們混為一談——**而我 2026-09-16 的裁決往另一個方向重複了同一個混淆**，把 §2.2 改成「上限 50」。原型那個「年齡係數固定為 1」被註解為 demo 簡化，但它實際上是把反 Sybil 相關的開機信用放大一倍（見 #50） | **已修**：`eeff.creditLine` 接受 `ageFactor`，Hub 記錄每個 DID 首次註冊時間（`joinedAt`，隨帳本持久化——否則 #17 的身分延續等於白做）並以 `HUB_AGE_RAMP_MS`（預設 30 天）換算。demo 與各 harness 顯式把斜坡壓縮成 1ms，所以既有行為不變，而且「這支 demo 壓縮了年齡斜坡」寫在各自的 spawn 裡看得見。修後 **75/75 步完全一致，最大分歧 0.00 CC**。工具留下：`node cl-compare.js` |
| 59 | **P1** | **殺價後不交付可以癱瘓整個市場，成本趨近於零**。選標只看報價（FR-012 是 requester 職權），而 Hub 的 `completed/failed` 只餵信用額度、**完全不影響得標**。實測：一個 provider 以 0.5 CC/unit 殺價（誠實方 1.0）贏下**每一場**拍賣後什麼都不交付——錢是安全的（無交付即無結算即無付款），但**結算數歸零、市場停止運作**。攻擊成本：身分免費、provider 無押金、失敗零代價。SDD §16 威脅 4「收 Credit 不執行工作」通常被讀成竊取，這是它的另一半：阻斷服務。發現方式：紅隊第二批的 never-deliver 對手 | **已修**（requester 側，不動協議）：agent 自行記錄每個 provider 的 won／delivered／failed，選標改用**有效價＝報價÷可靠度**（Laplace 平滑，新手起始等於 1.0，避免把冷啟動問題變嚴重），且「失敗 ≥2 次且零交付」者直接跳過。這是本地評分，符合 A 案 reputation 分層（事件層與評分層分離，評分模型可各自選擇）。實測修後：攻擊者拿到第一單、不交付、被放棄一次，之後誠實 provider 接手完成全部後續結算 |
| 58 | **P1** | **一個沉默的 verifier 就能讓它被選中的每一筆合約永遠無法結算**。#26 修了 Hub 側（只付「揭示內容能開啟事前承諾」者），但 **requester 建收據時仍付給推導出的整個 panel**。於是 2-of-3 quorum 成立、驗收通過，Hub 卻以 `verifier postings 3 != accountable attesters 2` 拒絕，而 requester 不會調整重試——沉默可能來自崩潰、斷線或惡意。既有測試全都涵蓋不到：demo 的三位都會揭示，金絲雀的偷懶者也會揭示（它投 PASS 但有揭示）。發現方式：紅隊第二批新增 `silentReveal` 對手 | **已修**：requester 依「揭示內容能開啟承諾」過濾出可歸責者，只對他們列分錄（quorum 仍需 ≥2）。付款人數與 Hub 的重新推導一致，結算恢復。實測：panel 3 位其中 1 位沉默時，每筆付 2 位、沉默者 0 CC。**修的過程本身揭露 #5 與 #26 的衝突**：一到 quorum（2 位）就結算，會讓第三位只因晚幾毫秒揭示而拿不到錢，另外兩位反而多分——#5 說「付給整個 panel 均分」、#26 說「只付可歸責者」，兩者只在有人沉默時衝突。解法是揭示側也加寬限期（與 #36 在承諾側同一形狀）：quorum 達成後等一個 `revealGraceMs`，全員揭示則提前結算。健康情況付全 panel，真正沉默者只讓交易慢一個寬限期且拿不到錢。另外付款集合與**附上的 attestation 集合必須是同一組**——先前只過濾付款、證據照舊全附，Hub 因而數到更多可歸責者而拒絕，`dual` 靜默變成 `forced` |
| 57 | **P1** | **無人 soak 的實測成長**，以及對 #41 成因判斷的第二次修正。40 分鐘 soak 在第 20 分鐘被作業系統以記憶體不足殺掉；它在死前記下了斜率。量測結果：Hub RSS 在 90–110MB 之間呈 GC 鋸齒、**不是失控洩漏**；真正線性成長的是匯出檔 **360 KB/分**，而它每 2 秒被整份重寫。成分（20 分鐘、295 筆結算）：`raw_log` **5,351 KB（72%）**、chains 753 KB、checkpoints 775 KB、receipts 315 KB、events 221 KB——**真正的帳本只有約 1.3 MB**。**這同時修正了我先前對 #41 的修正**：閒置時主因是週期 checkpoint，活躍交易時主因是 `raw_log`；兩次量測都對，條件不同而已。記在這裡是因為「在什麼條件下量的」必須跟數字一起走，否則下一個人會再外推一次 | **已修**：(a) `raw_log` **不再進災難匯出**——恢復需要的是 receipts／events／chains／checkpoints／pubkeys，`lib/rebuild.js` 也只驗這些；audit log 是活的除錯工具，留在 `export` 回應裡供 NFR-005 明文掃描用。(b) 記憶體中的 `rawLog` 加環狀上限（`HUB_RAW_LOG_MAX_BYTES`，預設 2 MB），因為掃描需要的是近期窗口而不是全部歷史。實測修後：匯出檔 **95 KB/分（−74%）**、Hub RSS 0.8 MB/分、209 筆結算下不變式零違反 |
| 56 | **P1** | http transport 的請求處理**不在 #13／#34 的例外隔離範圍內**：handler 內一個 `ReferenceError` 直接殺掉 Hub 進程，而症狀是所有 client 收到 ECONNREFUSED——看起來像「Hub 從來沒起來」，正好是最容易誤診的形態（試點上就誤判過一次）。實際觸發：我把 hooks 改成每連線工廠時，POST handler 引用了宣告在另一個請求作用域的變數 | **已修**：`http.createServer` 的 handler 包在 try/catch 裡，例外降級為 500 並記錄；每連線的 hook 狀態改與 channel 一起存在 `open` map 裡，而不是靠作用域碰巧看得到 |
| 54 | **P0** | **匯出檔的 `pubkeys` 來自當下連線中的 agents**，而不是持久化的紀錄。所以在無人連線的時刻取的匯出**無法驗證自己的任何一筆歷史收據**（`rebuild` 逐筆驗簽時公鑰查不到）。危險的不是稽核不便而是資料遺失：`HUB_DUMP_PATH` 每 2 秒覆寫同一個檔，只要有一次寫在那種時刻，好的備份就被壞的取代，之後 `HUB_IMPORT` 會拒絕啟動、帳本救不回來。#17 加自動匯出正是為了「不靠人記得執行」，結果它可能自己毒化備份。發現方式：`hub-moves` 情境在 Hub 換位址重啟後，取樣器回報**所有**收據簽章失效 | **已修**：Hub 持有獨立的 `pubkeys` Map（註冊時寫入、匯入時還原、匯出時輸出），不再從連線狀態推導；`lib/rebuild.js` 一併回傳公鑰讓接手的 Hub 延續。修後同情境 69 筆結算、不變式零違反 |
| 46 | **P0** | **靜默中斷時雙方都是瞎的**：連線被靜默切斷（封包被丟棄、連線不關閉）時，Hub 與 client 都察覺不到任何事。實測：注入 `blackhole` 後 120 秒，Hub 仍回報 3 位 verifier 在線、`list_verifiers` 持續把死掉的 pool 交給 Agent、Hub 零次偵測、verifier 也零次自認斷線（所以 #40 的重連永遠不會觸發）。後果比「全網停止成交」嚴重：Agent 會得標 judge-quorum 合約、把 verify_request 送進虛空、永遠拿不到 attestation——**合約產生了但永遠無法結算，且沒有任何一行 log 說明原因**，正是 #37 註明「最難查」的那個狀態。#35 只處理乾淨關閉；三台機器試點的情境 A 想測這個，但關 Wi-Fi 時 OS 送出 RST，兩秒就被偵測，測到的其實是乾淨關閉 | **已修**：`lib/channel.js` 加對稱的應用層活性——閒置就送 `ping`（預設 5s），對方靜默超過 `IDLE_TIMEOUT_MS`（預設 20s）即視為消失並關閉 channel。Hub 端關閉＝標記離線（#35），client 端關閉＝開始重連（#40），一份實作同時服務兩個方向與兩種傳輸。**刻意不用 TCP keepalive**：node 的 `setKeepAlive` 只設閒置時間，探測間隔與次數由 OS 決定（macOS 預設換算下來約十分鐘），而且它看不見「socket 活著但應用層卡死」的對手。`ping` 不進 `rawLog`（否則會膨脹每一份匯出，見 #41）。實測修後偵測時間 **20–25 秒**，兩邊皆然。**發現方式本身是這次的重點**：由 `lib/transport-chaos.js` 自動注入，不需要任何人拔線 |
| 45 | **P1** | 「協議內發現與輪替」只在區網成立：`lib/discovery.js` 是 UDP 廣播信標，跨網段／NAT／公網一律聽不到。Owner 裁定第一個可運行版本的驗收環境是**不同地點的多台機器**，那個環境裡 `hubHost` 只能是固定的公開位址——於是搬移 Hub 又變回「每台手改設定」，正是 #14 要消滅的事。§2.1 允許 Transport 中央化的三個條件之一因此在目標環境中不成立 | **已修**：`lib/rendezvous.js`——Hub 發布一份**簽署的**位址記錄 `{host, port, pub, ts}`，client 每次重連嘗試都重新解析（#40 本來就重新解析，這讓那件事在 WAN 上有了意義），`hubPin` 決定信不信。**承載記錄的主機是不受信任的基礎設施**：它可以扣住或提供過期記錄，但無法冒充——記錄帶 Hub 簽章，client 對照釘住的 DID。刻意不做成 AMCN 要運營的服務，記錄就是一個檔案，放物件儲存、靜態網站、repo 都行；本機路徑也可以，chaos 情境就是這樣在零基礎設施下演練輪替的。`hub-moves` 情境實測：殺掉 Hub、換一個埠以同 seed 從匯出重建並發布新記錄，**沒有人改任何設定**，69 筆結算、交易在 12 秒內恢復、不變式零違反 |
| 44 | **P1** | 傳輸層無任何加密。訊息有簽章、payload 有 E2E（NFR-005），但**信封 metadata 是明文**：誰跟誰交易、金額、時間、acceptance 方式、DID 全都在線上可讀。在受信任區網可接受（INSTALL §8 明確這樣寫），但 Owner 裁定驗收環境是不同地點的機器——流量會經過公網，§16 威脅 9「Metadata 洩露 Owner 身分、工作內容或模型使用習慣」就對任何路徑上的觀察者完全敞開 | **已修**：`lib/transport-secure.js`（第四個 ITransport 實作）。**刻意不是 TLS**：node 無法在不外呼 openssl（不是每台都有，且本專案零相依）或手刻 ASN.1（安全關鍵的東西不該手刻）的情況下簽發 X.509；更重要的是憑證鏈在這裡是錯的信任錨——系統裡沒有 CA，而 client 早就以 DID 釘住 Hub。所以握手直接驗**身分**，用的是 `lib/e2e.js` 已經在用的原語：臨時 X25519 → HKDF → AES-256-GCM，臨時金鑰由長期 Ed25519 身分簽章，方向分離的金鑰綁雙方 DID（記錄下來的 frame 無法重放到另一個方向或另一場連線）。實測側錄：路徑上的觀察者只看得到握手與密文，訊息型別與 payload 都不可見。**不取代簽章與 E2E**——那兩層防的是 Hub 本身，這層防的是路徑上的第三方，兩者是不同的攻擊者。`demo-transport.js` 現在斷言**三種實作產生同一本帳**（fingerprint `c2d81b96…` 完全相同）|
| 43 | **P2** | 跨平台診斷不存在：機器 2（Windows）連不上時，唯一的排查方式是逐條貼 `grep`／`nc`／`curl` 的輸出，而那些指令在 Windows 上不存在——與 #32（Verifier 需要 shell 引號 JSON）同一類問題，同一個根因：文件與工具預設了 Unix | **已修**：`pilot-doctor.js` 只用 node，三台都能跑，一個指令依序檢查 node 版本、本機位址是否與 Hub 同網段（跨 VLAN／訪客網路／VPN 是最難從錯誤訊息看出的原因）、設定檔 JSON 合法性與它實際指向哪、是否有固定 seed、TCP 可達、**AMCN 協議層可達**（送 `export` 並等回應——埠開著不代表協議通，傳輸實作不同或版本不符只會在這一關現形）、UDP 信標是否聽得到，最後指出第一個 FAIL 作為最可能原因 |
| 42 | **P2** | 長時間執行的 log **沒有任何時間戳**。發現時機：三台試點的 panel 掉線後，唯一可用的時間線索是 log 檔的 mtime。所有 demo 只跑 15–35 秒，順序就夠用、時鐘是雜訊——但 W10 演練要量的核心數字正是「多久才發現斷線」與「接手花了多久」 | **已修**：`lib/log.js` 在 hub／agent／verifier／canary／panel 安裝 UTC ISO-8601 前綴。**刻意包裝 console 而非逐一改呼叫端**：斷線時最關鍵的那幾行來自 `lib/channel.js` 與 `lib/transport-*.js`（`socket error`／`disconnected`／`reconnected`），應用層 logger 碰不到它們。用 UTC 是因為演練要跨三台機器對時間軸，本地時間會在最糟的時刻引起時區或日光節約的爭論。demo harness 刻意不安裝——它們的輸出是要被比對的驗收報告，不是時間軸。`AMCN_LOG_TIME=0` 可關閉 |
| 41 | **P1** | 自動匯出的成本隨歷史無上限成長，且 `HUB_DUMP_PATH` 每次都重寫整份匯出（`JSON.stringify` 整段歷史，在事件迴圈上阻塞）。所有 demo 只跑 15–35 秒，這個量級從未被觸及 | 未修，但**成因已量測，且推翻了本欄原本的假設**。原記：主因是 `rawLog`（保留每一行、供 NFR-005 明文掃描），依據是 `demo.js` 9 秒累積 38.5 KB。真實試點（三台、慢 33 倍節奏）實測 **1 小時後 718 KB，其中 0 筆收據、0 筆事件、`raw_log` 僅 1,664 bytes、checkpoint 3,246 個**——主因是**週期性 checkpoint**，與交易量完全無關：`CHECKPOINT_MS` 預設 1200ms，即每小時約 3,000 個，閒置也照長。推估：一天約 17 MB、一個月約 500 MB，而且每個 dump 間隔重寫一次。教訓與 #39 同型：demo 尺度的量測外推到試點尺度時，連「誰是主因」都會錯。修法不能是「閒置就不鑄造」——#24 的 bootstrap 修復正需要 checkpoint 無條件產生，否則未來-checkpoint 抽選的種子永遠不會到。選項：(a) dump 改增量；(b) 匯出只帶最近 N 個 checkpoint＋一份簽署摘要，完整歷史另存；(c) 調高 `HUB_CHECKPOINT_MS`——但它同時是 panel 種子的等待時間，調成 12s 會讓每筆 judge-quorum 任務的驗收最多延後 12s，**帳本成長與驗收延遲是同一個旋鈕的兩端**，這個耦合先前沒人注意到。**2026-09-19：主因第二次被實測推翻，而這次是「每個 checkpoint 有多大」而不是「有多少個」**。#74 已經交付選項 (a)（快照＋附加尾檔，dump 不再是 O(全部歷史)），而週期性 checkpoint 的數量問題也早被 `CHECKPOINT_KEEP_MS` 的稀疏保留吃掉（閒置時 root 不動就不保留，每小時約 60 個而不是 3,000 個）。於是量了 N=20 四十分鐘那份 **18.74MB** 匯出的組成：**checkpoints 8.56MB（43.8%）、chains 6.02MB（30.8%）、receipts 3.37MB（17.3%）、events 1.51MB（7.7%）**——真正簽署的當事人內容（收據）只佔 17%。再拆一個 checkpoint：2782 B 裡有 **2496 B 是 `cp.heads`**，而 `root` 就是 `sha256(canon(heads))`，**heads 與 root 完全冗餘**。而且**沒有任何消費端讀它**：`demo.js`、`lib/invariants.js`、`lib/rebuild.js`（含 #78 新加的 root 對照）全都是自己**從 chains 重算 heads** 再跟 root 比，簽章覆蓋 root 就已經覆蓋了它們。**修法**：`heads` 移出被簽署的 cp，協定 **v5 → v6**。**A/B（同情境、同種子、同時長）：匯出 3.27MB → 1.73MB，−47.1%；每個 checkpoint 2665 B → 300 B（8.9×）；checkpoint 佔比 55.2% → 12.0%**。取證能力沒有消失——歷史 checkpoint 的逐帳戶 heads 仍可從匯出的 chains 回推（分錄帶 `receipt_idx`，對照 `cp.receipts_count` 即得當時的前綴），只是要算。**但這是常數倍的勝利，不是成長級數的改變**：匯出仍隨歷史無上限成長，N=20 實測 432 KB/分，16MB 的 wire 上限只是從約 40 分鐘推到約 75 分鐘。**剩下的兩半**：(i) 分頁／增量的 wire 匯出（#76 殘留）——真實第三方客戶端仍然拿不到超過一個 frame 的帳；(ii) `chains` 現在是最大的一塊，見 #79。閘門：`demo` 26/26、`demo-rebuild` 7/7、`demo-reconnect` 8/8、`demo-canary` 6/6、`demo-autonomous` 12/12、`demo-transport` 8/8、`redteam` 43/43（含被改寫的分叉構造：cp 不再帶 heads，分支直接改 root）、`redteam-agents` 13/13、`n20-smoke` 4/4。**(i) 已於同日交付**：`export` 支援游標分頁，且**不分頁的請求在匯出過大時回一個指名的拒絕**（`ledger_export_too_large`，帶 bytes／max／該怎麼改）而不是送出一個超過 `MAX_LINE` 的 frame 讓收方靜默斷線——那個靜默正是 #76 的成因。**分頁一律從凍結的快照供應**（第一頁建立 token，之後每頁從同一份切，TTL 120s）：逐頁現算會讓第 1 頁的 receipts 與第 3 頁的 chains 來自不同的帳，重建出來的東西不對應任何真的存在過的狀態。純量欄位（balances／credit_lines／metrics／hub_pub…）只在第一頁；chains 逐帳戶整條給，因為切一半就沒辦法驗雜湊鏈結。客戶端側收進 `lib/ledgerfetch.js`：收到拒絕就自動改走分頁、跟著游標、重組，呼叫端只換一行——`ledger-dump.js`（災難恢復工具，從前在最需要它的時候什麼都拿不到）、`pilot-doctor.js`、`chaos-run` 的取樣都已改走。**閘門**：`demo-rebuild` 的排序器 A 把 `HUB_EXPORT_PAGE_BYTES` 調到 20KB，於是那支 demo 的每一次取帳都是分頁取回來的——七項既有斷言因此同時是分頁路徑的閘門（實測 3 頁重組、rebuild ok），另加兩條新斷言：「確實分頁而不是悄悄退回單頁」（沒有它，預算失效時整支 demo 會安靜地全綠——#76／#78 各犯過一次的錯）與「不分頁的客戶端得到指名的拒絕」（實測 192,427 B > 20,000 B）。9/9。**仍然沒有解決的是成長本身**：分頁讓「拿得到」與帳本大小脫鉤，但沒讓帳本變小，每頁仍是 O(頁大小) 的序列化成本，而 #79 的 chains 才是下一塊。**2026-09-20 的四小時跑驗證了分頁在真實量級上的正確性**：匯出在第 ~75 分鐘跨過 16MB，之後**每一次取帳都是分頁**，114 次取樣全部成功、最大約 52MB，七項不變式零違反——也就是 #81 的凍結快照在這個量級撐得住反覆的大型分頁 |
| 19 | **P2** | `now providing` 由 `agent.js` 的純計時器觸發，不檢查 Hub 連線狀態：一個從未註冊成功的 agent 在 `provide.afterMs` 後照樣印出該行。試點中因此誤判機器 2 已上線 | **已修**（W7 `d821eca`，本表一度漏標）：改為 `supply armed at N CC/unit, strategy X`，並在沒有 hub socket 時附 `WARNING: not connected to a hub yet`。試點重啟時實測有效 |

---

## 5. 12 週整合實作計畫

| 週 | 交付物 | 來源 |
|---|---|---|
| W1–2 | 重算三張攻擊成本表（統一幣值錨定）；收據/合約/attestation schema v1 凍結（含 tx_class、Verifier posting、pre_authorization）；`sim/amcn_sim` 換裝 INV-C1＋E_eff 公式 | C 修正＋B §9 |
| W3 | **GATE-0**：全量模擬過八判準（壞帳率、還債週期、洗量 CL 壓制、到期崩價幅度…），經濟參數凍結。不過門檻即回 W1 調參，不得帶病開工 | C §17 |
| W4–5 | Local Node（TS）：Keystore（OS Keychain）＋雙程序 Key 隔離＋Provider Adapter＋政策引擎；Hub：排序器＋hash chain＋checkpoint | B §13 |
| W6 | **首次真實閉環**：3 節點、A 借 B 還，deterministic 驗收，帳可重建 | §20 驗收 1/2/4/7 |
| W7 | 還債排程器＋目標餘額區間策略；MCP server 需求側入口（3 tool） | B §8.4/§23.1 |
| W8 | **完整借用＋自動還債 demo**（§27 閉環全程零人工） | §20 驗收 3/8 |
| W9 | 驗證市場最小版：3 人 Judge quorum＋未來-checkpoint 抽選＋commit-reveal＋金絲雀（重算後預算）；爭議單次上訴 | C §7 修正版 | **原型現況**：未來-checkpoint 抽選、commit-reveal、Verifier 報酬入帳、押注託管、金絲雀＋門檻式懲罰皆已交付並有斷言。**爭議單次上訴延後至 Phase 2**：現行驗收是確定性 DSL，裁決可計算、爭議由重算解決，上訴沒有可仲裁的對象；判斷性驗收（LLM judge）依 SDD §19 在 Phase 2，屆時才有意義。|
| W10 | **強制里程碑（不可砍）**：第二 ITransport 實作＋帳本匯出重建＋官方基礎設施拔線 4 小時演練 | B §13.3＋A §18 | **原型現況**：帳本匯出重建已交付（`demo-rebuild.js`，7 項）；第二 ITransport 實作已交付（`demo-transport.js`，7 項——tcp 與 http 同一本帳）。**拔線 4 小時演練未做**，它需要真實時段與多台機器，不是程式碼能自證的部分。程式碼前置已備齊（#40 重連、#42 時間戳），秒級預演 `demo-reconnect.js` 8 項斷言通過 |
| W11 | 紅隊：50 案例（含 P0/P1 登記簿全部攻擊重放）；洗量/Sybil 模擬對照真實 CL 曲線 | B §17＋C §10 |
| W12 | 10–20 節點封閉試點；輸出 §20 十項驗收證據包與市場指標（成交率、供需深度、違約率、平均還債時間） | §20 驗收 5/6/9/10 |

**團隊**：4.5–5.5 FTE（B 案編制＋0.5–1 FTE 經濟模擬專職——評審認定 B 案原編制超載 40–60%，模擬與紅隊不得掛在同一位 QA 身上）。

---

## 6. 仍然不知道的事（不得假裝已解決）

繼承三案「現在不知道」章節的交集，這些不是架構能解的，只能用實驗證偽（SDD §26）：

1. **需求側意願**（§26-1）：額度用完的人是否願意承擔未來服務義務——W8 demo 後的封閉試點是第一個數據點；MCP 入口是最便宜的證偽工具。
2. **供應商 ToS**（§26-3）：任何提供者條款變更可使供給瞬間萎縮；架構只能降低（本機開源模型 fallback）不能消除。上線前需逐家法遵判讀。
3. **議價 Token 摩擦**（§26-7）：全 Agent 議價的推理成本可能吃掉 2–10% 任務價值（A 案爆點 1）；MVP 用規則式定價引擎壓低，學習型議價延後。
4. **正餘額效用**（§26-6）：正 CC 是否有足夠可花之處，還是終將倒逼現金兌回——這是對 P-04 的長期壓力測試，試點期監控「正餘額週轉天數」。
5. **模型身分證明**（§21-Q6）：三案一致承認無密碼學證明 Provider 用了宣告模型；整合方案是金絲雀＋統計偏離懲罰，其威懾力依賴抽查預算（P1-7/8 修正後仍是估計值）。

---

## 附錄：評選過程工件

- 提案：`docs/proposals/proposal-{A,B,C}-*.md`（各約 1,500 行，§22 十八項交付物）
- 獨立評審報告：`docs/evaluation/reviews/review-{A,B,C}.md`；其矛盾清單已整編為本文件 §4 登記簿
- Phase 0 模擬器：`sim/amcn_sim/`（守恆、還債閉環、四情境）
