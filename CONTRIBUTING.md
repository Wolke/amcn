# 貢獻指南

歡迎 PR。這個專案有幾條不太常見的規矩，而它們不是風格偏好——是這套東西
到目前為止能不騙自己的原因。先讀這一份會省下被退回的來回。

## 零相依

`node/` 是純 Node.js（≥ 20）標準庫，`sim/` 是純 Python 3.10+ 標準庫。
**沒有 `package.json`、沒有 `npm install`、沒有 `requirements.txt`。**

這是刻意的：任何人 clone 下來就能跑，而且供應鏈攻擊面是零。要引入相依之前，
先問「能不能用 100 行標準庫解決」——到目前為止答案都是可以。

## 每個宣稱都要指向一個可以重跑的指令

專案的判準是「不接受『文件說有』」。所以：

- 修了一個 bug → **補一個會紅的閘門**。閘門的價值在於它在修之前是紅的；
  一個從來沒紅過的斷言不證明任何事。
- 改了經濟公式（`sim/amcn_sim/agents.py` 的額度、`market.py` 的定價）→
  **重跑模擬並附上前後的指標差異**。經濟參數以模擬為回歸閘門。
- 改了協定（訊息、簽章本體、帳本不變式）→ 說明**版本要不要升**
  （`node/lib/wire.js` 的 `PROTOCOL_VERSION`）。判準是「舊版收到會不會靜默
  忽略而讓對方等一個永遠不來的答覆」。

## PR 之前要跑的

```bash
python3 -m unittest discover -s sim/tests     # 模擬器 21 項
cd node && node demo.js                       # 閉環 26 項斷言
cd node && node demo-rebuild.js               # 帳本可由匯出重建
cd node && node redteam.js                    # 協議層紅隊 50 案
cd node && node redteam-agents.js             # 惡意參與者 13 案
```

動到 `lib/channel.js`、`hub.js`、`agent.js`、`verifier.js` 這幾個大家共用的
檔案時，請把 `node/README.md` 執行清單裡的**每一支** demo 都跑過——它們合計
約五分鐘，而這幾個檔案一動就會同時影響所有路徑。CI 會跑一部分，但長情境
（`chaos-run.js scenarios/*.json`）只能手動。

## 引用用穩定編號，不要用行號

需求：`FR-050`、`NFR-006`、`P-05`、`§16 威脅 7`（`docs/AMCN-SDD-v0.1.md`）。
缺陷：`docs/evaluation/final-architecture.md` §4 登記簿的編號（`#38`、`#87`…）。

行號會漂，編號不會。程式註解裡也照這個規矩寫。

## 缺陷登記簿

找到一個真的缺陷（不只是 bug，是「這個設計在某個情況下不成立」）時，**在登記簿
加一列**：它是什麼、為什麼在此之前沒被發現、修了什麼、用什麼閘門盯住。

那份表是這個專案最有價值的文件，而它的價值來自**沒有被藏起來的東西**。
所以一列裡最重要的往往不是修法，是「它怎麼被找到的」——例如 #85 是「照著自己
剛寫的文件跑第二次」才撞到的，而那句話比修法更值得下一個人知道。

**未修就寫未修。** 紅隊裡標 `known-open` 的案子是**攻擊成功才算通過**——它鎖定
「我們知道它壞、壞法是這樣」，哪天壞法變了會紅。

## 註解寫「為什麼」，不要寫「做什麼」

程式碼已經說了做什麼。值得寫下來的是：為什麼是這個做法、試過哪個做法而它
為什麼不行、以及這一段是哪個缺陷的產物。這個 repo 裡大部分長註解都是某次
失敗的紀錄，那是刻意的。

## 文件語言

設計與評估文件是繁體中文（zh-TW）。程式註解中英皆可——現況是混用，因為寫的
時候哪個語言能把那件事說清楚就用哪個。不必為了統一而改既有註解。

## Commit 訊息

第一行講**結論**而不是動作：「#38 在原型上也修好了：拿得回來的保證金」比
「修改 hub.js」有用得多。內文說清楚為什麼、量到什麼、以及跑了哪些閘門。

## 授權

送出 PR 即表示你同意你的貢獻以 Apache License 2.0 授權（見 `LICENSE`）。

## 安全問題不要開 issue

見 `SECURITY.md`：用 GitHub 的私下回報。
