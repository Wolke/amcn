# 安全回報

AMCN 是**原型**（Phase 1，協定 v7）。它處理的是記帳單位（CC），不是錢，
也沒有現金贖回（FR-054）；但它會在你的機器上收聽網路、簽署訊息、並執行
驗收斷言，所以它的安全性是真的問題。

## 怎麼回報

用 GitHub 的私下回報：repo 的 **Security → Report a vulnerability**。
不要開公開 issue，也不要貼在聊天群裡——先讓它被修掉。

回報時最有用的東西，依序是：

1. **可重現的步驟**，最好是一段能跑的腳本。這個 repo 的攻擊測試都是這樣寫的
   （`node/redteam.js`、`node/redteam-agents.js`：先讓誠實拓撲產生真實的收據與
   證據包，再變造它們），所以一個新案子最快的形式就是往那裡加一案。
2. **它違反了哪一條**。穩定編號比描述精確：`docs/AMCN-SDD-v0.1.md` 的
   `FR-xxx`／`NFR-xxx`／`P-xx`／`§16 威脅 n`，或 `docs/evaluation/final-architecture.md`
   §4 缺陷登記簿的編號。
3. **影響**：它動得到帳（守恆、額度上限、雙簽）、動得到別人的身分或 key，
   還是只讓服務變慢。

## 已知不安全、不必回報的事

這些是**已經寫在文件裡的設計現況**，不是未知漏洞：

- **預設傳輸沒有加密**（`tcp`／`http`）：訊息有簽章、payload 有端到端加密，
  但信封是明文（誰跟誰、多少錢、什麼時候）。要離開受信任的區網就用
  `AMCN_TRANSPORT=secure`（每一台都要設），見 `node/JOIN.md`。
- **排序器看得到 metadata**。那是它的工作；防的是路徑上的第三方，不是它。
- **沒有沙箱**（§20-6）：驗收斷言的執行環境沒有隔離設計，Phase 2 範圍。
- **爭議只有一條路**：驗收 FAIL 之後沒有仲裁流程，只有反拒付的「強制結算」。
- **經濟參數未定案**：信用額度、費率、保證金以 Phase 0 模擬的 GATE-0 為準，
  真實網路上需要重新校準。
- **缺陷登記簿裡標「未修」的項目**（`docs/evaluation/final-architecture.md` §4）。
  其中 `known-open` 的攻擊在紅隊裡是**攻擊成功才算通過**——那是刻意的，
  它鎖定「我們知道它壞、壞法是這樣」。

## 不要拿它做的事

- 不要把 Hub 綁在公網而不先讀 `node/INSTALL.md §12`（連線／速率／註冊的預算
  在那裡，預設值對家用頻寬是保守的，但沒有任何 DoS 防護）。
- 不要放任何有價值的東西在上面。它沒有經過外部稽核。
